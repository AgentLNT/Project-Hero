# Project Hero 游戏架构优化方案

## 核心目标

1. **逻辑与表现完全解耦** — 逻辑层零依赖 UnityEngine 视觉 API（GameObject、Transform、Camera、Animator 等）
2. **完全基于逻辑帧判定** — 所有会改变战斗状态的逻辑在离散 Tick 中执行，杜绝 `Update()` 推进逻辑状态
3. **可测试、可从 Tick 0 重演、可规范化校验** — 逻辑层使用纯 C# 数据且不引用 `UnityEngine`；首版通过 Unity EditMode Test Runner 自动化测试，无需启动战斗场景；确定性承诺限定为相同规则版本、配置哈希和兼容运行环境
4. **回合制表面 × 实时制底层** — 单位轮流行动、时间预算分配；底层实时 Tick 支持主角并发普通行动、全单位威胁反应与动作交互

## 落地修订摘要

本方案的架构方向保持不变，但实施策略做如下修订：

1. **`Update()` 不是禁区，逻辑状态变更才是禁区。** 输入采样、UI 刷新、视觉插值、相机、特效仍可运行在 `Update()` / `LateUpdate()`；它们只能产生命令或表现，不能直接修改战斗逻辑状态。
2. **P0 范围收紧。** 首批改造只建立程序集边界、兼容壳、事件 Outbox、单位注册表、Tick 阶段骨架、状态机和确定性测试夹具；资源模型与回合系统在基础设施稳定后迁移。
3. **状态机前置。** 当前多个 bool 标志会放大重构风险，必须先收敛为 `UnitStateMachine`，再迁移动作、资源和回合逻辑。
4. **网格逻辑化提前。** 命中、占位、寻路和回放都依赖网格确定性，`LogicGrid` 不应拖到纯表现层重构之后。
5. **每阶段必须有重演/快照验收。** 同一初始状态 + 同一命令序列 + 同一 RNG 种子，从 Tick 0 运行到 N Tick 后必须得到完全一致的单位位置、血量、状态与事件日志。
6. **逻辑权威时间只使用整数 Tick。** 秒只用于配置录入和 UI 展示；动作持续时间、时间预算、Buff 周期和 AI 间隔进入逻辑层前必须一次性量化为 Tick。
7. **先定义确定性契约，再扩展 Tick 骨架。** 事件 ID、命令顺序、容器遍历、RNG、回合窗口切换和多方仲裁都必须有稳定规则，不能仅依赖“固定帧率”。
8. **用程序集边界强制解耦。** 最终逻辑程序集不得引用 UnityEngine；ScriptableObject 只作为编辑器配置，通过适配器转换为纯 C# 不可变定义。
9. **逻辑事件只描述战斗事实。** 逻辑层输出不可变语义事件，不直接请求震屏、顿帧或慢动作；表现策略由 Unity 适配层映射。
10. **多方仲裁不是两两循环。** 动量必须包含离散方向和整数强度；除到期 Dodge 在构图前按稳定键原子提交位置这一项声明式例外外，本 Tick 先冻结只读输入并建立冲突图，再计算接触级结果、动作级终态和目标聚合结果，最后统一提交。
11. **动作族统一为普通动作与威胁绑定的高阶反应。** `Guard` 是消耗时间预算的普通防御，持续区间内提供部分伤害抵抗与部分动量抵抗；`Move` 是消耗时间预算、按步推进的普通移动。`Block` / `Dodge` 是消耗肾上腺素的高阶反应，只能响应已经公开、对防御者构成威胁的攻击计划：控制者选择反应动作而不填写触发 Tick，Logic 从来源攻击的 `ImpactTick` 推导唯一触发时机。
12. **攻击时序使用“速度修正前摇 → 命中 → 固定后摇”。** `BaseWindupTicks` 只定义基础前摇，`ActionSpeed` 在普通计划首次进入 `Editable` 时量化一次相对时长；排程编辑只重算绝对 Tick，不重新采样速度。命中发生在前摇结束 Tick，后摇默认 30 Tick 且不受速度缩短，预算成本等于解析后的前摇加后摇。
13. **Reservation 服从统一命令顺序。** 冻结命令批次规范化后依次执行；先成功提交者持有路径预留，后续冲突命令只回滚自身事务，不得用另一套排序抢占已提交计划。
14. **程序集和配置迁移分阶段完成。** 任务 02 只创建 `ProjectHero.Logic` / Tests；读取旧 `Assembly-CSharp` 类型的适配器暂留旧程序集。任务 02B 搬迁完整 Authoring 依赖闭包后再创建 `ProjectHero.Authoring`，并完成全资产稳定 ID、`BattleDefinitionBuilder` 与确定性初始化。任务 10 最后建立 UnityView 边界并消费该结果，不得现场补配置。
15. **命令身份、顺序、作用域和费用由 Logic 闭合。** UI/AI 只能向已注册入口提交 `CommandRequest`；入口绑定 `ControllerId` 与来源并分配生产者序号，Logic 网关规范化后分配全局唯一 `CommandSequence`。普通排程编辑校验 `ExpectedScheduleRevision`，新增/增加预算还校验 `ExpectedWindowId`；反应命令校验 `ReactionOpportunityId`。时间或肾上腺素费用只从权威定义读取，不能由命令载荷声明。
16. **ActionPlan 的所有终态统一闭合。** 自然完成、排程删除、目标失效、来源威胁取消、Clash/Intercept/控制打断、强制位移破坏旧起点或 Reservation、单位死亡和战斗结束都必须通过唯一 `ActionPlanTerminalCoordinator`；它幂等清理计划拥有的未冻结/未来 Intent、MovementSegment、空间/资源 Reservation、Lane 项、机会绑定与活动索引，同时保留历史，且不回卷 ID/Sequence、不撤销已提交结果。`Editable` 普通计划尚未消费的窗口预算预留在终态时释放；计划锁定后已消费的预算不退。来源攻击在高阶反应触发前取消时，肾上腺素仍只按 ReservationCycleId 规则释放。
17. **首版回放只做开发者重演工具。** 回放始终用 Header 中的定义、Encounter 与运行时输入新建模拟，并从 Tick 0 重演；`LogicSnapshot` 只用于规范化观察、哈希与差异定位，不是可反序列化的完整 `SimulationState`。任意 Tick 跳转、中途存档/恢复、周期性完整状态检查点、玩家回放库、分享与跨版本迁移均作为后续独立项目。
18. **场景只有一个运行时推进所有者。** `BattleRuntimeBootstrap` 是唯一通过 Unity `Update()` 选择并推进顶层战斗时钟的组件；迁移期显式区分 `Legacy`、`Shadow`、`New` 三种启动模式。`Shadow` 可以让新旧模拟同时计算，但只有 Legacy 写场景和播放反馈，新模拟只能输出差异报告；`New` 模式不得再调用旧 `BattleTimeline.AdvanceTime()`。
19. **伤害与动量是正交载荷。** 每次接触携带 `DamageComponents[]` 与 `MomentumPacket`；伤害按稳定 `DamageChannelId` 分通道结算，动量继续使用离散方向和整数强度。`Guard` / `Block` 分别配置伤害抵抗与动量抵抗，不能再用一个 `DamageReduction` 或 `ImpactType` 同时表达伤害类型、冲击传递与防御效果。
20. **高阶反应权限对所有单位一致。** 是否能够使用 `Block` / `Dodge` 只由单位的 `ActionSetDefinition`、当前状态与 Lane、有效 `ReactionOpportunity` 和可用肾上腺素决定，不检查 `IsPlayerControlled`。玩家和 AI 监听相同机会、提交相同 `ReactionCommand`，并经过相同校验和时序推导。
21. **肾上腺素获得与上限由规则闭合。** 实际造成/承受伤害按单位和 Tick 各聚合量化一次，Clash 按单位/冲突组、成功 Block/Dodge 按反应计划各奖励一次；Available 受个人周期上限约束，Block/Dodge 成功奖励都必须低于对应动作费用。
22. **玩家与 AI 使用相同反应输入提前量。** Tick N 新公开的机会只能产生 Tick N+1 请求；Step 开始后批次冻结，AI 不能同 Tick 抢跑。到期 Dodge 在冲突图构建前按稳定键提交新位置，构图同时读取冻结的旧格/新格记录。
23. **普通时间线采用滚动排程与执行前锁定。** 不建立要求玩家一次排完整个窗口的独立“草稿阶段”，也不复制第二种 ScheduledAction。普通 `ActionPlan` 从创建起拥有稳定 ID，并按 `Editable -> Locked -> Running -> Completed/Terminated` 演进；本地拖拽只是 UI 草稿。权威排程只可由成功的原子 `ScheduleEditCommand`，或计划到期时由执行前启动门禁触发的确定性系统自动延期事务改变。完成 Tick N 后，UI 锁定线显示在 N+1；Step N+1 先处理已冻结的排程编辑，再评估 StartTick=N+1 的计划。只有通过门禁的计划才原子完成锁定、预算消费与启动；可恢复阻塞只把受影响 `Editable` 普通计划向右重排，反应计划和 Locked/Running/终态计划保持只读。
24. **计划启动失败在执行前门禁闭合。** 到期普通计划若因有明确结束 Tick 的 `Staggered` / `KnockedDown` / `Recovering` 或限时空间阻塞暂时不能启动，使用同一 `ScheduleEvaluator` 原子延期并向右 ripple，预算保持 Reserved；无有限重试 Tick、超过自动延期次数/排程视野、目标或路径永久失效时，在 `Editable` 阶段经统一终态协调器结束并释放未消费预留。`Locked`、预算 `Reserved -> Spent` 与 `Running` 是一个不可分割的启动提交，不允许留下 Locked 但未启动的普通计划；固定反应计划永不自动延期。
25. **阵营关系由独立、闭合的模型回答。** `FactionModelDefinition` 对每一对不同阵营显式配置静态、对称的 `Allied / Neutral / Hostile` 关系；同阵营单位天然为 Allied，同一单位单独归类为 Self。攻击定义用 `TargetRelationMask` 声明允许影响 Self/Allied/Neutral/Hostile 中的哪些关系；命令校验、AOE 候选、反应机会、AI 选目标都调用同一个关系解析器，不得从 Controller、玩家标志、标签或名称猜测敌我。
26. **阵营归属、控制权与胜负目标彼此分离。** `EncounterUnitSlot` 显式携带稳定 `FactionId`，运行时单位从槽位继承且首版战斗中不可变；`ControllerBinding` 只回答谁能控制单位。消灭型胜负只读取 `VictoryDefinition` 中互斥的 Allied/Hostile 目标阵营集合，并显式处理双方同 Tick 全灭的 Draw；未列入目标集合的阵营仍按关系矩阵参与目标判定，但不阻止该消灭目标完成。`IsPlayerControlled` 不再是阵营、胜负或玩法权限依据。
27. **寻路沿用项目已有的 1/2 整数权重。** 三角格不引入方格语义的“直行/斜向成本”：偶数 `GridDirection` 的边权为 1，奇数方向的边权为 2；`MoveSpeed` 不参与选路，只在计划首次进入 `Editable` 时量化一个权重单位的 Tick。路径权重同时驱动 A*、移动段时长和预算。首版搜索上限固定为 `MaxExpandedNodes = 4096`、`MaxPathWeightUnits = 256`、`MaxPathEdges = 192`，等于上限仍合法、只有大于上限才失败；搜索空间只由 Encounter 的显式 `GridBoundary` 限定，失败码、检查位置、优先级及溢出规则全部版本化。
28. **强制位移使用独立的同时求解协议。** 同 Tick 全部击退请求先在不可变网格快照和临时占位空间中逐格同时求解，再通过 `ApplyBatchRelocation` 一次性提交最终位置；它不复用普通 Move、Pathfinder、MovementSegment 或普通 Reservation 竞争逻辑。边界/障碍在最后合法格停止，静止单位硬阻挡，同落点争抢、交换和循环全部失败，只允许最终通向空格的独立位移依赖链；不连锁推人。强制位移优先于 Reservation，先终止受影响空间计划并释放未来预留，再批量换位、发射事件，最后处理状态/控制与死亡。

29. **Dodge 实际换位后取消旧起点移动链。** 接受时保留未来 Move；TriggerTick 实际换位成功才在同一原子事务中，经统一终态协调器取消依赖旧起点的后续 Editable Move 闭包、释放原窗口未消费预算与路径预留。失败或取消且未换位时保留移动，不自动改路、不重排其他计划；确认前显示 Logic 条件预检，触发时重新求值。完整契约见 3.1.2。

---

## Agent 执行拆分

本文件继续作为唯一的架构与玩法事实来源。具体实施已拆成一份执行总则和 13 个可独立验收的任务包；agent 执行时不得用任务包反向改写本文件的核心设计。

- [00 - 执行规则与依赖](优化任务/00-执行规则与依赖.md)：文档优先级、全局不变量、依赖顺序、完成定义与交接模板
- [01 - 基线、清单与不变量测试](优化任务/01-基线与不变量.md)
- [02 - 程序集边界与纯数据定义](优化任务/02-程序集边界与纯数据.md)
- [02B - 完整配置、ID 迁移与战斗初始化](优化任务/02B-完整配置与战斗初始化.md)
- [03 - 确定性内核、事件与规范化快照](优化任务/03-确定性内核.md)
- [03B - Unity 运行时所有权壳与 Shadow 验证](优化任务/03B-运行时所有权与影子验证.md)
- [04 - 单位状态、持续效果、死亡与胜负](优化任务/04-单位状态与生命周期.md)
- [05 - 全局 ActionPlan 与 ActorLane](优化任务/05-ActionPlan与ActorLane.md)
- [06 - LogicGrid、移动段与 Reservation](优化任务/06-LogicGrid与移动段.md)
- [07 - TurnWindow、预算与提交授权](优化任务/07-TurnWindow与提交权限.md)
- [08 - 多方仲裁、伤害与动作交互](优化任务/08-多方仲裁与伤害.md)
- [09 - 命令入口、AI 与输入模式](优化任务/09-命令AI与输入.md)
- [10 - New 模式切换、UnityView 与场景迁移](优化任务/10-Unity接入与表现桥接.md)
- [11 - 开发者回放、性能、清理与最终验收](优化任务/11-回放性能与收尾.md)

默认执行顺序和允许的并行点以 `00-执行规则与依赖.md` 为准；本文件第四部分的阶段路线用于说明架构迁移节奏，不替代单任务的前置门槛与验收要求。

---

## 〇、游戏设计基准（优先于代码改造）

在写任何代码之前，先确立以下设计决策。这些决策直接影响架构形态。

### 0.1 战斗资源模型

**只保留两种战斗资源：时间 与 肾上腺素。**

| 资源 | 获得方式 | 消耗方式 | 生命周期 |
|------|----------|----------|----------|
| **时间（Turn Budget）** | 回合窗口打开时获得固定 Tick 数 | Attack / Guard / Move 在 `Editable` 时预留、启动门禁通过时与 Locked/Running 原子消费 | 账本归属 `TurnWindow`；窗口关闭后停止新增消费，但保留未锁定计划的预留审计直至释放或消费 |
| **肾上腺素（Adrenaline）** | 造成实际伤害、受到实际伤害、成功高阶反应、拼刀 | Block / Dodge 被接受时预留，触发时消费 | Available 不随时间衰减并跨其他单位窗口保留；在该单位下一次自身窗口**打开**时清零，已接受反应的预留继续履约 |

**移除：** 体力（Stamina）、专注点（Focus）、护甲重量（ArmorWeight）等传统 RPG 资源。

**统一动作族：**

| 动作 | 定位 | 费用 | 时序与效果 |
|---|---|---|---|
| `Guard` | 普通防御 | 当前窗口时间预算 | `Windup → Active 区间 → Recovery`；Active 区间内对每个合格接触施加部分 `DamageResistanceQ10` 与部分 `MomentumResistanceQ10` |
| `Block` | 高阶防御反应 | 单位肾上腺素 | 只能绑定已公开的 `ReactionOpportunity`；在来源攻击的 `ImpactTick` 对所有同 Tick、可格挡的入射接触提供完全伤害抵抗与完全动量抵抗 |
| `Move` | 普通移动 | 当前窗口时间预算 | 按路径边权与确定性的每权重单位 Tick 生成 `MovementSegment`，逐步提交逻辑位置 |
| `Dodge` | 高阶移动反应 | 单位肾上腺素 | 只能绑定已公开的 `ReactionOpportunity`；在来源攻击的 `ImpactTick` 原子提交到已校验落点，然后让攻击针对新位置重新判定；不提供通用无敌区间 |

`Guard` / `Move` 是窗口动作，必须拥有当前窗口提交权限并消费时间预算。`Block` / `Dodge` 是威胁绑定反应，不要求防御者拥有当前窗口，也不要求主角并发行动已经激活；肾上腺素就是它们越过窗口权限的玩法成本。反应计划仍进入同一条 `ActorLane` 和全局 `ActionPlan` 时间轴，遵守状态、占位、恢复和终态清理，不创建第二套调度器。

**反应不能被预先放置：**

- 对防御者构成威胁的 `AttackPlan` 在执行 Tick 锁定、启动并公开后，`ReactionOpportunitySystem` 才能为合法防御者创建机会；仍为 Editable 或不存在来源攻击时，不能提交 `Block` / `Dodge`。
- `ReactionCommand` 只携带 `ReactionOpportunityId`、防御者、所选高阶动作；Dodge 额外携带目的格。控制者不得填写 `BlockTick`、`EvadeTick` 或任意触发 Tick。
- Logic 固定推导 `TriggerTick = SourceAttack.ImpactTick`、`StartTick = TriggerTick - ReactionWindupTicks`、`EndTick = TriggerTick + RecoveryTicks`。处理命令时若已经晚于 `StartTick`，稳定拒绝为 `REACTION_TOO_LATE`。
- 可反应攻击必须显式带有 `Reactable`，且满足 `ImpactTick - TelegraphTick >= MinimumReactionLeadTicks`；无法提供至少一个输入往返的攻击必须标为 `Unreactable`，不能生成一个事实上无法响应的机会。
- 首版每个 `(SourceActionPlanId, DefenderUnitId)` 只生成一个机会。来源攻击在触发前终止时关闭机会，并使已接受反应以 `SourceThreatCancelled` 结束。

**设计理由：**
- 时间仍表达可主动规划的普通行动；肾上腺素专门表达在他人行动中作出高阶反应的稀缺能力。
- Guard/Block 与 Move/Dodge 分别形成“普通动作—高阶反应”的清晰升级关系，同时避免 Block/Dodge 退化为可提前铺设的免伤区间。
- Block 解决正面承受接触，Dodge 通过空间变化避免接触；二者的结果不同，AI 也能按威胁、位置与资源做真实选择。
- 反应绑定已存在攻击并由 Logic 推导 Tick，避免人类输入、AI 和回放分别实现一套猜测敌方命中时机的逻辑。

**逻辑表示约束：** 设计表中可以继续用秒填写，例如 `0.6s`；加载战斗配置时统一转换为 `36 Tick`。进入战斗后不得反复执行 `float seconds → Tick` 转换，也不得用浮点数累减时间预算。

**肾上腺素结算约束：** 肾上腺素必须跨对手窗口保留，否则在对手攻击命中前清零会破坏反应设计。每单位使用 `AdrenalineLedger(Available, CycleId, Reservations)`：自己的窗口打开时先递增 CycleId 并把 Available 清零，再授予新窗口预算；窗口关闭时不清零。反应接受时把费用从 Available 转为带 `ReservationCycleId` 的计划预留，TriggerTick 删除预留并视为消费。已接受反应即使跨过自身窗口打开边界仍可触发；来源威胁在触发前取消时，只有预留仍属于当前 Cycle 才返还 Available，旧 Cycle 预留只删除而不注入新周期。防御者主动取消、被控制打断、死亡或错误选位不退款；触发后无论结果如何均不退款。

获得量由版本化 `AdrenalineRules` 配置，但计算方式固定：每 Tick 全部 Resolution 提交后，按规范接触顺序分别汇总单位实际造成/承受的最终伤害，再各量化一次；每个单位在同一 ConflictGroup 成功 Clash 最多获得一次固定奖励，每个 Block/Dodge 计划在 TriggerTick 成功最多获得一次固定奖励。Block 只有实际降低了至少一个合格伤害分量或动量才算成功；Dodge 只有至少一个 `Dodgeable` 接触因新位置失效才算成功。所有类别先求和，再按 UnitId 一次性提交：

```text
DealtGain    = RoundHalfUp(ActualDamageDealt    * DamageDealtGainQ10    / 1024)
ReceivedGain = RoundHalfUp(ActualDamageReceived * DamageReceivedGainQ10 / 1024)
TotalGain    = DealtGain + ReceivedGain
             + SuccessfulBlockReward + SuccessfulDodgeReward + ClashReward
Available'   = min(MaxAvailablePerCycle, checked(Available + TotalGain))
```

各 Q10 比率和奖励必须非负，`MaxAvailablePerCycle > 0`，每个 Block/Dodge 成本不得超过上限；`SuccessfulBlockReward` 必须小于每个 Block 成本，`SuccessfulDodgeReward` 必须小于每个 Dodge 成本，防止一次高阶反应自行回本。Reservation 已从 Available 移出，不参与上限裁剪，也不会因同 Tick 新奖励被重复消费。

### 0.2 回合制表面 + 实时制底层

```
表面（玩家看到的世界）：
  ┌─────────────────────────────────────────────────────┐
  │  单位A的回合 → 单位B的回合 → 单位C的回合 → ...      │
  │  每回合获得时间预算，动作消耗时间                     │
  │  时间预算耗尽 → 关闭提交权限 → 下一个回合窗口          │
  └─────────────────────────────────────────────────────┘

底层（逻辑帧的真实世界）：
  ┌─────────────────────────────────────────────────────┐
  │  Tick 0 ── Tick 1 ── Tick 2 ── ... ── Tick N       │
  │  所有单位的时间线事件共存于同一条 BattleTimeline      │
  │  主角激活并发行动后，可与当前回合单位同时排入动作      │
  └─────────────────────────────────────────────────────┘
```

**为什么需要实时底层？**

核心原因是**主角的并发行动能力**：主角使用局外资源激活能力后，**获得在当前活跃单位（通常为敌方）的回合内行动的权利**。这意味着两个单位的动作时间窗口可能重叠在同一时间线上，需要一个能处理并发动作和动作间交互的底层系统。

**并发行动 ≠ 中断/偷回合：**
- 不是"打断敌人的回合，换成主角的回合"
- 不是"偷取敌方单位的时间预算"
- 而是"主角获得在当前回合内的**并发行动权**"——回合仍属于原单位，但主角现在也可以在里面排入动作
- 两个单位的动作共享同一个时间预算，并发执行

```
具体示例：
  单位A的回合（时间预算 3.0s）
    ├── 单位A排入 轻攻击 (0.6s)         → 剩余 2.4s
    ├── 单位A排入 重攻击 (1.2s)         → 剩余 1.2s
    ├── [★ 主角激活并发行动能力]         → 花费局外资源，获得并发行动权
    │     主角排入 Guard (0.4s)          → 剩余 0.8s
    │     主角排入 反击 (0.4s)           → 剩余 0.4s
    ├── 单位A排入 移动 (0.4s)            → 剩余 0.0s
    ├── [A 的重攻击公开反应机会]
    │     主角可另以肾上腺素响应 Block / Dodge，不再消费窗口预算
    └── 时间预算耗尽 → A 的窗口关闭（肾上腺素保留到 A 下次窗口打开）
  单位B的回合开始...

  已提交的轻攻击、重攻击、Guard、反击、移动和反应计划仍留在连续时间轴上，
  不因 A 的窗口关闭或 B 的窗口打开而取消、截断或提前结算。
```

**关键设计要点：**
1. 并发行动权需要消耗局外资源激活
2. 激活后，主角可以在当前回合剩余时间内排入动作，直至时间预算耗尽
3. 主角和回合拥有者的动作可以在时间线上重叠，`CombatArbiter` 负责在重叠帧判定交互
4. **普通动作服从窗口提交权限，高阶反应服从机会权限**——Attack/Guard/Move 需要窗口或并发授权；Block/Dodge 只需有效 ReactionOpportunity、可控单位、Lane/状态合法与足够肾上腺素
5. 回合窗口关闭后立即停止接受新动作，并发行动权同时结束；已接受的 ActionPlan 独立留在时间轴上继续执行

#### 0.2.1 连续时间轴的五条核心不变量

1. **所有 ActionPlan 默认跨回合窗口存在。** 窗口关闭不会自行取消、截断、结算或移动任何计划；只有显式排程编辑事务，或到期计划通过执行前门禁触发的确定性系统自动延期事务，可以改变仍为 `Editable` 的普通计划。
2. **回合只控制提交新动作的权限与预算。** 它不是动作所有者，也不是动作执行或结算边界。
3. **ActionPlan 生命周期独立于回合。** 普通计划可在锁定前经可回放命令编辑或删除；锁定后只会因自身完成、战斗交互、单位死亡或战斗结束而进入终态。
4. **ActorLane 决定单位是否还能接受或编辑动作。** 同一单位的动作如何排队、最早开始 Tick、锁定线、不可变区间和队列上限都由 Lane 与权威排程器共同决定。
5. **任何单位都可以与未来执行中的动作交互。** 仲裁只看动作在连续时间轴上的有效窗口和逻辑空间关系，不检查动作由哪个回合窗口提交。

### 0.3 回合结构

```
一次正常的敌方回合（主角未激活并发行动）：
  ┌──────────────────────────────────────────┐
  │ 1. 回合开始 — 获得 TimeBudget（如 3.0s）  │
  │ 2. AI 排布动作 — 消耗时间预算              │
  │ 3. 时间线执行 — 动作按序触发               │
  │ 4. 预算耗尽/AI 主动结束 → 当前窗口关闭        │
  │ 5. 关闭窗口 → 下一窗口；肾上腺素不在关闭时清零  │
  │    已提交 ActionPlan 留在连续时间轴继续执行    │
  └──────────────────────────────────────────┘

主角激活并发行动后的回合：
  ┌──────────────────────────────────────────┐
  │ 1. 回合开始 — AI 获得 TimeBudget          │
  │ 2. AI 排布动作                             │
  │ 3. [★ 主角激活并发行动]                    │
  │ 4. 主角与 AI 交替/同时排入动作              │
  │    主角可排入：攻击、Guard、Move             │
  │    任意单位可响应机会：Block、Dodge           │
  │ 5. 预算耗尽 → 当前窗口关闭                    │
  │ 6. 并发提交权结束 → 失去普通动作提交权；仍可响应机会 │
  │ 7. 肾上腺素保留到各单位下一次自身窗口打开       │
  │    双方已提交 ActionPlan 继续在时间轴执行      │
  └──────────────────────────────────────────┘
```

**规划与执行可以交错（"实时规划"）：** 玩家在时间预算内随时添加普通动作，而非一次性排完。这样设计是为了：
- 允许玩家根据前一动作的结果调整后续动作
- 让“看到敌人挥刀后再反应”成为一等流程——攻击公开后生成 `ReactionOpportunity`，玩家或 AI 在截止 Tick 前提交 Block/Dodge，Logic 绑定到该攻击的 `ImpactTick`；并发行动只影响普通动作，不再是高阶反应的前置条件

这里不再定义“草稿完一个行动窗口后一次执行”的阶段边界。玩家看到的循环始终是：在当前只读快照上本地预览若干编辑 → 原子提交一次排程事务 → 下一 Tick 先采纳该事务 → 锁定并执行到期动作 → 继续在新快照上规划。玩家可以随时打开或关闭行动面板；面板生命周期不创建、不提交也不结束一个逻辑草稿。这样即使 `TurnWindow` 边界不明显，规划语义仍由 Tick、排程修订号和 ActionPlan 状态闭合。

### 0.4 交互判定优先级矩阵

当多个动作在同一逻辑帧发生空间或目标关联时，先建立冲突图，再按以下阶段求解：

```
求解阶段（从高到低）：
  1. Attack vs Dodge        → 在 TriggerTick 原子换格并从新位置重新判定接触
  2. Attack vs Block        → 同 TriggerTick 的可格挡接触伤害与动量归零
  3. Attack vs Attack       → 有向拼刀：同组全部 Clash 接触同时计算动量损耗
  4. Attack vs Move (end)   → 拦截：该移动目标被击中并终止移动
  5. Attack vs Move (start) → 逃脱：该移动目标对应的命中失效
  6. Remaining Hits         → 应用 Guard/被动分通道抵抗，再聚合伤害、总冲击和有向合力
```

**设计原则：** 先提交空间变化，再处理完全抵抗，最后处理普通移动与部分抵抗。Dodge、Block、Guard 是三种不同结果，禁止全部实现成 `CanReceiveDirectHit == false`。

**目标资格分层：** `InteractionCandidateBuilder` 根据本 Tick 的有效 Intent、逻辑空间、`FactionRelationResolver + AttackPayloadSpec.AllowedTargetRelations` 和反应绑定收集候选，不使用状态布尔值、Controller 类型或玩家标志提前丢弃接触。Dodge 使用下述旧格/新格双位置契约：冻结位移前接触，再提交位置并查询新格；只有旧格成立、新格失效的 Dodgeable 接触记录为 `Dodged`，Undodgeable 保留旧格接触，新格新增接触正常参与求解。Block 只处理与自身 `TriggerTick` 相同且带 `Blockable` 的接触。Guard 不让接触失效，而在 Remaining Hit 中缩放伤害分量和动量包。只有控制态等明确规则禁止承受直接 Hit 时，才在最后阶段使用 `CanReceiveDirectHit`。

**防御范围与作用域：** `Guard` 的部分抵抗在其 `[ActiveStartTick, ActiveEndTick)` 内持续生效。`Block` / `Dodge` 没有持续判定区间，只有一个由来源攻击决定的 `TriggerTick`；Windup/Recovery 只占用 Lane 和驱动状态表现，不提供额外免伤。一次 Block 处理该防御者在 TriggerTick 收到的全部可格挡接触，以避免同 Tick 多攻击因排序不同而穿透；它不终止攻击计划，也不影响同一 AOE 的其他目标。Dodge 只因位置变化而使接触失效，不能用“闪避中”状态免除仍覆盖新位置的 AOE、持续区域或不可闪避接触。

#### 0.4.1 威胁绑定反应机会

`ReactionOpportunity` 是已经越过锁定线、启动并公开的权威威胁所生成的、可审计且有截止时间的逻辑对象，不是 UI 的临时提示：

```csharp
public sealed record ReactionOption(
    ActionSpecId ReactionActionSpecId,
    ActionType ReactionType,
    long ResponseDeadlineTick);

public sealed record ReactionOpportunity(
    ReactionOpportunityId ReactionOpportunityId,
    ActionPlanId SourceAttackPlanId,
    UnitId DefenderUnitId,
    long TelegraphTick,
    long TriggerTick,
    IReadOnlyList<ReactionOption> Options,
    ReactionOpportunityState State);
```

- 攻击计划在 Editable 排程中只作为控制者自己的规划信息；到执行 Tick 完成锁定并在 `TelegraphTick`（首版固定等于该计划 `StartTick`）启动后，才向合法防御者公开威胁并生成机会，不能泄露仍可移动或删除的未来计划。PrimaryTargetOnly 为通过 `AllowedTargetRelations` 校验的固定目标生成候选；区域攻击按 TelegraphTick 的可见威胁区域、单位逻辑格和同一关系掩码生成候选。每个机会只保留该单位 ActionSet 中合法且与来源标签相容的选项：来源至少含一个可格挡伤害分量或可格挡动量才提供 Block，来源接触为 Dodgeable 才提供 Dodge；并按 ActionSpecId 排序。没有相容选项时不创建空机会。
- `TriggerTick` 固定等于来源攻击的 `ImpactTick`；每个选项独立计算 `ResponseDeadlineTick = TriggerTick - 该选项 ReactionWindupTicks`。只有截止 Tick 不早于 `TelegraphTick + CommandIngressLeadTicks` 的选项才能公开；同一机会可以先失去较慢选项，直到全部选项过期才整体关闭。
- `ReactionCommand` 必须引用机会，不能只引用攻击者，也不能声明目标 Tick。处理器校验发行者控制权、单位 `ActionSetDefinition`、所选 Option 尚未过期、来源攻击仍有效、动作标签、Lane/状态、目的格和 `AdrenalineLedger.Available`；既有计划预留已经从 Available 移出，不能重复消费。
- 接受命令时原子预留肾上腺素与 Dodge 目的格，创建带 `TriggerBinding`、直接处于 Locked 的统一 ActionPlan。计划的 `SubmittedWindowId` 为空，`BudgetCostTicks = 0`，但仍占用 ActorLane 到恢复结束。
- 来源威胁在 TriggerTick 前进入终态时，机会关闭；已绑定反应以 `SourceThreatCancelled` 终止并释放目的格预留。肾上腺素预留若仍属当前 Cycle 则返还 Available，若已跨周期则只删除；反应因防御者主动取消、被控、死亡或目的格自身失效而终止时遵循不退款规则。
- 一个机会最多接受一个反应。控制者未响应、命令过晚或资源不足时稳定过期；系统不会替单位自动选择 Block/Dodge。
- 玩家、AI 与回放都只消费同一机会流。AI 可以按威胁伤害、动量、可用格、资源和后续 Lane 代价评分，但最终必须提交同一种 `ReactionCommand`，不能直接创建反应计划。

`ReactionOpportunityState` 首版固定为 `Open / Accepted / Triggered / Expired / SourceCancelled / BattleEnded`；只有 `Open` 接受命令。选项的截止 Tick 本身仍有效，命令阶段结束后才移除该选项并发射一次 `ReactionOptionExpiredEvent`；最后一个选项移除时机会转为 `Expired`。接受命令使机会转为 `Accepted` 并停止接收第二个反应，在 TriggerTick 实际触发后转为 `Triggered`（Dodge 须等待位置事务成功，不能在生成反应 Intent 时提前转换或广播）；来源取消和战斗结束分别进入对应状态。`ReactionOpportunityClosedEvent` 只在第一次离开 `Open` 时发射，后续 Accepted → Triggered/SourceCancelled 由 `ReactionTriggeredEvent` 或绑定计划终态事件记录，不能重复发“关闭”事件。

首版反应可用性标签至少包含 `Reactable/Unreactable`、`Blockable/Unblockable`、`Dodgeable/Undodgeable`。`CommandIngressLeadTicks`、`MinimumReactionLeadTicks`、反应 Windup、费用与标签全部进入 `BattleDefinitionHash`。攻击若未满足最小提前量，定义构建器必须要求其标记为 `Unreactable`；运行时不得公开零时长、已经过期或连一次默认输入往返都容不下的选项。

**Dodge 旧格/新格与 Undodgeable 契约：**

1. 在本 Tick 普通移动边界、命令和动作阶段完成，全部有效 Intent 冻结之后、任何到期 Dodge 提交之前，捕获统一的只读逻辑空间快照。以该快照和同一批攻击 Intent 冻结每个 Dodge 防御者的旧格接触集合；不是读取 TelegraphTick 的历史位置，也不是按多个 Dodge 的提交先后分别采样旧世界。接触必须满足攻击区域、固定 PrimaryTarget（若有）和关系掩码。
2. 到期 Dodge 按既有稳定键执行位置事务；全部提交结束后，以最终新格快照查询接触。构图读取冻结的旧接触与新接触并集，按规范 ContactKey 去重；首版单段攻击对同一目标最多结算一次。旧、新查询均只读取逻辑位置、体积和同一权威攻击载荷，不读取 Transform，不重复生成攻击 Intent。
3. 下表只决定空间接触是否保留，随后仍遵守 Clash、Block、Guard、被动抵抗及目标聚合规则。未成功提交 Dodge 时按实际未变位置正常求解，不产生 Dodged。

| 位移前旧格接触 | 位移后新格接触 | Dodgeable | Undodgeable |
|---|---|---|---|
| 成立 | 不成立 | 记为 Dodged | 保留原接触，追踪命中位移后的目标 |
| 成立 | 成立 | 正常接触，不算避开 | 合并为一次正常接触 |
| 不成立 | 成立 | 新增正常接触 | 新增正常接触 |
| 不成立 | 不成立 | 无接触，不算避开 | 无接触，不凭空追踪 |

4. `Undodgeable` 是特殊技能显式配置的标签，与 `Dodgeable` 互斥，矛盾配置由 Builder 拒绝并纳入定义哈希。它不提供该攻击的 Dodge 选项，但单位仍可响应同 Tick 其他攻击进行 Dodge；这时它保留位移前已经成立的接触。它只忽略本次 Dodge 导致的空间失效，不赋予无视格挡、减伤、关系掩码或 Clash 的能力，也不锁定 TelegraphTick 时的目标位置。普通移动若已在本 Tick Dodge 前离开攻击范围，不会因此被追踪。保留接触沿用原攻击的伤害、动量方向与强度，不按追踪距离增伤或重新指向；后续效果作用于已位移的单位。
5. **触发与成功避伤分别广播。** Dodge 到达 TriggerTick 且位置事务成功时，只产生一次 `ReactionTriggeredEvent`，即使没有避开攻击或新格仍受伤也会触发；位置提交失败不发该触发事件。每个进入 TriggerTick 位置提交尝试的 Dodge 计划，在 Resolution 完成后只产生一次 `DodgeResolvedEvent`，记录旧格、目的格、DestinationCommitted、失败原因、实际避开的攻击、最终仍命中的攻击及其中保留旧接触的 Undodgeable 攻击；无接触时集合为空，ConflictGroupKey 可空。触发前已终止的计划只发既有终态事件，不伪造触发或结算事件。机会仅在成功触发时转为 Triggered；失败由计划终态记录，不能伪造成功机会状态。
6. 只有成功提交 Dodge 且至少一个旧格成立的 Dodgeable 接触因新格失效，才生成成功 Dodge 事实、一次肾上腺素奖励和一个反击窗口；只闪进攻击范围、只被 Undodgeable 追踪或单纯换位都不算成功避伤。事件通过 LogicEventOutbox 在本 Tick 全部提交完成后发布，表现层/事件总线只消费这些事实，不从动画、Dodging 状态或位置差自行推断成功。输入排列不得改变旧/新接触、去重结果、事件次数或奖励。

#### 0.4.2 能力边界：从二体标量升级为多体有向规则

旧系统只支持二体、标量、默认相向的简化模型：一个 Intent 遇到第一个对手后立即处理，Attack vs Attack 直接比较两个标量动量。该模型不能表达三方相互攻击、二人夹击一人、AOE 中部分目标 Block/Dodge 或多个相反冲量同时作用于同一目标。

新系统仍是**离散玩法规则**，不是连续刚体物理：

- `AttackIntent` 在产生时从本 Tick 的只读单位快照和 ActionPlan 固定朝向计算一次 `MomentumUnits`，随后仲裁不再读取浮点属性。
- 动量表示为 `DirectionalMomentum(GridDirection Direction, int MomentumUnits)`；`MomentumUnits > 0`，所有分支只使用整数。
- 首版每个攻击只有一个固定动量方向：`Normalize12(ActionPlan.Facing + ActionSpec.MomentumDirectionOffsetSteps)`；普通直线攻击偏移为 0。需要每个接触使用不同方向的横扫动作属于后续显式扩展。
- 方向使用现有 12 向 `GridDirection`。方向向量与夹角系数采用 Q10 定点查表，并进入 `BattleDefinitionHash`；不得运行时调用 `sin/cos`。
- 伤害、总冲击和有向合力是三个不同结果：相反冲量可以让击退合力为零，但伤害和硬直用的总冲击仍然累加。

标准 Q10 方向向量如下，枚举数值必须保持 `East = 0` 到 `EastSouth = 11`：

| 方向索引 | 角度 | Q10 向量 `(x, y)` |
|---:|---:|---:|
| 0 | 0° | `(1024, 0)` |
| 1 | 30° | `(887, 512)` |
| 2 | 60° | `(512, 887)` |
| 3 | 90° | `(0, 1024)` |
| 4 | 120° | `(-512, 887)` |
| 5 | 150° | `(-887, 512)` |
| 6 | 180° | `(-1024, 0)` |
| 7 | 210° | `(-887, -512)` |
| 8 | 240° | `(-512, -887)` |
| 9 | 270° | `(0, -1024)` |
| 10 | 300° | `(512, -887)` |
| 11 | 330° | `(887, -512)` |

两方向的最小索引差记为 `d ∈ [0, 6]`。首版反向损耗系数 `OppositionFactorQ10[d]` 固定为：

```text
d:                       0  1  2  3    4    5     6
OppositionFactorQ10:     0  0  0  0  512  887  1024
```

单条 Clash 接触对攻击 A 造成的损耗为：

```text
LossFromBToA = (B.MomentumUnits * OppositionFactorQ10[d] + 512) / 1024
```

整数除法向下取整；`+512` 表示非负数的 half-up 舍入。方向表、系数表或量化方式变化都属于玩法规则变化，必须更新规则版本和 golden snapshot。

单个 Intent 的 `MomentumUnits` 使用正 `int`；乘法、组内损耗和目标聚合全部使用 checked `long` 中间值。超出 BattleRules 明确上限的配置或冲突组必须稳定拒绝/失败，不能溢出回绕或饱和后继续模拟。

##### 0.4.2.1 首版动量、伤害与控制量化

首版以旧 `PhysicsEngine` 的数值链为兼容基准，但必须在 Authoring 边界把“伤害类型”和“冲击类型”拆开。`TotalMass` 映射为 `Mass`，`Swiftness` 分别映射为动作时序使用的 `ActionSpeed` 与冲击阈值使用的 `MomentumSpeed`；旧 `ArmorDefense / (ArmorDefense + 100)` 只转换为对应物理伤害通道的基础抵抗。进入 Logic 后不得再次读取旧 MonoBehaviour 或运行时重新换算。

每条接触的权威载荷固定为两个正交部分：

```csharp
public readonly record struct DamageComponentSpec(
    DamageChannelId ChannelId,
    float RawAmount,
    DamageTagMask Tags);

public readonly record struct MomentumPacket(
    GridDirection Direction,
    int MomentumUnits,
    ImpactProfileId ImpactProfileId,
    MomentumTagMask Tags);

public sealed record ContactPayload(
    IReadOnlyList<DamageComponentSpec> DamageComponents,
    MomentumPacket Momentum);
```

`DamageChannelId` 使用稳定 ID 而不是封闭 enum；首版内置 `physical.blunt`、`physical.slash`、`physical.pierce`、`elemental.fire`、`elemental.frost`、`elemental.lightning`、`arcane` 与 `true`。单位持有按通道的 `BaseDamageResistanceQ10`，缺失通道视为 0。`DamageTagMask` 至少区分 `Blockable`、`Guardable`、`BypassPassiveResistance` 与 `BypassActionResistance`；带绕过标签的分量分别跳过对应层。`true` 通道默认同时带两个绕过标签且不带 `Blockable/Guardable`，因此严格绕过单位被动抵抗和 Guard/Block；Builder 必须拒绝同一分量同时声明动作绕过与 Blockable/Guardable 的矛盾标签。`ImpactProfileId` 只决定动量生成/传递，不再充当伤害类型。旧 Blunt/Slash/Pierce 资产在迁移时可以同时映射到同名物理伤害通道和兼容冲击 Profile，但两个字段从此独立配置。

以下常量属于 `BattleRules`，必须全部进入 `BattleDefinitionHash`：

```text
MomentumUnitsPerLegacyMomentum = 100
ImpactTransferPercent:
  Blunt  = 100
  Slash  = 60
  Pierce = 30
NormalImpactDamageDivisor = 5000
ClashResidualDamageDivisor = 1000
ResistanceScale = 1024
MaxMomentumUnitsPerIntent = int.MaxValue
MaxConflictGroupNodes = 256
MaxConflictGroupEdges = 32640
MaxTargetsPerConflictGroup = 256
```

`Mass`、`MomentumSpeed` 和 `ForceMultiplier` 必须是有限正数；每个伤害分量必须有限非负；所有抵抗必须在 `[0,1024]`。攻击 Intent 创建时只量化一次：

```csharp
double scaled =
    (double)unit.Stats.Mass
    * unit.Stats.MomentumSpeed
    * action.ForceMultiplier
    * BattleRules.ImpactTransferPercent[action.ImpactProfileId];

long rounded = checked((long)Math.Round(
    scaled,
    MidpointRounding.AwayFromZero));

int momentumUnits = checked((int)Math.Max(1L, rounded));
```

该公式等价于旧公式 `TotalMass * Swiftness * Kw * ForceMultiplier` 的 100 倍整数表示，其中兼容 Blunt/Slash/Pierce 冲击 Profile 的 `Kw` 分别为 `1.0/0.6/0.3`。非有限值、非正值、舍入后超过 `MaxMomentumUnitsPerIntent` 的值必须以稳定原因码 `MOMENTUM_OUT_OF_RANGE` 拒绝；不得饱和、回绕或静默修正。

直接接触先构造原始载荷，再按以下顺序结算：

```text
LegacyBaseComponent =
  DamageComponent(ChannelFromLegacyImpactType, ActionSpec.BaseDamage)
LegacyImpactComponent =
  DamageComponent(physical.blunt, MomentumUnits / 5000)
ClashResidualComponent =
  DamageComponent(physical.blunt, ResidualMomentumUnits / 1000)

AfterPassiveResistance =
  Has(BypassPassiveResistance)
    ? RawAmount
    : RawAmount * (1024 - Target.BaseDamageResistanceQ10[Channel]) / 1024
AfterActionResistance =
  Has(BypassActionResistance)
    ? AfterPassiveResistance
    : AfterPassiveResistance * (1024 - ActiveDefense.DamageResistanceQ10[Channel]) / 1024
AfterMomentumResistanceUnits =
  RoundHalfUp(IncomingMomentumUnits
    * (1024 - ActiveDefense.MomentumResistanceQ10) / 1024)
```

`Guard` 为 `Guardable` 分量提供配置的部分 `DamageResistanceQ10`，并对动量提供独立的部分 `MomentumResistanceQ10`。`Block` 对 `Blockable` 分量和可格挡动量使用 1024，因此结果严格为 0；不合格分量继续正常结算。没有活动防御时动作抵抗为 0。抵抗按“被动 → 动作”依次乘算，不相加，禁止用单个 `DamageReduction` 同时覆盖所有通道和动量。

目标的全部接触先按 `ContactKey → DamageChannelId` 排序，以 `double` 中间值分别求和，最后只转换一次为逻辑血量使用的 `float`；不得按容器发现顺序逐次做 float 累加。伤害事件必须保留各通道的 Raw、被动抵抗后、动作抵抗后数值以及抵抗来源，便于验证 Guard/Block 是否只修改了预期维度。

控制阈值按旧 `impactVelocity = momentum / targetMass` 规则等价量化：

```text
ControlResistanceUnits =
  max(1, RoundHalfUp(Target.Mass * Target.MomentumSpeed * 100))
StaggerThresholdUnits = ControlResistanceUnits
KnockdownThresholdUnits = ceil(ControlResistanceUnits * 3 / 2)
KnockbackSteps =
  HasResultantDirection
    ? floor(ResultantMomentumUnits / ControlResistanceUnits)
    : 0
```

硬直和击倒读取 `TotalImpactUnits`；击退步数读取 `ResultantMomentumUnits`。因此相反冲量抵消时仍可触发硬直或击倒，但无合力方向时 `KnockbackSteps` 必须为 0。冲突组任一项超过节点、边或目标上限时，整组以稳定原因码 `CONFLICT_GROUP_LIMIT_EXCEEDED` 失败；不得丢边、拆组或截断目标后继续求解。

#### 0.4.3 冲突图与冲突组

本 Tick 每个有效 Intent 是一个节点。满足以下任一条件时创建带类型的接触边：

1. 两个 Intent 根据交互矩阵和逻辑空间产生直接交互，例如 Attack 与 Attack 的有效区域相交。
2. Attack 与 TriggerTick 相同的 Dodge/Block、Active 区间内的 Guard 或 Move 的拥有者形成合法攻击目标关系；所有 Attack→Unit 候选还必须通过 `FactionRelationResolver + AllowedTargetRelations`，防御绑定不能反向绕过该资格。
3. 两个或更多 Attack 在同 Tick 指向同一目标；即使攻击者彼此没有 Clash，它们也必须进入包含该目标的同一求解组。

冲突图的连通分量是 `ConflictGroup`。组键为组内最小 `IntentSequence`；节点、接触边和目标集合在求解前全部规范化排序。构图只读取 Intent、LogicGrid、MovementSegment 和单位只读快照，不修改动作、状态、血量、位置或事件队列。

#### 0.4.4 接触级、动作级与目标级结果

仲裁必须显式区分三种结果，禁止用一个 `IsCancelled` 同时表达全部语义：

- `ContactResolution`：一条攻防或攻击目标关系的结果，如 Hit、Dodged、Blocked、PartiallyBlocked、BlockIneffective、Clashed、Escaped、Intercepted。
- `ActionTerminalResolution`：整个 ActionPlan 是否因 Clash、移动拦截、击倒等原因进入终态。
- `TargetAggregateResolution`：某单位在本组最终承受的总伤害、总冲击、有向合力、硬直/击倒候选。

首版取消范围固定如下：

| 场景 | 取消范围 | 多方语义 |
|---|---|---|
| Dodge | 目标级 + 空间提交 | 冻结旧格接触后原子提交目的格；旧格成立、新格失效的 Dodgeable 接触记为 Dodged，Undodgeable 保留旧接触，新格新增接触正常求解，攻击计划及其他 AOE 目标继续生效 |
| Block | 目标级载荷抵抗 | TriggerTick 内全部入射接触的合格伤害分量与动量归零；实际降低过合格载荷且最终载荷全为 0 时记为 Blocked，实际降低过合格载荷但仍有绕过分量时记为 PartiallyBlocked；完全没有可降低载荷时记为 BlockIneffective/Hit 且不算成功反应；攻击计划及其他 AOE 目标继续生效 |
| Guard | 不取消接触 | Active 区间内按配置分别缩放合格伤害分量与动量，接触仍是 Hit 并参与聚合 |
| Clash | 动作级 | 只要参与至少一条有效 Clash，相关攻击动作都在组末终止；剩余动量只生成 ClashResidualImpact，不让原攻击继续命中其他目标 |
| Escape | 目标级 | 只使该移动目标对应的命中失效 |
| Intercept | 目标级 + 移动动作级 | 该目标被命中，其移动计划终止；攻击是否继续由其他接触结果决定 |

一次 Dodge 在 TriggerTick 因同一次换格避开多个攻击时只授予**一个**反击窗口。默认反击对象按 `入射 MomentumUnits 降序 → AttackerUnitId → ActionPlanId` 选择；事件同时记录全部被避开的攻击计划 ID。一次 Block 同时处理多个攻击时，每条接触分别记录 `Blocked`、`PartiallyBlocked` 或 `BlockIneffective` 及被抵抗/绕过的载荷；同一防御者、同一冲突组可以聚合为一个 Block 事件并列出全部攻击计划 ID，但聚合不得改变各接触结果。只有前两种且实际抵抗量大于 0 才触发一次成功 Block 奖励。高阶反应在 TriggerTick 结算一次后进入 Recovery，不存在“第一次接触消费动作”与“持续区间多次判定”的歧义。

AOE/多目标策略必须由 ActionSpec 显式声明：`AllTargetsInArea` 可以对区域内通过 `AllowedTargetRelations` 的多个单位产生接触；`PrimaryTargetOnly` 必须由 ActionPlan 固定一个通过同一关系掩码的 `PrimaryTargetUnitId`，不得在仲裁时依赖容器顺序临时挑选或自动替换目标。

#### 0.4.5 同时求解规则

求解器按阶段工作，但同一阶段的全部输入都来自阶段开始时的不可变中间结果：

1. 全 Tick Dodge 已在构图前统一冻结旧接触并按稳定键完成位置提交，组内不得再次换位。按旧/新格契约处理 Dodgeable 的空间失效、Undodgeable 的旧接触保留和新格新增接触，再计算 Block；Block 只把可格挡接触的合格载荷归零，两者都不终止来源攻击计划。
2. 对仍有效的 Attack 收集全部 Clash 接触。某个目标的 Block/Dodge 不妨碍同一攻击计划与其他攻击发生 Clash，也不妨碍它继续处理其他目标接触。
3. 对每个攻击 A 同时计算。只有方向损耗系数大于 0 的 Attack-Attack 接触才是有效 Clash。首版规定同一个攻击以完整原始 MomentumUnits 参与每一条合法 Clash 接触；这是明确的多目标玩法规则，不声称守恒真实物理能量：

   ```text
   TotalOppositionLoss(A) = Σ LossFromOtherToA
   RemainingMomentum(A) = max(0, A.MomentumUnits - TotalOppositionLoss(A))
   ```

   不能在处理某一条 Clash 边后修改 A，再让下一条边读取修改后的值。
4. 所有参与有效 Clash 的攻击动作都产生动作级终止，保持旧两两拼刀“双方动作结束”的玩法。它们尚未消解的直接 Hit 接触全部失效；`RemainingMomentum > 0` 只转换为 `ClashResidualImpact`。
5. 攻击 A 的剩余动量在其直接 Clash 对手间按 `Weight(A→B) = LossFromAToB` 比例分配：先计算 `floor(Remaining * Weight / SumWeight)`，未分完的整数余数按 `OpponentUnitId → OpponentActionPlanId` 每轮加 1。分配总和必须严格等于 RemainingMomentum，每份冲击方向沿用 A.Direction。
6. 尚未消解的直接 Hit 先按伤害通道应用被动抵抗，再按 Active Guard 应用部分伤害/动量抵抗；ClashResidualImpact 只携带其显式伤害分量与剩余动量，不重复附加攻击的基础伤害。所有系数都属于 BattleRules/ActionSpec 并进入配置哈希。
7. 对每个目标收集所有未失效的直接 Hit 和 ClashResidualImpact：各 `DamageChannelId` 的分量按稳定键求和；`TotalImpactUnits` 累加抵抗后的入射动量绝对值；有向合力按 Q10 方向向量求和。
8. 合力方向选择与合力向量点积最大的 `GridDirection`，并列时取枚举值较小者；最大点积不大于 0 时无击退方向。合力强度为 `maxDot / 1024` 向下取整。
9. 硬直/击倒阈值读取 `TotalImpactUnits`，击退读取有向合力。因此两股等强反向冲量会造成两次伤害并累加硬直，但击退可以为零。

所有 `ContactResolution`、`ActionTerminalResolution` 和 `TargetAggregateResolution` 计算完成后，`ResolutionCommitter` 才能按稳定顺序统一提交。由目标聚合产生的击退必须再进入 0.4.6 的全 Tick 同时强制位移批次；不能按冲突组或目标逐个改写世界。死亡只在批量换位、状态和控制提交后处理，允许同 Tick 同归于尽。

**稳定仲裁键：**

```text
ConflictGroupKey
→ ResolutionPhase
→ TargetUnitId
→ SourceUnitId
→ ActionPlanId
→ ContactKey
```

- `IntentSequence` 是逻辑层单调递增序号，不使用 `Time.frameCount`
- `ContactKey` 由接触类型、双方稳定 UnitId、双方 ActionPlanId 和目标 UnitId 规范化生成，不使用发现顺序
- 稳定键只决定输出与提交顺序，不参与多方动量数值求解
- 对同一组 Intent、接触边或目标集合做任意排列，Resolution、事件和快照必须一致
- 仲裁只产出 Resolution；前一个接触或目标的副作用不得改变后一个接触的查询结果

#### 0.4.6 同时强制位移协议

强制位移是目标聚合之后、状态/控制与死亡之前的一次独立空间协议。它描述“本 Tick 已经确定要被推向哪个方向、请求多少格”，不是普通移动计划，也不是一条临时路径：不得调用 Pathfinder、不得创建或续写 MovementSegment、不得逐段消费 Move 预算，也不得让普通 Reservation 仲裁器决定赢家。表现层只能根据最终位移事件插值；中间模拟格不是可观察的逻辑状态。

同一 Tick 的所有 `TargetAggregateResolution` 先跨冲突组合并并规范化为“每个目标单位至多一个请求”。同一目标若仍出现两个请求属于 `InvariantViolation`，不能靠枚举顺序或 UnitId 选一个。`RequestedSteps <= 0` 或合力没有方向时不创建请求。输入可按 `TargetUnitId` 排序以获得稳定遍历和输出，但排序不得改变成功者。

```csharp
public sealed record ForcedDisplacementRequest(
    UnitId TargetUnitId,
    GridDirection Direction,
    int RequestedSteps,
    long MomentumUnits,
    long ConflictGroupKey);

public enum ForcedDisplacementStopReason
{
    Completed = 0,
    Boundary = 1,
    StaticObstacle = 2,
    OccupiedUnit = 3,
    DestinationContention = 4,
    DependencyCycle = 5,
    VolumeOverlap = 6
}

public sealed record ForcedDisplacementResolution(
    UnitId TargetUnitId,
    GridPoint From,
    GridPoint To,
    int RequestedSteps,
    int AppliedSteps,
    ForcedDisplacementStopReason StopReason,
    IReadOnlyList<ActionPlanId> InvalidatedPlanIds);
```

`MomentumUnits` 只用于审计、阈值和事件，不用于在空间冲突中挑选赢家；首版没有“更大力量抢到格子”的规则。`ConflictGroupKey` 用于回溯来源，但所有组的请求必须合成一个全局批次后再求解，否则两个独立组仍可能写入同一落点。

首版冻结以下空间规则：

| 情况 | 冻结规则 |
|---|---|
| 边界或静态障碍 | 在最后一个合法格停止，不反弹、不侧滑 |
| 单步与朝向 | 每步只应用一次规范 `GridDirection` 邻居增量，不读取 1/2 PathWeight；平移当前朝向的 footprint，不旋转单位或改 Facing |
| 多格单位 | 每一步的完整目标 footprint 都合法才可移动该步 |
| 普通单位占位 | 没有独立强制位移请求的静止单位是硬阻挡 |
| 多人争抢同一锚点 | 所有争抢者本步都失败，不按 UnitId、动量或输入顺序选赢家 |
| 不同锚点但 footprint 重叠 | 所有重叠提案本步都以 `VolumeOverlap` 失败 |
| 两单位交换位置 | 视为依赖循环，双方停止 |
| 同时腾挪 | 只有占位依赖图的每条分支最终都通向空格时才允许 |
| 连锁推人 | 首版不支持；A 的请求不能隐式生成 B 的请求 |
| 中途遇阻 | 保留此前成功步数，失败单位取消剩余距离 |
| 已完成短距离单位 | 在后续模拟步中成为静止占位，可以阻挡仍在移动的单位 |
| Reservation | 求解时不视为物理障碍；最终位置确定后再抢占冲突预留 |
| 同 Tick 致死 | 先提交强制位移，再处理死亡和占位移除 |

“部分移动”与批次原子性不冲突：每个单位可以只实现请求距离的一部分，但真实 `LogicGrid` 只能看到批次提交前和批次提交后的两个状态，不能看到逐格或逐单位写入。

同时求解器读取阶段开始时的不可变 `LogicGrid` 快照，在自己的临时占位空间逐格推进完整请求距离：

```text
1. 汇总本 Tick 每个单位唯一的 ForcedDisplacementRequest
2. 读取不可变 LogicGrid 快照并建立临时占位空间
3. 对 step = 1..最大 RequestedSteps：
   a. 所有仍活动且尚有剩余距离的单位同时提出下一锚点及完整 footprint
   b. 拒绝越界提案；拒绝与静态障碍相交的提案
   c. 同锚点多方争抢全部记 DestinationContention；不同锚点 footprint 相交全部记 VolumeOverlap
   d. 对其余提案建立“目标 footprint 当前由哪些单位占据”的依赖图；自身旧 footprint 不构成依赖
   e. 允许所有依赖分支最终通向空格、且依赖单位本步提案均成功的无环链或 DAG
   f. 拒绝交换、任意循环，以及依赖静止/已失败单位的提案
   g. 将本步全部成功提案同时写入临时占位空间
   h. 失败单位记录停止原因并退出其余步；已走完单位成为后续步骤的静止占位
4. 从临时空间生成按 TargetUnitId 规范排序的最终 ResolutionBatch
5. 预处理计划失效与 Reservation 抢占，再一次性提交到真实 LogicGrid
```

依赖于静止单位或本步失败单位的提案使用 `OccupiedUnit`；交换及长度大于二的闭环使用 `DependencyCycle`。例如，A、B 都有独立强制位移请求且 `A → B 当前格、B → 空格` 时两者可同时移动；若 B 没有请求或本步失败，A 被阻挡。具有多个占位依赖的多格单位只有在每个依赖分支都成功时才能移动，不能部分写入 footprint。

Reservation 是计划之间的未来空间承诺，不是强制位移求解器的隐形墙：

```text
普通命令 vs 已有 Reservation：已有 Reservation 胜出
强制位移 vs Reservation：强制位移胜出
```

求解阶段忽略全部 Reservation，包括临时中间格上的 Reservation。最终 footprint 确定后，系统找出与最终占位相交的 Reservation，按 `ActionPlanId` 去重并稳定排序，通过 `ActionPlanTerminalCoordinator` 以 `ReservationPreemptedByForcedDisplacement` 终止其所属计划，释放该计划全部活动/未来 MovementSegment 和空间 Reservation；不自动前移、压缩或重排后续计划。该规则也覆盖未来 Dodge 目的格预留：只有目的格 Reservation 实际与本批最终 footprint 冲突时才终止，单纯穿过临时格不终止。被实际位移单位自身依赖旧起点的活动或排队 Move 计划及其 Editable 移动依赖闭包，以 `MovementOriginInvalidated` 终止；Attack、Guard、Block 等不持有固定空间依赖的未来计划可以保留，Dodge 若未被抢占则继续在 TriggerTick 接受既有空间复核。

同一个移动计划同时满足两类失效时，原因优先级固定为：拥有者实际被换位且计划依赖旧起点时使用 `MovementOriginInvalidated`；否则使用 `ReservationPreemptedByForcedDisplacement`。计划 ID 先去重、再按 `ActionPlanId` 提交，统一终态协调器继续遵守“第一次终态请求胜出”。已消费的 Spent 预算或已触发肾上腺素不退款；仍为 Editable 的普通计划仅按既有终态协议释放未消费 Reserved，这属于预留清理，不重开已关闭窗口、不转移额度，也不构成玩法退款。

强制位移计划失效位于“其余 ActionTerminalResolution”之前；因此同一 Move 若还收到本 Tick 的 `InterruptedByIntercept` 等后置终态请求，已经写入的 `MovementOriginInvalidated` / `ReservationPreemptedByForcedDisplacement` 保持，后置请求是幂等空操作。该阶段优先级属于协议的一部分，不能由 Resolution 枚举顺序改变。

固定提交顺序为：

```text
伤害与合力聚合
→ 生成本 Tick 全部 ForcedDisplacementRequest
→ 全局同时空间求解
→ 终止被位移破坏的移动计划并释放其未来 Segment/Reservation
→ ApplyBatchRelocation 一次性提交全部最终位置
→ 发射 ForcedDisplacementResolvedEvent
→ 提交状态/控制及其余终态结果
→ 统一处理死亡与占位移除
```

`ApplyBatchRelocation` 是 `LogicGrid` 提供的原子占位提交原语，而不是另一个求解器。它必须先验证全部成功单位仍位于预期来源锚点、来源 footprint 与基准快照一致、最终 footprint 合法且彼此不重叠、待抢占 Reservation 已按协议清理；验证全部通过后，先统一移除所有成功单位的旧 footprint，再统一写入全部新 footprint。任何验证失败都是 `InvariantViolation`，真实网格保持完全不变；禁止临时退化为逐单位提交、按 UnitId 选赢家或重新调用普通移动逻辑。

方向表、footprint 规则、停止原因、依赖图判定、计划失效原因优先级、Reservation 抢占策略与上述提交阶段都属于版本化玩法规则，必须进入 `RulesVersion` / `BattleDefinitionHash`，并建立事件、快照与重演 golden fixture。

### 0.5 并发行动系统设计

#### 0.5.1 核心概念

主角拥有一种与游戏局外资源（meta-progression currency）挂钩的能力：**并发行动（Concurrent Action）**。

```
非主角窗口 → 主角默认没有普通动作提交权
       ↓
主角支付局外资源 → 激活并发行动 → 获得当前窗口的额外普通动作提交权
       ↓
主角可以排入 Attack / Guard / Move（消费当前窗口时间预算）
       ↓
窗口关闭 → 并发提交权自动结束；已接受普通计划继续执行

任意时刻出现合法 ReactionOpportunity
       ↓
对应单位可独立提交 Block / Dodge（消费自身肾上腺素，不消费窗口预算）
```

**关键约束：**
- 主角只能在自己的回合**之外**激活并发行动（在敌方回合或盟友回合）
- 并发行动激活后，主角提交的 Attack/Guard/Move 消耗当前活跃窗口的时间预算
- 当时间预算耗尽，当前窗口关闭并停止接受新的普通动作；并发提交权同时结束，但已接受动作继续存在
- **Block/Dodge 与并发授权正交**：并发行动既不授予也不撤销高阶反应权限，后者只由 ReactionOpportunity、ActionSet、控制权、状态/Lane、目的格和肾上腺素决定

#### 0.5.2 为什么并发行动与高阶反应必须正交？

| 场景 | Attack/Guard/Move | Block/Dodge |
|------|-------------------|-------------|
| 自身窗口，没有有效机会 | 可按窗口预算提交 | 不可提交；没有可绑定威胁 |
| 自身窗口，存在有效机会 | 可按窗口预算提交 | 可按机会和肾上腺素提交 |
| 他人窗口，未激活并发行动 | 不可提交 | 仍可按有效机会和肾上腺素提交 |
| 他人窗口，已激活并发行动 | 可按共享窗口预算提交 | 仍走独立机会/肾上腺素路径 |
| 已激活并发行动，但没有有效机会 | 可提交普通动作 | 不可借并发授权伪造 Block/Dodge |

并发行动的价值是让主角在他人窗口主动 Attack、预先 Guard 或 Move，而不是购买高阶反应资格。看到攻击起手后，只要 Logic 已公开有效机会，任何受控单位都可根据 ActionSet 和肾上腺素选择 Block、Dodge 或不响应；若还想在同一窗口追加普通反击，才需要该窗口的天然权限或并发授权。两条权限路径可以同时存在，但不能相互替代。

#### 0.5.3 并发行动中的时间预算共享

```
时间预算不属于任何一个单位——它属于当前 `TurnWindow`。
回合拥有者和并发行动者都从中预留；普通 ActionPlan 在 Editable 时占用预算，只有执行前门禁通过时才与 Locked/Running 原子转为已消费，系统自动延期期间仍为 Reserved。

每个单位拥有独立 `ActorLane`：同一单位的普通动作默认串行，但 Editable 区允许显式未来放置、重定位、删除和向右避让；不同单位的动作可以重叠。威胁绑定反应不会顺延到 Lane 尾部；它只能按来源攻击推导出的固定区间无冲突插入，否则稳定拒绝。这样主角能够与敌人并发，同时 Block/Dodge 也不会被普通队列偷偷改到另一个触发 Tick。

时间预算 = 3.0s（单位A的回合开始获得）
  ├── 单位A：轻攻击 (-0.6s)  → 剩余 2.4s
  ├── 单位A：重攻击 (-1.2s)  → 剩余 1.2s
  ├── [主角激活并发行动]
  ├── 主角：Guard (-0.3s)    → 剩余 0.9s
  ├── 主角：轻攻击 (-0.4s)   → 剩余 0.5s
  ├── 单位A：移动 (-0.5s)    → 剩余 0.0s
  └── 当前窗口关闭；已接受动作继续留在时间轴
```

时间线上的并发重叠：

```
Tick:   0      36      60   78       102  108       138
A 轨道: [轻攻击 0-36][重攻击 36----------------108][移动108-138]
主角轨道:               [Guard60-78][反击78-----102]
                                                   → 动作继续执行，与后续窗口动作交互
```

在同一 Tick 内重叠的动作由统一 Resolution 管线进行交互判定。示意图只表达普通计划的重叠关系，实际起止 Tick 由定义和各单位 ActorLane 计算；若该期间公开 ReactionOpportunity，Block/Dodge 仍按独立 TriggerTick 插入和判定。

**连续时间轴规则：** 时间预算耗尽或主动结束只关闭当前 `TurnWindow` 的新增普通动作/预算增加权限，下一回合窗口无需等待任何 ActionPlan。所有已进入权威排程的 ActionPlan 默认跨窗口存在；Editable 普通计划只会被显式排程命令或执行前门禁的确定性系统自动延期修改，窗口切换本身不动它。来自过去窗口的动作可以与当前及未来窗口动作发生 Guard、拼刀、拦截等交互，也可以在公开威胁后被新的 Block/Dodge 反应。

---

## 一、现状分析

### 1.1 已有优秀设计（应保留并强化）

| 设计 | 说明 |
|------|------|
| **BattleTimeline** | 60 Ticks/s 的离散逻辑帧系统，带 `VisualTime` 平滑插值 |
| **CombatIntent 体系** | 所有行动分解为意图，在时间线上调度执行 |
| **TimelinePriority** | 同一 Tick 内按优先级排序执行（System→State→Defense→Attack→Cleanup） |
| **CombatArbiter** | 意图间交互判定（Clash/Parry/Dodge/Intercept/Escape） |
| **CommitMoveStepIntent** | 逻辑位置提交与视觉移动分离的雏形 |
| **Reservation 系统** | 移动中的临时占位，防止重叠移动 |
| **时间线编辑器** | 已有未来 Tick 放置、关键帧吸附、删除、拖动重定位、重叠时向右避让，以及移动链位置/路径/时长重算；这些交互语义应迁移为 Logic 权威排程事务，而不是随旧 UI 删除 |
| **ActionLibrarySO / AttackPattern / UnitVolume** | ScriptableObject 数据驱动的设计 |

### 1.2 需要移除的设计（与新的资源模型冲突）

| 移除项 | 原因 |
|--------|------|
| Stamina 系统（`CurrentStamina`, `MaxStamina`, `StaminaCost`, 体力恢复） | 被时间预算替代 |
| Focus 系统（`CurrentFocus`, `MaxFocus`, `FocusCost`） | 被肾上腺素替代 |
| `ArmorWeight` | 移除其资源/负重职责；若内容仍需要重量，只能迁移为明确的 Mass 或动作属性 |
| `ArmorDefense`, `MagicResistance` | 在 Authoring 边界迁移为按 `DamageChannelId` 配置的 `BaseDamageResistanceQ10`；Logic 不保留通用护甲/魔抗减伤 |
| `IsExhausted` 状态 | 体力移除后不再存在 |
| `Swiftness` 属性（体力影响的部分） | 显式拆为 `ActionSpeed`、`MomentumSpeed` 与 `MoveSpeed`，不再由体力隐式联动 |

### 1.3 核心问题清单

#### 问题 A：逻辑层直接依赖视觉层

```
PhysicsEngine.ResolveCollision()
  ├── GameFeelManager.Instance.ShowDamageNumber()   ← 伤害数字
  ├── GameFeelManager.Instance.ScreenShake()         ← 屏幕震动
  ├── GameFeelManager.Instance.HitStop()             ← 顿帧
  └── target.GetComponent<UnitBounce>().OnImpact()   ← 弹跳动画

CombatArbiter.ApplyInteraction()
  ├── GameFeelManager.Instance.ShowStatusText()      ← 状态文字
  ├── GameFeelManager.Instance.HitStop()             ← 顿帧
  └── timeline.TriggerSlowMotion()                   ← 慢动作

AttackIntent.ExecuteSuccess()
  └── target.GetComponent<UnitMovement>()            ← 读取视觉位置做命中判定
      └── target.transform.position                   ← 直接读 Transform
```

**后果：** 逻辑不可脱离 Unity 测试；回放时表现层副作用干扰逻辑；新增表现效果需要改逻辑代码。

#### 问题 B：业务逻辑散落在 Update() 中

```
CombatUnit.Update()
  ├── 肾上腺素衰减 (Time.deltaTime)    ← 设计已变更：不再衰减
  └── 体力恢复 (Time.deltaTime)        ← 设计已变更：移除体力

BattleManager.Update()
  └── CheckWinCondition() (每帧 FindObjectsByType)

EnemyAIController.Update()
  ├── 目标搜索 (每 0.1s)
  └── MakeDecision()

UIManager.Update()
  └── 暂停状态轮询
```

**后果：** 逻辑执行频率不统一（有的按帧、有的按秒、有的按 Tick）；无法精确复现；`FindObjectsByType` 性能差。

#### 问题 C：战斗单位状态管理分散

`CombatUnit` 有 8 个 boolean 标志位，无状态机：
```csharp
IsActing, InWindup, InRecovery, IsMoving,
IsStaggered, IsKnockedDown, IsRecoveringAction, IsExhausted(computed)
```

状态转换逻辑散落在 `StateChangeIntent`、`PhysicsEngine`、`ActionScheduler` 中。

#### 问题 D：输入系统通过布尔标志通信

```csharp
// TacticsController 设置
InputManager.Instance.IgnoreUnitClicks = true;

// TacticsController 复位
InputManager.Instance.IgnoreUnitClicks = false;
```

极易遗漏复位，导致输入永久失效。

#### 问题 E：缺少统一事件总线

仅有的几个事件全部挂在 `BattleTimeline` 上：
- `OnScheduleChanged`
- `OnTickProcessed`
- `OnDodgeSuccessRequestMove`

无通用事件分发机制，跨系统通信靠直接调用。

#### 问题 F：Singleton 过量使用

`BattleManager`、`InputManager`、`GridManager`、`UIManager`、`GameFeelManager` 全部是 MonoBehaviour Singleton，强耦合，无法替换或 Mock。

---

## 二、目标架构

```
┌────────────────────────────────────────────────────────────────────┐
│ Unity 表现与输入程序集 (ProjectHero.UnityView)                     │
│ InputSampler / CombatUnitView / GameFeel / UI / Camera / Audio     │
│                                                                    │
│ BattleRuntimeBootstrap（唯一由 Update 选择并推进顶层战斗时钟）       │
│   ├── Legacy：只调用旧运行时适配器                                  │
│   ├── Shadow：Legacy 权威 + 新模拟只读比较                           │
│   └── New：只调用 UnityBattleDriver                                 │
│ UnityBattleDriver：累积 unscaledDeltaTime、提交 CommandRequest、     │
│                    消费 EventBatch / Snapshot                       │
└───────────────────────────┬────────────────────────────────────────┘
                            │ CommandBatch ↓   ↑ EventBatch / Snapshot
┌───────────────────────────┴────────────────────────────────────────┐
│ 纯逻辑程序集 (ProjectHero.Logic，禁止 UnityEngine 引用)             │
│                                                                    │
│ BattleSimulation.Step(tick, commands)                              │
│   1. 提交动作前边界；推进状态到期、Buff、死亡/胜负与窗口边界            │
│   2. 冻结此前已投递的玩家/AI 请求并按稳定键规范化                       │
│   3. 原子应用 ScheduleEdit/Reaction；普通计划只预留窗口预算             │
│   4. 对到期普通计划执行启动门禁：自动延期、终止或原子消费/锁定/启动       │
│   5. 取出本 Tick Intent，提交 Dodge 位置并按连通分量统一仲裁             │
│   6. 提交 Resolution，更新血量、状态、逻辑位置并再处理死亡/胜负           │
│   7. 关闭提交窗口、排定下一窗口；AI 读取快照后只投递下一 Tick             │
│   8. 运行不变量检查，输出不可变 EventBatch 与规范化 Snapshot             │
│                                                                    │
│ LogicWorld / LogicGrid / FactionRelationResolver                   │
│ UnitStateMachine / TurnWindow / ActionPlanner / CombatArbiter      │
│ DeterministicRng                                                   │
└───────────────────────────▲────────────────────────────────────────┘
                            │ 纯数据定义
┌───────────────────────────┴────────────────────────────────────────┐
│ Unity 配置适配程序集 (ProjectHero.Authoring)                        │
│ ActionLibrarySO / AttackPattern / UnitVolume                        │
│  → BattleDefinitionBuilder                                        │
│  → ActionSpec / VolumeSpec / FactionModel                          │
└────────────────────────────────────────────────────────────────────┘
```

### 架构原则

1. **逻辑层零 UnityEngine 依赖**，并由独立程序集和自动化检查强制执行，而不是只依赖目录和命名空间约定。
2. **所有会改变战斗结果的代码只在 `BattleSimulation.Step()` 中执行**；`Update()` / `LateUpdate()` 只采样输入、刷新 UI、插值和播放表现。
3. **逻辑层只产出语义事件。** 例如 `DamageResolved`、`BlockResolved`；震屏强度、顿帧时长和慢动作倍率由表现映射器决定。
4. **表现层只消费事件和只读快照，不写回逻辑对象。** 订阅回调不得直接排入或取消 Intent。
5. **输入通过值类型请求批次进入逻辑层。** 生产者提交的 `CommandRequest` 只携带动作选择、目标、目标 Tick 与判别式 `CommandScope`：普通动作引用 `ExpectedWindowId`，高阶反应引用 `ReactionOpportunityId`；它不持有 MonoBehaviour、ScriptableObject、来源优先级、全局序号、规则费用或反应触发 Tick。已注册入口绑定发行者与来源，Logic 网关生成内部 `CommandEnvelope`。
6. **回合只是预算与提交授权窗口。** ActionPlan 一旦被接受就进入全局连续时间轴，其生命周期不再受回合窗口打开或关闭影响；新窗口无需等待旧动作结算。
7. **确定性优先于表现即时性。** 命中、占位、回放只读取逻辑状态；所有可影响结果的遍历必须有稳定顺序。
8. **逻辑时钟不受表现时间缩放影响。** Unity 驱动器使用 `unscaledDeltaTime`；用户暂停由驱动器在 Tick 边界停止调用 `Step()`，并同时禁止生成战斗命令。
9. **确定性承诺限定为同一规则版本与兼容运行环境。** 回放文件记录规则版本、配置哈希和 RNG 状态，不做未声明的跨版本兼容承诺。
10. **顶层运行时推进权必须唯一。** 场景中只有 `BattleRuntimeBootstrap` 拥有战斗时钟/Timeline 的顶层 `Update()` 调度权；Legacy 适配器、Shadow runner 和 `UnityBattleDriver` 都是被调用者，不得各自推进顶层时钟。UI、视觉和迁移期 Legacy 内部旧组件可以暂时保留自身 Unity 回调，但必须归入明确的 Legacy 运行组：只在 Legacy/Shadow 启用，在 New 中全部禁用；任务 04-10 逐步移除其中会写战斗状态的回调。
11. **同时计算不等于同时写入。** Shadow 模式的新模拟使用独立纯逻辑世界，只能消费镜像输入并输出比较报告，不能绑定 `CombatUnitView`、修改旧 `CombatUnit`、播放反馈或决定胜负表现。

### 2.1 程序集边界

| 程序集 | 内容 | 依赖规则 |
|------|------|----------|
| `ProjectHero.Logic` | 模拟循环、状态、网格、仲裁、回放数据 | 禁止引用 UnityEngine；建议启用 `noEngineReferences` |
| `ProjectHero.Authoring` | ScriptableObject 配置与 DTO 转换 | 可引用 UnityEngine，只向 Logic 输出不可变定义 |
| `ProjectHero.UnityView` | MonoBehaviour、输入、UI、动画、特效 | 可引用 Logic 和 Authoring，不得被 Logic 反向引用 |
| `ProjectHero.Logic.Tests` | Unity EditMode 下运行的纯逻辑单元测试、快照与排列不变性测试 | 只引用 Logic，不通过 UnityView 访问逻辑 |

上表是最终结构，不是一次提交内同时创建全部 asmdef。Unity 的自定义 asmdef 不能引用预定义 `Assembly-CSharp`，因此迁移顺序固定为：

1. **任务 02：Logic 边界。** 只创建 `ProjectHero.Logic` 和 `ProjectHero.Logic.Tests`。读取旧 `ActionLibrarySO` / `AttackPattern` / `UnitVolume` 的最小适配器仍编译在 `Assembly-CSharp`，由旧程序集单向引用 Logic。
2. **任务 02B：Authoring 边界。** 先按依赖闭包搬迁旧配置类型及必要依赖并保留 `.meta`，再创建 `ProjectHero.Authoring`。该程序集只引用 UnityEngine 和 Logic，不得反向引用 `Assembly-CSharp`。
3. **任务 03B：运行时所有权壳。** 在旧 `Assembly-CSharp` 兼容区建立唯一 Bootstrap、Legacy/Shadow/New 模式和隐藏验证场景；此时不提前创建 UnityView asmdef，也不迁移完整表现层。
4. **任务 10：UnityView 边界与 New 模式切换。** 在完整配置、初始化接口及 Shadow 证据已经可用后搬迁表现依赖闭包并创建 `ProjectHero.UnityView`。Bootstrap、运行时适配器契约和 New Driver 保留 `.meta` 搬入 UnityView；仍需回切的 Legacy 具体适配器暂留 `Assembly-CSharp`，实现 UnityView 拥有的接口并通过显式 `MonoBehaviour` 槽位校验/注入。UnityView 不得反向引用 Legacy 具体类型。

不得通过复制一份旧配置类、把 Unity 类型包装进 Logic、或建立循环 asmdef 来缩短迁移。任一阶段都必须保持旧场景可编译；正式切换前只能有一套权威战斗写入路径。

**测试环境边界：** “逻辑层纯 C#”指被测生产代码不引用 `UnityEngine`，不等于首版测试运行器必须脱离 Unity。逻辑规则、状态机、Tick、仲裁、快照和回放默认由 Unity EditMode Test Runner 执行，不加载战斗场景；场景、Prefab、MonoBehaviour、输入和表现桥接使用少量 PlayMode 测试。独立 SDK 风格的 `.NET` 测试工程不是本轮重构的完成条件，只有在以后需要无 Unity CI、服务器模拟或独立战斗工具时再评估引入。

#### 2.1.1 运行时推进所有权与迁移模式

迁移期间允许旧模拟与新模拟并存于代码库，但不允许两个顶层 Unity 生命周期入口自行推进战斗时钟。场景只挂载一个 `BattleRuntimeBootstrap`，模式在创建本场战斗前确定，战斗进行中不得热切换；所谓“回切演练”是结束当前实例后以另一模式重新创建战斗，不是把运行中状态跨内核迁移。

“唯一推进所有者”不等于任务 03B 立即删除所有 Unity 回调。现有 `CombatUnit.Update()`、`BattleManager.Update()`、`EnemyAIController.Update()` 等旧逻辑写入先登记为 **Legacy 从属写入者**，由 Bootstrap 在创建战斗时整体启停：Legacy/Shadow 可按基线启用，New 必须全部禁用。输入、UI、相机和视觉插值等无权威写入回调另列为 **表现回调**，可以跨模式保留；任务 04-10 再把 Legacy 从属写入逐项迁入 Step 或只读表现桥。

```csharp
public enum BattleRuntimeMode
{
    Legacy,
    Shadow,
    New
}

public sealed class BattleRuntimeBootstrap : MonoBehaviour
{
    [SerializeField] private BattleRuntimeMode mode;

    private void Update()
    {
        switch (mode)
        {
            case BattleRuntimeMode.Legacy:
                _legacy.AdvanceFrame(Time.deltaTime);
                break;

            case BattleRuntimeMode.Shadow:
                _legacy.AdvanceFrame(Time.deltaTime);          // 唯一场景权威
                _shadow.AdvanceFrame(Time.unscaledDeltaTime);  // 独立世界、暂不写报告
                break;

            case BattleRuntimeMode.New:
                _newDriver.AdvanceFrame(Time.unscaledDeltaTime);
                break;
        }
    }

    private void LateUpdate()
    {
        if (mode == BattleRuntimeMode.Shadow)
            _shadow.CompareAtReadOnlyCheckpoint();
    }
}
```

Shadow 的推进与比较必须分开。`Update()` 只推进两边；同一个 Bootstrap 的只读 `LateUpdate()` 检查点在已登记 Legacy 从属写入者完成后采样并比较，不推进任何时钟。执行顺序必须显式配置并由 PlayMode 测试证明；若某个旧写入者尚未能在该检查点前稳定完成，对应字段只能标记为“暂不可比较”并给出对象路径/迁移任务，不能按同帧索引猜测配对，也不能把它加入永久忽略列表。

Shadow 是开发/CI 诊断模式，不是正式发行默认路径。隐藏验证场景使用记录好的固定帧时间序列，避免双跑开销反过来改变 Legacy 的帧节奏；诊断主场景若超过约定预算，只能把本次比较标记为无效并报告 backlog/耗时，不能丢 Tick、修改传给 Legacy 的 `deltaTime`、切换权威路径或继续产出“通过”结论。

运行模式契约：

- **Legacy**：只有旧 Timeline 与已登记 Legacy 从属写入者修改场景战斗状态；新模拟不创建、不推进。
- **Shadow**：完整 Legacy 运行组仍是唯一场景权威。新模拟从相同 `BattleDefinition`、Encounter、运行时输入和可镜像请求创建独立世界；其事件与快照不连接 View、UI、音效或旧状态，只进入 `ShadowComparisonReport`。
- **New**：只有 `UnityBattleDriver` 调用 `BattleSimulation.Step()`；旧适配器的推进调用数必须为 0，全部 Legacy 从属写入者禁用，旧组件即使暂留也只能作为无写入兼容壳。
- **失败即停**：发现多个 Bootstrap、旧 `CombatDemo.Update()` 仍自行推进 Timeline、Shadow 新模拟写入场景、New 模式调用旧 `AdvanceTime()`、或 New 中仍启用任何已登记 Legacy 从属写入者时，开发与测试环境必须明确失败，不能静默选一个继续。

Shadow 比较不要求把预期玩法变更伪装成完全相等。比较项分为四类：

1. **必须相等的基础设施事实**：初始化输入、请求镜像数量、Tick 边界、稳定 ID 映射，以及明确声明未改变规则的 characterization fixture。
2. **按新规则验证的事实**：资源、Block/Dodge、动作时序、多方仲裁等已明确变更的玩法，用新版 golden snapshot 和不变量判断。
3. **暂不可比较的旧字段**：只允许用于尚在受控 Legacy 回调中、无法在统一检查点稳定采样的精确字段；必须记录对象路径、原因、负责人任务和移除期限，任务 10 切换前清零。
4. **已批准差异**：必须按用例、规则版本、字段和原因记录；禁止使用“忽略全部事件/血量/位置”等宽泛白名单让 Shadow 永久变绿。

任务 03B 先建立 Bootstrap、模式互斥、隐藏验证场景和最小空 Tick/请求镜像报告。任务 04-09 每完成一个子系统，都必须扩展并重跑相应 Shadow/PlayMode 证据。任务 10 负责把主场景默认模式切到 New；任务 11 在完成回切演练、稳定性验证和引用扫描后，才删除 Legacy 推进路径。

### 2.2 稳定 ID 与 Sequence 规则

首版不设计分布式 ID，也不讨论“几进制、固定几位”或把类型编码进数字。ID 只回答“这是哪个对象”，Sequence 只回答“它第几个发生”。配置定义使用稳定字符串，战斗实例使用从 1 开始、单场战斗内单调递增且永不复用的 `long`；日志默认以普通十进制显示，不补零。

#### 2.2.1 ID 分类

| 类别 | 类型 | 用途 | 例子 |
|------|------|------|------|
| 配置定义 ID | 受约束的字符串值类型 | 标识人工制作的配置 | `unit.skeleton_archer`、`action.heavy_slash` |
| 固定出场槽位 ID | `EncounterSlotId` 字符串值类型 | 稳定描述战斗初始单位槽位 | `hero`、`enemy.left` |
| 战斗实例 ID | 包装 `long` 的强类型 | 标识本场战斗中的单位、计划、反应机会、窗口或效果实例 | `UnitId(1)`、`ReactionOpportunityId(7)` |
| 顺序号 | `long` | 固定同类事实的发生顺序 | `CommandSequence`、`EventSequence` |

配置 ID 首版统一使用小写英文字母、数字、点和下划线，不能为空且在各自命名空间内不得重复。显示名称、GameObject 名称、容器注册顺序、`GetInstanceID()`、对象地址和运行时随机 GUID 均不得作为逻辑 ID。

```csharp
// 配置定义：底层是字符串，但不同类别不能互相误传。
public readonly record struct UnitDefinitionId(string Value);
public readonly record struct ActionSpecId(string Value);
public readonly record struct VolumeSpecId(string Value);
public readonly record struct AttackPatternId(string Value);
public readonly record struct MovementPatternId(string Value);
public readonly record struct ActionSetId(string Value);
public readonly record struct DamageChannelId(string Value);
public readonly record struct ImpactProfileId(string Value);
public readonly record struct StatusEffectSpecId(string Value);
public readonly record struct EncounterDefinitionId(string Value);
public readonly record struct EncounterSlotId(string Value);
public readonly record struct FactionId(string Value);
public readonly record struct ControllerId(string Value);

// 战斗实例：底层统一是 long，0 保留为 Invalid/None，首个有效值为 1。
public readonly record struct UnitId(long Value);
public readonly record struct ActionPlanId(long Value);
public readonly record struct ReactionOpportunityId(long Value);
public readonly record struct WindowId(long Value);
public readonly record struct EffectId(long Value);
```

这些包装类型只负责阻止把 `UnitId` 误传成 `WindowId`。字符串格式、空值和重复值由 `BattleDefinitionBuilder` 集中验证；运行时 ID 的正数约束由逻辑构造入口验证。不得使用诸如“1 开头表示单位、2 开头表示动作”的数字分段编码。

#### 2.2.2 什么需要独立 ID

- `UnitDefinition`、`ActionSpec`、`VolumeSpec`、`AttackPatternSpec`、`MovementPatternSpec`、`DamageChannelDefinition`、`ImpactProfileDefinition`、阵营、固定出场槽位和 Controller 需要稳定配置 ID，因为它们会被其他定义、权限映射或回放记录引用。
- 战斗单位、ActionPlan、ReactionOpportunity、TurnWindow，以及需要单独叠加、移除或到期的效果实例需要战斗实例 ID。
- 命令、Intent、Resolution、事件使用单调 Sequence 固定顺序；如果它们不会被其他对象独立引用，不再额外创建一份同义 ID。
- `GridPoint`、伤害值、`UnitState` 等纯值不需要 ID。`MovementSegment` 使用 `(ActionPlanId, StepIndex)` 定位，Reservation 归属于 `ActionPlanId`，不额外创建 `MovementSegmentId` 或 `ReservationId`。

#### 2.2.3 UnitId 的确定性分配

固定参战单位先由 Authoring 生成纯数据槽位：

```csharp
public sealed record EncounterUnitSlot(
    EncounterSlotId SlotId,
    UnitDefinitionId DefinitionId,
    FactionId FactionId,
    GridPoint InitialPosition,
    GridDirection InitialFacing);
```

战斗初始化时，先验证 `EncounterSlotId` 非空且唯一，再使用 `StringComparer.Ordinal` 按 SlotId 排序，并依次分配 `UnitId(1)`、`UnitId(2)`……。动态召唤单位按已经规范化的命令处理顺序调用同一个 `NextUnitId()`；只要初始定义、命令和 RNG 相同，分配结果就相同。单位死亡后 ID 仍不复用。

```csharp
public sealed class LogicIdGenerator
{
    private long _nextUnitId = 1;
    private long _nextActionPlanId = 1;
    private long _nextReactionOpportunityId = 1;
    private long _nextWindowId = 1;
    private long _nextEffectId = 1;

    public UnitId NextUnitId() => new(_nextUnitId++);
    public ActionPlanId NextActionPlanId() => new(_nextActionPlanId++);
    public ReactionOpportunityId NextReactionOpportunityId() =>
        new(_nextReactionOpportunityId++);
    public WindowId NextWindowId() => new(_nextWindowId++);
    public EffectId NextEffectId() => new(_nextEffectId++);
}
```

所有 `_next...` 值和所有 Sequence 的下一个值都必须进入规范化快照与哈希，用于尽早发现分配顺序的确定性偏差；正常运行中不得重复、回退或根据当前集合重新计算。首版不要求从该快照恢复生成器状态，开发者回放通过相同初始输入从 Tick 0 重建。UnityView 以 `EncounterSlotId` 找到初始化映射，再保存只读 `UnitId` 绑定；不得把运行时分配结果手填进场景。

### 2.3 完整配置与战斗初始化链

纯逻辑内核不能直接读取场景、Resources、ScriptableObject 或旧字符串名称。进入 `BattleSimulation` 前，Authoring 必须把当前 Encounter 使用的全部配置转换成一个闭合、不可变、可哈希的 `BattleDefinition`：

```csharp
public sealed record BattleDefinition(
    string RulesVersion,
    int TicksPerSecond,
    BattleRules Rules,
    ConcurrentActionDefinition ConcurrentAction,
    ReactionRules ReactionRules,
    AdrenalineRules AdrenalineRules,
    FactionModelDefinition FactionModel,
    IReadOnlyList<DamageChannelDefinition> DamageChannels,
    IReadOnlyList<ImpactProfileDefinition> ImpactProfiles,
    IReadOnlyList<UnitDefinition> Units,
    IReadOnlyList<ActionSpec> Actions,
    IReadOnlyList<AttackPatternSpec> AttackPatterns,
    IReadOnlyList<VolumeSpec> Volumes,
    IReadOnlyList<ActionSetDefinition> ActionSets,
    IReadOnlyList<StatusEffectSpec> StatusEffects,
    IReadOnlyList<EncounterDefinition> Encounters,
    string BattleDefinitionHash);

public sealed record ConcurrentActionDefinition(int MetaResourceCost);
public sealed record ReactionRules(
    int CommandIngressLeadTicks,
    int MinimumReactionLeadTicks);

public sealed record AdrenalineRules(
    int MaxAvailablePerCycle,
    int DamageDealtGainQ10,
    int DamageReceivedGainQ10,
    int SuccessfulBlockReward,
    int SuccessfulDodgeReward,
    int ClashReward);

public enum FactionDisposition
{
    Allied  = 0,
    Neutral = 1,
    Hostile = 2
}

public enum UnitRelation
{
    Self    = 0,
    Allied  = 1,
    Neutral = 2,
    Hostile = 3
}

[Flags]
public enum TargetRelationMask
{
    None    = 0,
    Self    = 1 << 0,
    Allied  = 1 << 1,
    Neutral = 1 << 2,
    Hostile = 1 << 3
}

public sealed record FactionDefinition(FactionId FactionId);

// FactionAId 必须按 StringComparer.Ordinal 小于 FactionBId；同一无序对只能出现一次。
public sealed record FactionRelationDefinition(
    FactionId FactionAId,
    FactionId FactionBId,
    FactionDisposition Disposition);

public sealed record FactionModelDefinition(
    IReadOnlyList<FactionDefinition> Factions,
    IReadOnlyList<FactionRelationDefinition> Relations);

public sealed record ControllerBinding(
    ControllerId ControllerId,
    CommandSourceKind SourceKind,
    IReadOnlyList<EncounterSlotId> ControlledSlots);

public sealed record VictoryDefinition(
    IReadOnlyList<FactionId> AlliedFactionIds,
    IReadOnlyList<FactionId> HostileFactionIds,
    string VictoryResultCode,
    string DefeatResultCode,
    string DrawResultCode);

public sealed record EncounterDefinition(
    EncounterDefinitionId EncounterId,
    GridBoundaryDefinition GridBoundary,
    IReadOnlyList<EncounterUnitSlot> Slots,
    IReadOnlyList<ControllerBinding> Controllers,
    VictoryDefinition Victory);

public sealed record BattleRuntimeInputs(
    ulong InitialRngSeed,
    int InitialMetaResource);
```

#### 2.3.1 Pattern / Volume 的构建期整数展开

当前项目的 `AttackPattern` 与 `UnitVolume` 确实会按朝向旋转离散三角格：旧实现由 `GridMath.Rotate(TrianglePoint, int)` 在运行时调用 `Mathf.Sqrt/Cos/Sin/RoundToInt`，`AttackPattern.GetAffectedTriangles`、`UnitVolume.GetVolumeFor`、命中查询、占位和寻路都可能消费该结果。现有 Pattern 资产提供 East（偶数方向）与 EastNorth（奇数方向）两套基准，现有 Volume 资产也提供方向 0/1；对当前已序列化坐标的只读检查未发现非法点，因此这不是已证实的现有玩法错误，而是迁移到确定性纯 Logic 前必须关闭的输入契约。

首版不在 Logic 运行时重新实现任意角旋转。`BattleDefinitionBuilder` 在读取 Authoring 资产时一次性把两套基准展开成按 `GridDirection` 底层整数值 `0 -> 11` 排列的规范表；Logic 只保存和查询最终整数表：

```csharp
public sealed record DirectionalTriangleSet(
    GridDirection Direction,
    IReadOnlyList<TrianglePoint> Triangles);

// Directions.Count 必须为 12，且 Directions[i].Direction == (GridDirection)i。
public sealed record AttackPatternSpec(
    AttackPatternId AttackPatternId,
    IReadOnlyList<DirectionalTriangleSet> Directions);

public sealed record VolumeSpec(
    VolumeSpecId VolumeSpecId,
    IReadOnlyList<DirectionalTriangleSet> Directions);
```

对合法三角格点 `(X, Y, T)`，`T` 只能为 `-1` 或 `1`，且 `X + Y + T` 必须为偶数。逆时针旋转 60° 的唯一整数变换固定为：

```text
X' = (X - 3Y - T) / 2
Y' = (X +  Y + T) / 2
T' = -T
```

上述奇偶约束保证两个除法都整除；Builder 使用受检整数运算，越界即拒绝，不能转成浮点后舍入。偶数方向由 East 基准连续应用 `0..5` 次 60° 旋转，奇数方向由 EastNorth 基准连续应用 `0..5` 次 60° 旋转，交错形成 12 个方向。每个方向内按 `(X, Y, T)` 去重并升序规范化；若带朝向的 `MovementPatternSpec` 也使用三角格偏移，则复用完全相同的最终表契约。

兼容导入必须同时取得 East 与 EastNorth 基准；缺少任一基准、非法 `T`、奇偶约束失败、受检运算溢出或最终表不满 12 向时，整体定义构建失败。Builder 至少冻结 `DIRECTIONAL_BASE_MISSING`、`TRIANGLE_POINT_INVALID`、`DIRECTIONAL_EXPANSION_OVERFLOW` 和 `DIRECTIONAL_TABLE_INCOMPLETE`，不得用偶数基准猜测 30° 方向，也不得退回旧运行时旋转。若旧资产显式序列化了其他方向，Builder 只能把它们作为校验输入，与上述生成后的规范结果逐点比较；不允许用加载顺序决定覆盖结果。

`BattleDefinitionHash` 哈希最终规范化的 12 向整数表、`GridDirection` 数值和方向顺序，不哈希资产内点的原始排列、资产路径或 Builder 实现细节。因而改写 Builder 但产生相同规范表时哈希保持不变；任何会改变实际命中区或占位区的表变化都会改变哈希。Logic 的命中、占位、Dodge 合法性和寻路只可按方向索引该表，再与单位逻辑位置做整数平移；不得调用 `Mathf`、三角函数、浮点舍入或旧 `GridMath.Rotate`。

#### 2.3.2 阵营模型、单位关系与目标资格

`FactionId` 只表示单位归属；`FactionDisposition` 表示两个阵营之间的关系；`UnitRelation` 表示一次具体的“来源单位 → 候选单位”查询结果。三者不能互相替代。首版关系模型冻结如下：

Logic 中的 `FactionDefinition` 首版只需要稳定 ID；显示名、本地化键、图标和颜色属于 Authoring/UnityView 的 `FactionPresentationDefinition` 或等价映射，不参与敌我推导，也不能回写 Logic。只有真正影响玩法的阵营字段才可进入 Logic 定义与哈希。

1. `FactionModelDefinition.Relations` 是静态、对称、闭合矩阵的规范化上三角表示。若共有 N 个阵营，必须恰好配置 `N * (N - 1) / 2` 个不同阵营无序对；不得缺项后默认 Hostile/Neutral，也不得同时配置 A→B 与 B→A。
2. 同一个 `FactionId` 的两个不同单位固定得到 `UnitRelation.Allied`；来源和候选是同一个 `UnitId` 时固定得到 `UnitRelation.Self`。不同阵营只读取已验证矩阵，任何未知阵营、缺失关系或冲突关系都是定义/运行时不变量错误。
3. `FactionRelationResolver.Classify(sourceUnitId, targetUnitId)` 是 Logic 中唯一单位关系查询；`Allows(mask, source, target)` 先得到上述唯一关系，再检查对应 `TargetRelationMask` 位。关系查询只读运行时单位的 `FactionId` 和不可变 `FactionModelDefinition`，不得读取 `ControllerBinding`、当前窗口、AI/Player 来源、GameObject Tag/Layer 或 `IsPlayerControlled`。
4. 所有能够产生单位目标接触的 `AttackPayloadSpec` 必须显式声明非空 `AllowedTargetRelations`。`PrimaryTargetOnly` 在 Add、编辑后候选校验和启动门禁都调用同一 `Allows`；命令目标关系不允许时稳定拒绝 `TARGET_RELATION_NOT_ALLOWED`，启动门禁发现既有目标不再合法时以 `TargetInvalid` 终止且不自动换目标。`AllTargetsInArea` 在 Telegraph 候选与 Impact 接触构建时逐单位调用它，不满足者只是没有接触。Self、友军伤害与中立目标都只能通过显式位开启；当前旧攻击迁移时逐资产确认，不能在 Logic 中补一个“默认只打敌人”。
5. `ReactionOpportunity` 只为该来源攻击按空间、`TargetPolicy` 和 `AllowedTargetRelations` 确实可能影响的单位创建。若动作显式允许友军伤害，被威胁友军也可按相同规则获得反应；关系本身不授予 Block/Dodge，仍需 ActionSet、标签、状态、Lane、空间和肾上腺素全部合法。
6. AI 与 UI 可以用相同解析结果筛选/展示候选，但只能 Logic 的命令处理器作最终授权。关系模型不授予控制权，也不改变所有活单位的 LogicGrid 占位/Reservation；攻击—攻击 Clash 是否成立继续由空间与动作交互规则决定，不能仅因两个 Controller 不同就自动成立。
7. `VictoryDefinition` 是 Encounter 的目标分组，不是第二套敌我查询。`AlliedFactionIds` 内部必须在关系矩阵中互为 Allied，每个 Allied—Hostile 跨组对必须互为 Hostile；`HostileFactionIds` 内部不附加关系约束，允许多个敌对阵营彼此 Allied、Neutral 或 Hostile。未列入两组的“目标外阵营”不参与该消灭条件，但仍保留矩阵中的真实关系，不能被一概改写为 Neutral。
8. 首版单位的 `FactionId` 在战斗创建后不可变。动态生成/召唤必须从已验证定义解析出固定阵营，或使用定义中显式声明的 `InheritSourceFaction` 策略，并把解析后的 `FactionId` 写入创建事件和 UnitSnapshot；命令、Controller 和 View 不得直接提供或改写阵营。运行时外交、魅惑换阵营和非对称关系属于后续版本化扩展。

```csharp
public interface IFactionRelationResolver
{
    UnitRelation Classify(UnitId sourceUnitId, UnitId targetUnitId);
    bool Allows(
        TargetRelationMask allowedRelations,
        UnitId sourceUnitId,
        UnitId targetUnitId);
}
```

Builder 至少冻结 `FACTION_RELATION_MISSING`、`FACTION_RELATION_DUPLICATE`、`FACTION_RELATION_NOT_CANONICAL`、`FACTION_RELATION_UNKNOWN_ID`、`TARGET_RELATION_MASK_INVALID` 和 `VICTORY_FACTION_RELATION_CONFLICT`；已通过 Builder 的战斗若仍出现未知 FactionId 或无法解析关系，则以 `FACTION_RELATION_INVARIANT_VIOLATION` 明确令 Step 失败，不能退回 Neutral/Hostile 后继续。

关系矩阵、每个动作的 `AllowedTargetRelations`、动态生成阵营策略与 `VictoryDefinition` 都进入 `BattleDefinitionHash`。矩阵输入枚举顺序不影响规范结果；规范顺序固定为 `FactionAId -> FactionBId` 的 Ordinal 顺序。由于首版关系和单位阵营均不可变，矩阵无需逐 Tick 重复写入 `LogicSnapshot`，但回放 Header 必须携带匹配的 `BattleDefinitionHash`，每个 UnitSnapshot 必须携带其 `FactionId`。

字段可在实现时拆分成更细的不可变类型，但必须覆盖所有会影响初始世界和未来战斗结果的数据。`BattleDefinitionBuilder` 是唯一配置真相入口，职责包括：

- 转换全部当前战斗引用的 ActionLibrary、AttackPattern、UnitVolume、单位、动作集合和 Encounter，而不是只转换一条样例。
- 校验稳定 ID、跨定义引用、数值范围、Tick 量化、动作时序、PathCost/PathSearch 规则、Pattern/Volume 两套方向基准与三角格点、Encounter 合法网格边界/槽位、闭合 Faction 关系矩阵、动作目标关系掩码与胜负分组；任一被引用配置失败时整体拒绝。
- 在 Authoring 边界用 2.3.1 的受检整数公式展开并规范化 Pattern/Volume 的 12 向最终表；完整定义中不保留需要 Logic 运行时解释的旋转配方或浮点坐标。
- 对集合做规范化排序并计算 `BattleDefinitionHash`；最终 12 向表参与哈希，Unity 资产发现顺序、资产内点顺序、显示名和路径不得影响逻辑结果。
- 接收显式旧 ID 迁移清单。旧资产可以用“资产 GUID + 局部条目 ID”在 Authoring 边界定位，但最终 Logic ID 必须是人工确认的稳定字符串，GUID 不进入 Logic 或 Replay。
- 拒绝同一个最终 ID 对应不同内容。诸如多个动作库都包含 `QuickSlash` 时，只有规范化定义完全一致且迁移清单明确声明共享才可合并，否则必须使用不同 `ActionSpecId`。

`ControllerBinding` 使用稳定 `ControllerId` 和 `CommandSourceKind` 绑定一组 `EncounterSlotId`；同一槽位的可控关系、同一 `ControllerId` 的重复定义和外部可伪造的 `System` 来源都必须在构建时拒绝。`FactionId` 与控制权正交：每个槽位必须引用 `FactionModelDefinition` 中的已定义阵营，关系矩阵必须闭合；`VictoryDefinition` 的 Allied/Hostile 集合必须非空、互不相交并只引用该 Encounter 中出现的阵营，同时满足 Allied 组内部互为 Allied、每个 Allied—Hostile 跨组对互为 Hostile；Hostile 组内部关系不受目标分组覆盖。未列入两组的目标外阵营不参与消灭判定，但其目标资格仍读取关系矩阵。不得从玩家/AI Controller、`IsPlayerControlled` 或单位定义猜测阵营。战斗初始化只接收 `BattleDefinition + EncounterDefinitionId + BattleRuntimeInputs`，按 `EncounterSlotId` 的 Ordinal 顺序分配 `UnitId`，把槽位阵营复制到运行时单位，把控制关系映射为只读 `ControllerId -> UnitId 集合`，并返回 `EncounterSlotId -> UnitId` 映射供 View 绑定。它不得读取 GameObject 名、场景注册顺序、`GetInstanceID()`、运行时随机 GUID 或外部 Singleton。

任务边界固定为：

- 任务 02 只冻结纯数据接口并用旧程序集适配器验证一条切片。
- 任务 02B 负责完整 Authoring asmdef、全资产 ID 迁移、Builder、哈希和初始化入口。
- 任务 03 使用 02B 产出的真实定义 fixture，不建立测试专用的第二套配置模型。
- 任务 10 只选择 Encounter、调用初始化并绑定 Slot；发现缺失映射或非法定义时回到 02B 修复，不得运行时生成临时 ID 或默认值。

---

## 三、具体优化方案

### 3.0 落地优先级修订

| 优先级 | 目标 | 包含内容 | 不包含内容 |
|------|------|----------|------------|
| **P0-契约与地基** | 建立可验证的确定性边界 | 分阶段程序集隔离、完整 `BattleDefinition`/Faction 关系模型与初始化链、整数 Tick、单调逻辑 ID、稳定遍历、`LogicEventOutbox`、单位注册表、状态机、Tick 阶段骨架、快照夹具 | 大规模资源模型替换、完整回合系统 |
| **P1-玩法核心** | 迁移真正影响战斗结果的系统 | `TurnWindow`、整数时间预算、肾上腺素、命令批次、全局动作轨道、`LogicGrid`、AI Tick 化、从 Tick 0 重演的开发者回放记录 | 高级 Buff、跨版本回放兼容 |
| **P2-完善** | 降低耦合并补齐本轮长期能力 | `CombatUnitView`、表现层重构、Buff 配置扩展与表现接入、开发者回放控制、性能基准 | 玩家回放产品、任意跳转、中途存档/恢复 |
| **后续独立项目** | 产品化或支持随机访问 | 玩家回放库、分享/观战、任意 Tick 跳转、周期性完整状态检查点、版本迁移 | 不纳入本轮完成定义 |

**实施原则：** 每一阶段结束时都必须保留一个可运行中间版本；如果一个改动会同时触碰资源、回合、状态机和表现层，应拆成两个阶段。

### 3.1 【P1-重要】资源模型重构

#### 3.1.1 新的 CombatUnitLogic 属性定义

```csharp
public sealed class UnitStats
{
    // 基础属性（简化后）
    public float Strength = 10f;
    public float Dexterity = 10f;
    public float Constitution = 10f;
    public float Wisdom = 10f;
    public float Intelligence = 10f;

    // 派生属性
    public float MaxHealth => Constitution * 20f;
    public float ActionSpeed => 10f + Dexterity * 0.5f; // 只修正攻击前摇，不修正后摇
    public float MoveSpeed => 10f + Dexterity * 0.5f;   // 只修正普通 Move 每权重单位 Tick
    public IReadOnlyDictionary<DamageChannelId, int> BaseDamageResistanceQ10;

    // 冲击与控制量化输入；不与动作时序速度混用
    public float Mass = 100f;
    public float MomentumSpeed = 10f;

    // 单位资源；回合共享预算不存放在单位上
    public float CurrentHealth = 100f;
    public int AvailableAdrenaline = 0;
    public long AdrenalineCycleId = 0;
    public int MaxTurnBudgetTicks = 180; // 3.0s @ 60 Tick/s
}

// 肾上腺素规则：
// - 获得：按实际造成/承受伤害量化，成功高阶反应与拼刀按 AdrenalineRules 奖励
// - 不随时间衰减
// - Block/Dodge 接受时从 Available 转入带 CycleId 的计划预留，TriggerTick 消费
// - Available 跨其他单位窗口保留；该单位下一次自己的窗口打开时清零
// - 已接受反应的预留可跨周期履约，但旧周期预留取消时不得返还到新周期
```

`ActionSpeed` 只服务攻击前摇在普通计划首次进入 Editable 时的量化，并由此影响动作总时长与预算预留；`MoveSpeed` 只服务普通 Move 每权重单位 Tick 的首次量化，不参与路线选择；二者都不缩短 Recovery，也不因后续时间线拖动重新采样。`Mass * MomentumSpeed` 只服务动量、硬直、击倒与击退量化。兼容适配器在战斗创建时把旧 `TotalMass`、`Swiftness` 和各类防御字段一次性转换为上述纯逻辑字段，战斗开始后不得回读旧组件。

`CurrentHealth` 等数值可在首轮迁移中继续使用 `float`，但不得由 `deltaTime` 累加。快照序列化必须使用统一格式和精度；所有会影响分支的阈值计算必须集中在逻辑层，并由回归测试覆盖。

#### 3.1.2 动作定义与一次性量化

```csharp
public abstract record ActionTimingSpec;

// 每种动作族必须有自己的权威 TimingSpec。
public sealed record AttackTimingSpec(
    int BaseWindupTicks,
    int RecoveryTicks) : ActionTimingSpec;

public sealed record GuardTimingSpec(
    int WindupTicks,
    int ActiveTicks,
    int RecoveryTicks) : ActionTimingSpec;

public sealed record MoveTimingSpec(
    int BaseStepTicks,
    int RecoveryTicks) : ActionTimingSpec;

public sealed record BlockReactionTimingSpec(
    int ReactionWindupTicks,
    int RecoveryTicks) : ActionTimingSpec;

public sealed record DodgeReactionTimingSpec(
    int ReactionWindupTicks,
    int RecoveryTicks) : ActionTimingSpec;

public abstract record ActionPayloadSpec;

public sealed record AttackPayloadSpec(
    IReadOnlyList<DamageComponentSpec> DamageComponents,
    ImpactProfileId ImpactProfileId,
    float ForceMultiplier,
    TargetPolicy TargetPolicy,
    TargetRelationMask AllowedTargetRelations,
    int MomentumDirectionOffsetSteps,
    AttackPatternSpec Pattern,
    AttackTagMask Tags) : ActionPayloadSpec;

public sealed record GuardPayloadSpec(
    IReadOnlyDictionary<DamageChannelId, int> DamageResistanceQ10,
    int MomentumResistanceQ10,
    DefenseTagMask EligibleTags) : ActionPayloadSpec;

public sealed record BlockPayloadSpec(
    DefenseTagMask EligibleTags) : ActionPayloadSpec; // 合格载荷固定使用 1024/1024

public sealed record MovePayloadSpec(
    int MaxPathWeightUnits,
    MovementPatternSpec Pattern) : ActionPayloadSpec;

public sealed record DodgePayloadSpec(
    int MaxDistanceSteps,
    MovementPatternSpec Pattern) : ActionPayloadSpec;

// 纯逻辑不可变定义；由 Authoring 层从 ScriptableObject 转换而来
public sealed record ActionSpec(
    ActionSpecId ActionSpecId,
    ActionType Type,
    ActionTimingSpec Timing,
    ActionPayloadSpec Payload,
    int AdrenalineCost);

// 命令通过校验后生成；它是预算、动作轨道和 Intent 的共同依据
public enum ActionPlanState
{
    Editable,   // 普通计划：可通过排程事务移动、删除和连锁重算
    Locked,     // 固定反应计划的持久状态；普通计划只在原子启动提交中跨过该边界
    Running,
    Completed,  // 自然完成
    Terminated  // 非自然终止，必须携带 ActionTerminationReason
}

public enum ActionTerminationReason
{
    CancelledByCommand,
    TargetInvalid,
    ActorUnavailableAtStart,
    AutoDeferralLimitExceeded,
    SourceThreatCancelled,
    InterruptedByClash,
    InterruptedByIntercept,
    InterruptedByControl,
    ReservationPreemptedByForcedDisplacement,
    MovementOriginInvalidated,
    OwnerDied,
    BattleEnded
}

public sealed class ActionPlan
{
    public ActionPlanId ActionPlanId { get; init; }
    public WindowId? SubmittedWindowId { get; init; } // 普通动作必填；反应计划为空
    public ReactionOpportunityId? ReactionOpportunityId { get; init; }
    public ActionPlanId? SourceThreatPlanId { get; init; }
    public UnitId ActorUnitId { get; init; }
    public ActionSpecId ActionSpecId { get; init; }
    public GridDirection Facing { get; init; }
    public UnitId? PrimaryTargetUnitId { get; init; }
    public GridPoint? Destination { get; init; }

    // LastRequestedStartTick 只记录最近一次直接拖动请求；显式 ripple 或系统自动延期不会把它
    // 当成未来“自动左吸”的依据。Start/End 是当前权威投影，Locked 后冻结。
    public long? LastRequestedStartTick { get; private set; }
    public long StartTick { get; private set; }
    public long? ImpactTick { get; private set; }
    public long? TriggerTick { get; private set; }      // Block/Dodge 固定，由来源威胁推导
    public long? ActiveStartTick { get; private set; }
    public long? ActiveEndTick { get; private set; }
    public int ResolvedWindupTicks { get; private set; }
    public int? ResolvedPathEdgeCount { get; private set; }
    public int? ResolvedPathWeightUnits { get; private set; }
    public int? ResolvedBaseStepTicks { get; private set; }
    public int RecoveryTicks { get; private set; }
    public long EndTick { get; private set; }
    public int BudgetCostTicks { get; private set; }
    public int ReservedTurnBudgetTicks { get; private set; }
    public int ReservedAdrenaline { get; init; }
    public long? AdrenalineReservationCycleId { get; init; }
    public long CreatedAtTick { get; init; }
    public long LastEditedScheduleRevision { get; private set; }
    public int AutomaticDeferralCount { get; private set; }
    public long? LockedAtTick { get; private set; }
    public bool ReactionTriggered { get; private set; }
    public ActionPlanState State { get; private set; } = ActionPlanState.Editable;
    public ActionTerminationReason? TerminationReason { get; private set; }
    public long? TerminalTick { get; private set; }

    // 只能由 ScheduleEditor 或执行前门禁的系统自动延期事务对 Editable 普通计划调用；
    // 一次事务内统一写入。系统延期不改 LastRequestedStartTick。
    internal void ApplyEditableProjection(/* canonical projection */) { /* ... */ }

    // 只能作为 ActionStartCommitter 已完成全部候选校验后的最终写入之一调用；
    // 普通计划不暴露可观察的 Locked-but-not-Running 中间态，但保留锁定事实供事件/审计。
    internal void ApplyOrdinaryStartCommit(long tick)
    {
        if (State != ActionPlanState.Editable)
            throw new InvalidOperationException("PLAN_NOT_EDITABLE_AT_START_COMMIT");
        LockedAtTick = tick;
        State = ActionPlanState.Running;
    }

    // 只供 ReactionPlanner 的接受事务使用；固定反应创建后直接 Locked。
    internal void InitializeReactionLocked(long tick)
    {
        LockedAtTick = tick;
        State = ActionPlanState.Locked;
    }

    // 只允许 ActionPlanTerminalCoordinator 调用。
    internal bool TryEnterTerminal(
        ActionPlanState terminalState,
        ActionTerminationReason? reason,
        long tick)
    {
        if (IsTerminal(State)) return false;
        if (!IsTerminal(terminalState))
            throw new ArgumentOutOfRangeException(nameof(terminalState));
        if (terminalState != ActionPlanState.Completed && reason is null)
            throw new ArgumentNullException(nameof(reason));

        State = terminalState;
        TerminationReason = reason;
        TerminalTick = tick;
        return true;
    }

    private static bool IsTerminal(ActionPlanState state) =>
        state is ActionPlanState.Completed or ActionPlanState.Terminated;
}
```

`ActionType`、Timing 与 Payload 必须使用闭合匹配矩阵，不能把 AttackTiming 配给 Guard 或让普通动作伪装成反应。每个 Attack 的 `AllowedTargetRelations` 必须非空且不含未定义位；是否允许 Self、Allied 或 Neutral 必须由资产显式声明。`Attack/Guard/Move` 的 `AdrenalineCost` 必须为 0；`Block/Dodge` 必须为正。Guard 对已声明合格伤害通道的每项动作抵抗及其动量抵抗必须严格位于 `[1,1023]`，从定义层保证“部分抵抗”；Block 的合格伤害/动量抵抗由规则固定为 1024，Authoring 不提供可降低该值的字段。单位被动抵抗仍可使用 `[0,1024]`。Builder 对不匹配或越界定义整体拒绝，不能在运行时钳制。

`Completed` 表示计划自然完成，可以没有 `ActionTerminationReason`；`Terminated` 必须记录原因。首版至少区分 `CancelledByCommand`、`TargetInvalid`、`ActorUnavailableAtStart`、`AutoDeferralLimitExceeded`、`SourceThreatCancelled`、`InterruptedByClash`、`InterruptedByIntercept`、`InterruptedByControl`、`ReservationPreemptedByForcedDisplacement`、`MovementOriginInvalidated`、`MovementOriginInvalidatedByDodge`、`OwnerDied` 和 `BattleEnded`，不能把主动取消、启动失败、来源威胁消失、交互打断、强制位移破坏空间承诺、死亡和战斗结束混写为一个无原因的 `Cancelled`。`ActorUnavailableAtStart` 表示普通计划在最终门禁发现无有限恢复时机，或一次由临时单位状态触发的合法自动延期无法形成可启动投影；`AutoDeferralLimitExceeded` 表示超过每计划自动延期次数或最大排程视野；永久目标、目的格或路径失效继续使用 `TargetInvalid`。`MovementOriginInvalidated` 专指单位已经被本 Tick 批量换位、其 Move 计划仍依赖旧起点；`ReservationPreemptedByForcedDisplacement` 专指计划拥有的空间承诺被本批最终强制位移 footprint 抢占。同一计划第一次成功进入终态后，后续重复或冲突的终态请求都是幂等空操作，不覆盖原状态、原因或 `TerminalTick`。

首版攻击正式采用“速度修正前摇 → 命中 → 固定后摇”的时序。旧攻击资产中的 `BaseTime`（单位秒）在兼容导入时解释为 `BaseWindupSeconds`；新 Authoring 字段直接命名为 `BaseWindupSeconds`。秒只在 `BattleDefinitionBuilder` 中转换一次：

Authoring 先验证秒值有限且大于 0，再执行以下量化；若正秒值舍入后小于 1 Tick，也以稳定配置错误拒绝，不通过钳制制造有效配置：

```csharp
int baseWindupTicks =
    RoundHalfUp(baseWindupSeconds * BattleRules.TicksPerSecond);

int recoveryTicks = recoverySeconds.HasValue
    ? RoundHalfUp(recoverySeconds.Value * BattleRules.TicksPerSecond)
    : BattleRules.DefaultAttackRecoveryTicks; // 30 Tick = 0.5s

if (baseWindupTicks < 1 || recoveryTicks < 1)
    return DefinitionResult.Reject("ATTACK_TIMING_OUT_OF_RANGE");
```

以下常量和公式属于首版玩法规则，并进入 `BattleDefinitionHash`：

```text
ReferenceActionSpeed = 20
ReferenceMoveSpeed = 20
DefaultAttackRecoveryTicks = 30

ResolvedWindupTicks =
  max(1, RoundHalfUp(
    BaseWindupTicks * ReferenceActionSpeed / Actor.ActionSpeed))

ImpactTick       = StartTick + ResolvedWindupTicks
EndTick          = ImpactTick + RecoveryTicks
BudgetCostTicks  = ResolvedWindupTicks + RecoveryTicks
```

`ActionSpeed` 必须是有限正数；`BaseWindupTicks` 与 `RecoveryTicks` 必须为正整数。非法值以稳定原因码拒绝，不能钳制后继续模拟。速度修正只作用于前摇，后摇不受 `ActionSpeed` 缩短。兼容导入缺少独立后摇字段的旧攻击资产时，统一使用 30 Tick 默认值；不得继续在调度器中硬编码 `0.5f`。

普通攻击首次作为 `Editable` ActionPlan 原子加入权威排程时，从当时的单位只读属性只解析一次相对时长 `ResolvedWindupTicks`、`RecoveryTicks` 与 `BudgetCostTicks`。显式移动、显式 ripple 或系统自动延期会据新 `StartTick` 重算绝对 `ImpactTick/EndTick`，但不会重新采样 `ActionSpeed`；计划排队期间发生的速度 Buff/Debuff 只影响之后新建的计划。执行前门禁返回 Startable 后，原子启动提交把当前权威投影冻结为执行字段；Locked/Running 后任何排程事务都不得再改写。

首版普通攻击的阶段与同 Tick 语义固定为：

- `Windup = [StartTick, ImpactTick)`。
- 在 `ImpactTick`，状态进入 `Recovery`，并且恰好产生一个 `AttackIntent`。
- `Recovery = [ImpactTick, EndTick)`；在 `EndTick` 进入动作结束边界。
- `AttackIntent` 的有效性由 ActionPlan 与 Intent 生命周期判断，不能仅因单位在 `ImpactTick` 已显示为 `Recovery` 而被过滤。
- 在 `ImpactTick` 之前进入终态的计划不得再产生攻击 Intent；已经进入 `ImpactTick` 仲裁的 Intent 先参与本 Tick 的 Dodge、Block、Clash、Move、Guard 和 Hit 求解，再按 Resolution 进入动作终态。
- 首版不实现多段攻击。未来若需要多段命中，必须在 `ActionTimingSpec` 中显式增加多个命中偏移并更新规则版本，不能由调度器隐式平均分配。

逻辑层中的攻击总时长与预算成本均为 `ResolvedWindupTicks + RecoveryTicks`，保持“时间既是持续时间也是成本”的设计。若以后要让预算成本与占用时间分离，或让其他资源缩短后摇，必须作为明确玩法变更引入。

其他普通动作同样在首次进入 `Editable` 时量化与属性有关的相对时长；排程编辑可以重绑绝对 Tick，并按依赖链重算尚未锁定 Move 的路径边数与路径权重。反应计划没有 Editable 阶段，接受后直接以固定 Tick 进入 `Locked`：

```text
Guard:
  ActiveStartTick = StartTick + WindupTicks
  ActiveEndTick   = ActiveStartTick + ActiveTicks
  EndTick         = ActiveEndTick + RecoveryTicks
  BudgetCostTicks = WindupTicks + ActiveTicks + RecoveryTicks

Move:
  ResolvedPathEdgeCount = ValidatedPath.EdgeCount
  ResolvedPathWeightUnits = sum(PathCostRules.StepWeightUnits(edge.Direction))
  ResolvedBaseStepTicks =
    max(1, RoundHalfUp(BaseStepTicks * ReferenceMoveSpeed / Actor.MoveSpeed))
  Segment[i].DurationTicks =
    checked(Segment[i].StepWeightUnits * ResolvedBaseStepTicks)
  EndTick         = checked(StartTick + ResolvedPathWeightUnits * ResolvedBaseStepTicks + RecoveryTicks)
  BudgetCostTicks = checked(ResolvedPathWeightUnits * ResolvedBaseStepTicks + RecoveryTicks)

Block / Dodge:
  TriggerTick     = SourceAttack.ImpactTick
  StartTick       = TriggerTick - ReactionWindupTicks
  EndTick         = TriggerTick + RecoveryTicks
  BudgetCostTicks = 0
  ReservedAdrenaline = ActionSpec.AdrenalineCost
```

Guard 只在半开 Active 区间内提供部分抵抗。Move 首次加入排程时量化当时 `MoveSpeed` 得到固定 `ResolvedBaseStepTicks`；`MoveSpeed` 不得进入 A* 的 `g/h/f` 或改变路线。`Editable` 期间如果自身 Destination、Lane 顺序或更早 Move 改变了预测起点，排程器必须用同一确定性寻路规则重算受影响链的路径、`ResolvedPathEdgeCount`、`ResolvedPathWeightUnits`、绝对 Tick、预算预留和空间 Reservation，但不重新采样速度。锁定后这些字段全部固定，属性变化、视觉距离或后续排程编辑都不得重算。Block/Dodge 只有 TriggerTick 一次特殊判定；Windup 与 Recovery 不提供抵抗或无敌。

旧 `ActionScheduler` 的 `Clamp(distance / (Swiftness * 0.1), 0.2s, 4.0s)` 只保留为任务 01 characterization；New 规则明确不保留这个逐边浮点 Clamp。首版只在每权重单位量化时应用 `max(1 Tick, ...)`，随后按整数边权相乘，因此同一计划中权重 2 的边始终消耗权重 1 边的两倍 Tick/预算。这是显式玩法规则变化，必须更新 `RulesVersion`、定义哈希与移动 golden snapshot，不能作为“重构无差异”隐藏。

`MovePayloadSpec.MaxPathWeightUnits` 必须为正且不大于 `BattleRules.PathSearchRules.MaxPathWeightUnits`。速度量化先验证 `MoveSpeed` 有限正数，再使用受检中间值；路径权重、Segment 时长、`EndTick` 或 `BudgetCostTicks` 任一乘加/转换溢出或超出目标字段范围时，整个 SchedulePreview/ScheduleEdit/系统延期候选以 `MOVE_TIMING_OUT_OF_RANGE` 稳定失败并零写入，不得钳制、饱和或只保留部分 Segment。

反应计划的固定区间不能被 ActorLane 尾部向后平移，也不能被时间线编辑器拖动。接受时必须检查 `[StartTick, EndTick)` 与 Lane 中 Locked/Running 计划及当前 Editable 投影是否冲突；冲突则稳定拒绝 `REACTION_LANE_CONFLICT`，首版不自动打断，也不因插入反应而隐式重排普通计划。之后玩家可以显式移动仍为 Editable 的普通计划绕开该固定区间。Dodge 的目的格以 TriggerTick 为生效边界原子预留；如果目的格在接受时已知非法则拒绝，如果在触发前因更早顺序的合法提交变为非法，则反应按 `TargetInvalid` 终止且不退款。

**Dodge 换位后的移动依赖失效（首版固定规则）：**

1. 接受 Dodge 时保留既有 Lane 时间区间、状态、费用、目的格与 Reservation 校验。后续 Move 仅因预测起点可能被 Dodge 改变，不构成拒绝反应的理由；这不豁免真实时间重叠、非法目的格或空间预留冲突。接受、预览和等待触发均不提前取消移动计划，不按预期 Dodge 落点自动改路。
2. 在 TriggerTick 成功提交实际位置变化时，才使该单位尚未开始、仍为 Editable 且依赖旧起点的 Move 及其传递移动依赖闭包失效。例如 A→B→D 前插入 A→C 的 Dodge，成功换位后取消 A→B 与 B→D；不得只取消第一段，也不得清空全部 Lane。依赖查询复用 ScheduleEvaluator 的位置依赖模型，跨过中间的 Attack/Guard 查找移动依赖；以触发时当前权威排程为准，包含接受后新增或编辑的 Move，不能使用接受时缓存的 PlanId 列表直接提交。
3. DodgeTriggerCommitter 先在只读候选中验证落点、实际 From、当前依赖闭包与全部清理参与者，再以单一原子事务完成：按 ActionPlanId 经唯一 ActionPlanTerminalCoordinator 终止受影响 Move、释放其未来 Intent/MovementSegment/空间与预算预留、移除 Lane/活动索引、提交新 Occupancy 并释放 Dodge 目的格预留。终止原因为 `MovementOriginInvalidatedByDodge`，与强制位移的 `MovementOriginInvalidated` 分开记录。计划、网格、账本、索引和事件必须一起提交；不得先取消真实计划再执行可能失败的换位，内部提交失败必须无局部写入并报告 InvariantViolation。
4. 来源攻击取消、Dodge 被控制打断、目的格失效或其他原因使其未实际换位时，不因本 Dodge 改变后续 Move、路径、预留和预算；其他独立终态原因仍照常处理，Dodge 自身终态和肾上腺素退款仍遵守原规则。这里的“成功”只指位置提交成功，不要求避开攻击或获得成功反应奖励；即使新格仍被 AOE 命中，也应执行移动依赖清理。未发生实际位置变化时不触发该清理。
5. 只释放受影响 Editable Move 尚未消费的 Reserved，分别归回原 SubmittedWindowId 账本；关闭窗口仅更新历史账本，不重开、不转移额度。其余 Attack/Guard 等无该空间依赖的计划保留原 Tick 并继续走正常启动门禁；不重算路线、不向左压缩、不向右 ripple。此为执行终态，不是 ScheduleEdit 或系统自动延期，因此不增加 ScheduleRevision、不回卷 ID。Lane 若出现与已执行 Dodge 重叠的 Locked/Running 普通 Move，应报告既有 Lane 不变量错误，不以本规则静默修复。
6. Logic 提供只读条件预检：返回稳定排序的受影响 Move PlanId、数量，以及按 SubmittedWindowId 分组的拟释放 Reserved；预检不分配 ID、不写状态。UI 在确认前提示“闪避成功将取消后续 N 个移动计划，并释放其未消费预算”，接受后标记可能失效的移动；已关闭窗口的额度不得显示为可重新使用。预检只代表当前快照，触发时必须重新求值。实际 Dodge 提交记录/语义事件包含 From/To、反应 PlanId、InvalidatedPlanIds 和按窗口分组的实际释放额，供表现与回放审计。
7. 原子换位及移动依赖清理仍发生在同 Tick 冲突图构建之前；不修改已经冻结的本 Tick Intent，也不产生第二次位置提交。任务 05 提供依赖查询和终态接缝，06 提供空间候选与原子提交，07 接入预算参与者，08 负责触发阶段与记录，10 接入只读预检提示，11 验证重演。本规则及新终态原因进入 RulesVersion / BattleDefinitionHash，更新对应 golden fixture。

#### 3.1.3 执行前启动门禁与系统自动延期

普通计划的 `StartTick` 是当前权威投影，不是创建后永久固定的承诺。到期只表示“本 Tick 必须做最终启动判定”；只有判定成功才会跨过不可退款、不可重排的 Locked 边界。门禁使用闭合结果类型，不允许调用方用布尔值自行猜测“终止、延期还是跳过”：

```csharp
public enum ActionStartGateKind
{
    Startable,
    Retryable,
    Terminal,
    InvariantViolation
}

public enum StartBlockerReason
{
    Staggered,
    KnockedDown,
    Recovering,
    TimedSpatialReservation
}

public sealed record ActionStartGateResult(
    ActionStartGateKind Kind,
    StartBlockerReason? BlockerReason,
    long? RetryAtTick,
    ActionTerminationReason? TerminationReason,
    string? InvariantErrorCode);
```

`BattleRules.MaxAutomaticDeferralsPerPlan` 是大于等于 0 的版本化规则，并与 `MaxScheduleAheadTicks` 一起进入 `RulesVersion` / `BattleDefinitionHash`。`ActionPlan.AutomaticDeferralCount` 进入规范化快照。门禁与提交规则冻结如下：

1. 状态自动到期、持续效果、死亡/胜负、窗口/肾上腺素边界和本 Tick 已冻结命令全部先处理；随后按 `UnitId -> StartTick -> ActionPlanId` 检查 `StartTick == tick` 的 Editable 普通计划。控制效果恰在 Tick T 到期时，状态先恢复，T 的计划应得到 `Startable`，不能多延一 Tick。
2. `Startable` 只允许一个原子启动提交：冻结 Timing/Path/MovementSegment/Reservation，原子执行 TurnBudget `Reserved -> Spent`，设置 `LockedAtTick`、发出 `ActionPlanLockedEvent`，再令同一对象进入 `Running` 并应用起始状态。任一步失败都不允许留下 `Locked`、部分预算消费或已公开 Telegraph；内部提交失败属于不变量错误，不转成普通玩法终态。
3. `Retryable` 只用于能从当前权威状态直接给出有限且严格大于当前 Tick 的 `RetryAtTick`：例如带确定结束 Tick 的 `Staggered`、`KnockedDown`、`Recovering`，或能给出释放 Tick 的限时空间预留。未知/无限控制、Owner 已死亡、永久目标/目的格/路径失效都不得每 Tick 轮询式“跳过”。
4. Retryable 触发一个无外部命令的系统排程事务。它调用与 UI 预览/`ScheduleEditCommand` 完全相同的 `ScheduleEvaluator`，把到期计划的候选起点移到 `RetryAtTick`，并只对其后续 Editable 普通计划依赖闭包做向右 ripple；Locked/Running/终态计划、固定反应区间和其他 Lane 都是不可移动障碍。禁止向左吸附，`LastRequestedStartTick` 保持用户最近一次直接请求值。
5. 系统事务必须在候选图中原子重算绝对 Tick、Move 预测起点/路径/`ResolvedPathEdgeCount`/`ResolvedPathWeightUnits`、预算差额、MovementSegment 与空间 Reservation，并复用相同的视野、队列、预算和 Reservation 校验。它不重新采样 ActionSpeed/MoveSpeed，不转移预算来源，也不因来源窗口关闭而重开提交权限。
6. 成功时计划保持 `Editable`，不产生 Locked/Running 事件；预算继续为 Reserved（仅按同源求值结果原子调整），`AutomaticDeferralCount` 加一，`LastEditedScheduleRevision` 写入新修订，全局 `ScheduleRevision` 恰好加一，并发出一个 `ActionPlanAutoDeferredEvent`。同一 Step 多个到期计划按上述稳定键逐事务处理并读取前一个成功事务后的当前排程。
7. 若下一次延期会超过 `MaxAutomaticDeferralsPerPlan` 或 `MaxScheduleAheadTicks`，以 `AutoDeferralLimitExceeded` 终止；若重算发现目标、目的格或路径已永久失效，以 `TargetInvalid` 终止；无有限恢复 Tick 或由临时单位状态触发的候选事务无法形成合法启动投影时，以 `ActorUnavailableAtStart` 终止。终止发生在 Editable，统一协调器释放未消费 TurnBudget 与空间 Reservation，`ScheduleRevision` 不增加，也不会自动前移后续计划。
8. Lane 投影声称可启动、但单位状态/执行所有权显示另一个普通动作仍占有同一启动区间，或 RetryAtTick 非法等内部矛盾，返回 `InvariantViolation(errorCode)` 并令当前 Step 明确失败；不得把实现错误伪装成 `TargetInvalid` 或吞掉本 Tick。
9. Block/Dodge 创建后已是 Locked，永不进入本自动延期流程。固定反应在 StartTick/TriggerTick 前因控制失去执行资格时按 `InterruptedByControl` 终止，目标/目的格失效按 `TargetInvalid` 终止；Locked/Running 普通计划也不重排。肾上腺素仍只有来源威胁在 TriggerTick 前取消且 Cycle 匹配时返还，控制失败不退款。

系统自动延期是由相同初始状态和命令必然再生的逻辑事实，不写入外部回放输入。事件至少携带 `ActionPlanId`、`StartBlockerReason`、旧/新 StartTick、`RetryAtTick`、新的 `AutomaticDeferralCount`、受 ripple 影响的 PlanId 稳定列表和提交后的 `ScheduleRevision`；从 Tick 0 重演必须逐 Tick 复现并校验。

#### 3.1.4 窗口预算与全局动作轨道

预算只决定当前窗口还能新增多少普通动作，不决定这些动作何时结束。普通 ActionPlan 首次进入 `Editable` 时预留预算；只有启动门禁返回 Startable 时，才在同一个锁定/启动提交中把预留转为已消费。Retryable 系统延期继续保持 Reserved；锁定前终态释放它。时间线编辑不是独立的“整窗口草稿提交”，而是对同一个全局 ActionPlan 注册表执行可回放的原子事务。反应计划走 3.2.4 的机会/肾上腺素事务，创建后直接 Locked，不进入普通排程编辑入口。

```csharp
public sealed record ScheduleEditScope(
    long ExpectedScheduleRevision,
    WindowId? ExpectedWindowId) : CommandScope;

public sealed record ScheduleEditCommand(
    IReadOnlyList<ScheduleEditOperation> Operations) : BattleCommand;

public abstract record ScheduleEditOperation;

public sealed record AddOrdinaryPlan(
    UnitId ActorUnitId,
    ActionSpecId ActionSpecId,
    long? RequestedStartTick,        // null = 在本次事务的 Lane 尾部追加
    ActionPlanId? InsertBeforePlanId,
    GridDirection? Facing,
    GridPoint? Destination,
    UnitId? PrimaryTargetUnitId) : ScheduleEditOperation;

public sealed record MoveEditablePlan(
    ActionPlanId ActionPlanId,
    long RequestedStartTick,
    ActionPlanId? InsertBeforePlanId) : ScheduleEditOperation;

public sealed record RemoveEditablePlan(
    ActionPlanId ActionPlanId) : ScheduleEditOperation;
```

一次命令可以包含若干 Add/Move/Remove，但必须满足以下事务边界：

1. Step 冻结命令批次时同时冻结 `BatchBaseScheduleRevision`。每个 `ExpectedScheduleRevision` 必须等于该基准，否则整批以 `STALE_SCHEDULE_REVISION` 拒绝；不能与本批前一条命令刚递增后的实时值比较，否则同一快照产生的玩家/AI 命令会被错误互斥。操作在命令内按稳定 `OperationOrdinal` 解释；同一计划被重复编辑、插入锚点循环或跨 Lane 非法引用时整批拒绝。同一冻结批次内，不相交 Lane 的事务可按 CommandSequence 依次成功；若后处理事务触及本批已改写的 Plan/Lane 依赖闭包，则以 `SCHEDULE_EDIT_CONFLICT_IN_BATCH` 整批拒绝，先处理者不回滚。
2. 所有被移动/删除的计划都必须仍为普通 `Editable`，且发行者必须控制其单位。Add/Move 的 RequestedStartTick 不得早于命令 `TargetTick`；null 追加也以 TargetTick 为最早边界，越界以 `START_BEFORE_EDIT_TARGET_TICK` 整批拒绝。成本中性的 Move 与 Remove 不要求原提交窗口仍打开；Add、动作成本增加或任何新增预算占用必须同时提供有效 `ExpectedWindowId`，并重新校验当前窗口、普通动作提交授权与可用预算。
3. 排程器先在内存候选图中应用全部操作，再从每条受影响 Lane 的最后一个不可变前驱开始规范化。Locked/Running/终态计划与固定反应区间都是不可移动障碍；只允许把 Editable 普通计划向右推到首个无重叠位置。只有被本命令直接移动的计划可以早于其原 StartTick，自动连锁绝不把未直接移动的后续计划向左吸附。
4. 新增计划在候选事务中使用临时稳定键，全部验证成功后才分配正式 `ActionPlanId`。成功后 ID 在 Editable、Locked、Running 和历史终态间保持不变，不再复制成另一种执行对象。
5. 对受影响依赖闭包重算绝对 Start/Impact/Active/EndTick、预测起止逻辑格、Move 路径/`ResolvedPathEdgeCount`/`ResolvedPathWeightUnits`、预算预留差额、MovementSegment 和空间 Reservation。与属性有关的 `ResolvedWindupTicks/ResolvedBaseStepTicks/RecoveryTicks` 仍使用计划首次创建时的样本，不因拖动重采样。成本下降释放到该计划的 `SubmittedWindowId` 账本；成本上升只能从同一个仍为当前且开放的来源窗口追加预留，首版禁止把旧计划的预算来源转移或拆分到另一个窗口，否则以 `BUDGET_SOURCE_CLOSED_OR_MISMATCH` 拒绝整批。
6. 任何目标、路径、最大预排 Tick、队列数量、预算或 Reservation 校验失败都回滚当前命令整批，不回滚同一冻结批次中更早已提交的命令。每个成功事务一次写入全部 Plan/Lane/预算/空间变化、发射一个规范化提交事件，并把全局 `ScheduleRevision` 加一；因此一个 Step 有 k 个互不冲突的成功事务时总计 +k。UI 预览必须调用同一纯求值器，不能复制一套近似重排算法后直接写 Logic。

删除的语义是把 Editable ActionPlan 经统一终态协调器转为 `Terminated(CancelledByCommand)`，保留 ID 和历史，而不是从注册表物理抹除。它会释放尚未消费的 TurnBudget 与可替换空间预留；若原窗口已经关闭，只更新该窗口的历史账本，不重新打开提交权限。删除默认保留后续计划当前的绝对 Tick，因此留下空隙；玩家若希望压缩，必须在同一批或后续批次显式移动相关计划。

首版排程拒绝码至少冻结：`STALE_SCHEDULE_REVISION`、`SCHEDULE_EDIT_CONFLICT_IN_BATCH`、`PLAN_NOT_EDITABLE`、`ISSUER_CANNOT_EDIT_PLAN`、`START_BEFORE_EDIT_TARGET_TICK`、`INVALID_INSERTION_ANCHOR`、`SCHEDULE_HORIZON_EXCEEDED`、`DEPENDENCY_RECALC_FAILED`、`BUDGET_SOURCE_CLOSED_OR_MISMATCH`、`INSUFFICIENT_WINDOW_BUDGET` 与 `RESERVATION_CONFLICT`。拒绝事件必须指出失败 OperationOrdinal 与受影响 PlanId（若存在），但不得泄露对手仍为 Editable 的未公开计划内容。

开发者用规范化 `LogicSnapshot` 校验全部权威状态：仍影响未来的计划保留完整字段，已冻结历史由记录数与增量摘要覆盖，完整明细按需从只读归档读取（见 3.11.2），不得逐 Tick 复制全部历史。玩家/AI 使用的 `DecisionSnapshot` 必须按 Controller 过滤。控制者只看到自己可控制单位的 Editable 细节；其他 Controller 的普通计划在锁定并按玩法公开前不得暴露 ActionSpec、目标、Destination 或精确 Tick。ReactionOpportunity 仍是防御者获知威胁的唯一标准接口。

**首版时间线编辑能力取舍：**

| 能力 | 首版 | 规则 |
|---|---|---|
| 指定未来起点 / Lane 尾部追加 | 保留 | RequestedStartTick 为整数 Tick；null 表示本次事务尾部追加 |
| 拖动重定位与顺序调整 | 保留 | 仅 Editable 普通计划；同时提交时间与 InsertBefore 锚点 |
| 删除 | 保留 | RemoveEditablePlan 进入统一终态并释放未消费预算；不物理抹除历史 |
| 整数 Tick / 关键帧吸附 | 保留 | UI 交互辅助；最终整数 Tick 仍由 Logic 校验 |
| 重叠自动重排 | 保留受限版 | 只把受影响 Editable 计划向右 ripple，不移动不可变区间 |
| 到期普通计划被限时状态/空间门禁阻塞 | 系统自动延期 | 使用同一 ScheduleEvaluator 移到 RetryAtTick 并只向右 ripple；成功保持 Editable/Reserved，失败按冻结原因终止 |
| Move 链位置、路径、时长、预算重算 | 保留 | Logic 对依赖闭包原子重算，UI 只展示同源预览 |
| 多项一起编辑 | 保留 | 一个 ScheduleEditCommand 内原子提交，任一失败整批回滚 |
| 删除后自动左吸 / 自动填缝 | 默认不做 | 保留玩家有意留出的空隙；需要压缩时显式移动 |
| 拖动 Block/Dodge 或其 TriggerTick | 禁止 | 固定绑定来源 ImpactTick，反应计划创建后直接 Locked |
| 自动抢占、切断或重排 Locked/Running 计划 | 禁止 | 会改写已经公开/执行的战斗事实 |
| 动作裁剪、时间拉伸、分割与已提交事务的魔法撤销 | 延后 | 与离散动作语义不等价；撤销权威变化必须提交新的、基于最新修订的反向编辑 |

**ActionPlan 与 ActorLane 规则：**

- 每个单位只有一条全局 ActorLane；它跨越所有回合窗口，按当前权威 `StartTick → ActionPlanId` 保存计划，并显式区分可编辑区与不可变区。
- 完成 Tick N 后，UI 把锁定线画在 `N + 1`。这表示“下一次 Step 会先推进状态到期并处理目标 Tick=N+1 的冻结编辑，再评估 StartTick=N+1 的普通计划；只有 Startable 才原子锁定/启动”，而不是在 Tick N 就提前锁死整整一个 Tick。命令批次冻结后才到达的输入只能目标 N+2。锁定线是普通计划的执行截止线，不承诺其右侧全部可编辑；固定反应计划可能在更远未来就已经 Locked。
- 同一单位的普通动作默认串行；固定时机反应和已经 Locked/Running 的区间是不可移动障碍。规范化排程可把 Editable 普通计划向右避让，但不能移动反应、已锁定计划或把未直接编辑的后续计划向左吸附。
- Lane 是否接受新增或编辑由单位存活、控制锁、计划状态、队列上限和最大预排 Tick 共同决定，不由“是不是仍有上个回合动作”简单判断。
- Attack/Guard 的相对时长和 Move 的每权重单位 Tick 在首次创建时量化；Editable 投影可因显式移动、显式编辑引起的向右避让、执行前门禁的系统自动延期或移动链依赖变化而重算绝对 Tick、路径边数、路径权重、预算与 Reservation。Locked 后全部冻结。
- 命令校验失败：不预留/消费预算，不创建或改变 ActionPlan；排程事务任一步失败：回滚本批 Plan/Lane/预算/MovementSegment/Reservation 的所有候选变化，`ScheduleRevision` 不增加。
- Editable 普通计划终止会释放尚未消费的 TurnBudget 预留；Locked 时已经从 Reserved 转为 Spent，之后自然完成、目标失效、Clash、控制或死亡均不退款。反应计划继续遵守肾上腺素的独立周期规则。
- 回合窗口关闭不会自行取消、截断、加速、重排或结算任何 ActionPlan；窗口切换本身也不会增加 `ScheduleRevision`。

```csharp
public sealed class ActorLane
{
    public UnitId UnitId { get; init; }
    public bool IsSubmissionLocked { get; private set; }
    public long NextAvailableTick { get; private set; }

    private readonly SortedDictionary<(long StartTick, ActionPlanId ActionPlanId), ActionPlan>
        _plans = new();

    public bool CanAdd(ActionSpec spec, long targetTick) =>
        !IsSubmissionLocked &&
        _plans.Count < BattleRules.MaxScheduledPlansPerLane &&
        NextAvailableTick <= targetTick + BattleRules.MaxScheduleAheadTicks &&
        spec != null;

    // 只能由 ScheduleEditor 或启动门禁系统自动延期的成功事务整体替换 Editable 投影。
    internal void ReplaceEditableProjection(/* canonical lane projection */) { /* ... */ }

    // 只由 ActionPlanTerminalCoordinator 调用。
    internal void RemoveTerminalPlan(ActionPlan plan)
    {
        _plans.Remove((plan.StartTick, plan.ActionPlanId));
        // 不自动移动后续计划；NextAvailableTick 从仍存在的权威区间重建，
        // 但后续计划的 StartTick 保持不变，因而删除留下可见空隙。
    }

    // 控制效果、死亡或特定玩法可以锁定“继续提交”，
    // 但锁定本身不会清除已经存在的 ActionPlan。
    public void SetSubmissionLocked(bool locked) => IsSubmissionLocked = locked;
}
```

ActorLane 与 ScheduleEditor 共同构成“能否新增、编辑”的权威来源，ActionStartGate 再把 Lane 投影、当前状态、目标和空间事实闭合为“能否锁定/启动”的唯一判定。UnitStateMachine 只描述当前正在执行或承受的状态。`Idle` 不等于 Lane 一定可提交，`Windup/Recovery/Moving` 也不等于 Lane 一定不能接受排在未来的计划。锁定线仍然必要：即使它只领先一个 Tick，也负责标记可变投影将在下一 Step 接受最终门禁、建立回放输入截止点，并禁止同一 Step 的表现回调或迟到输入改写正在执行的动作；它不是额外的长草稿阶段。

#### 3.1.5 统一 ActionPlan 终态与从属对象清理

任何进入权威注册表的 ActionPlan 都可能拥有调度索引、未执行 Intent、MovementSegment、空间/预算/肾上腺素 Reservation、ReactionOpportunity 绑定和 ActorLane 项。自然完成、排程删除、目标失效、来源威胁取消、Clash/Intercept/控制打断、强制位移的旧起点失效/空间预留抢占、Owner 死亡和战斗结束必须调用同一个 `ActionPlanTerminalCoordinator.EnterTerminal(...)`；其他系统不得直接改写计划状态或各自做一部分清理。

协调器按固定参与者顺序同步完成以下操作：

1. 调用 `TryEnterTerminal` 建立唯一终态。若计划已经是终态，立即幂等返回，不重复发事件，也不覆盖第一次确定的原因。
2. 从到期/活动调度索引移除计划，禁止它再启动或产生新 Intent；按规范键删除该计划尚未冻结的本 Tick Intent 和全部未来 Intent。
3. 按 `(ActionPlanId, StepIndex)` 终止并移除活动及未来 MovementSegment，再按稳定空间键释放该计划持有的路径/Dodge 目的格 Reservation。若移动在 `EndTick` 前被打断，单位保留最后一次已提交的逻辑格（通常是当前段的 `From`），不得在原 `EndTick` 再提交到 `To`；若该 Tick 的命令前边界或 Dodge Trigger 已经提交位置，则保留已提交格。视觉插值位置和动画完成不得参与判断。
4. 结算资源所有权：Editable 普通计划释放尚未消费的 TurnBudget 预留；Locked/Running 普通计划的 SpentBudget 不变。反应计划关闭或解绑 Opportunity，并按原肾上腺素规则处理：已 Trigger 或任意普通终态只删除预留且不返还；只有 `SourceThreatCancelled` 且预留 CycleId 仍等于当前 CycleId 时返还 Available，旧周期预留只删除。
5. 从 ActorLane 和所有活动计划索引移除计划，但保留历史注册表、创建/编辑/锁定 Tick、终态、原因和 `TerminalTick`；重建 Lane 尾部索引时不得改变任何后续计划的 StartTick，删除因而留下空隙。
6. 只为第一次终态转换发射一次规范化生命周期事件。清理参与者必须在初始化时显式装配并使用固定顺序，不能依赖事件订阅、反射发现或容器注册顺序。

终态请求先服从 `Step()` 阶段顺序；同一提交阶段内再服从其既有稳定键（Resolution 使用 `ResolutionSequence`，死亡使用 `UnitId → ActionPlanId`，战斗结束使用 `ActionPlanId`）。因此“第一次请求胜出”是确定规则，不依赖回调或容器枚举顺序；后续死亡或 BattleEnded 不覆盖已经确定的终态原因。

本 Tick 已经由 `_intentQueue.DrainOrdered(tick)` 冻结并进入冲突图的 Intent 是本 Tick 的只读求解输入：它先完成当前 Tick 仲裁，随后 Resolution 才可令计划进入终态。协调器只清理未冻结和未来对象，不能追溯删除冲突图输入或撤销已经提交的伤害、状态和移动。

终态清理不是“排程事务失败”的回滚：它不退还 Locked 后已消费的预算或已触发肾上腺素，不回卷或复用任何 ID/Sequence，不撤销已经提交的结果，也不自动压缩 Lane。Editable 普通计划释放的是尚未消费的预算 Reservation，不属于已消费资源退款；反应仍仅保留上述来源威胁取消例外。死亡系统负责 Dead 状态和单位占位移除；终态协调器只负责该单位各计划拥有的从属对象。战斗结束 Finalizer 也必须按 `ActionPlanId` 调用同一协调器，不能另写全局扫描式的计划清理路径。

每个 `Step()` 对外返回前必须满足：活动计划表、Lane、Intent 调度器/队列、MovementSegment、空间 Reservation、Adrenaline Reservation 和机会绑定中都不存在指向终态 ActionPlan 的记录；历史快照中的引用不受此限制。开发与测试构建应以统一不变量检查器扫描并失败，而不是依赖后续 Tick “顺手清掉”。

**设计理由：** 可主动规划的 Attack/Guard/Move 用时间表达占用与成本；Block/Dodge 用肾上腺素表达越过当前窗口的高阶反应，同时仍承担 Lane Windup/Recovery。两类动作共享 ActionPlan 生命周期，但不能混用预算账本。

---

### 3.2 【P1-重要】回合窗口管理系统

#### 3.2.1 TurnWindowManager

```csharp
public sealed class TurnWindow
{
    public WindowId WindowId { get; init; }
    public UnitId OwnerUnitId { get; init; }
    public long OpenedAtTick { get; init; }
    public int TotalBudgetTicks { get; init; }
    public int ReservedBudgetTicks { get; private set; }
    public int SpentBudgetTicks { get; private set; }
    public int AvailableBudgetTicks =>
        TotalBudgetTicks - ReservedBudgetTicks - SpentBudgetTicks;
    public bool IsOpen { get; private set; } = true;
    public bool IsAcceptingSubmissions { get; private set; } = true;
    public TurnWindowCloseReason? CloseReason { get; private set; }

    public bool CanReserve(int cost) =>
        IsOpen && IsAcceptingSubmissions &&
        cost > 0 && AvailableBudgetTicks >= cost;

    internal void ReserveForEditablePlan(int cost)
    {
        if (!CanReserve(cost)) throw new InvalidOperationException();
        ReservedBudgetTicks += cost;
    }

    internal void ConsumeReservationAtLock(int cost)
    {
        ReservedBudgetTicks -= cost;
        SpentBudgetTicks += cost;
    }

    // 只允许 ScheduleEditor 回滚失败事务，或终态协调器释放 Editable 计划。
    internal void ReleaseEditableReservation(int cost) =>
        ReservedBudgetTicks -= cost;

    public void RequestClose(TurnWindowCloseReason reason)
    {
        if (!IsOpen || !IsAcceptingSubmissions) return;
        IsAcceptingSubmissions = false; // 本 Tick 后续命令不能再提交
        CloseReason = reason;
    }

    public void FinalizeClose() => IsOpen = false;
}
```

`TurnWindowManager` 只管理稳定的窗口顺序、提交权限和窗口预算：

- 窗口顺序键为 `PlayerFirst → ActionSpeed 降序 → UnitId`，同速时不得依赖容器遍历顺序。
- 打开窗口时先递增拥有者 `AdrenalineCycleId` 并清零上一个个人周期遗留的 Available；已接受反应的预留继续履约，但之后取消不得返还到新周期。随后创建新的 `WindowId` 和预算；该顺序必须先于本 Tick 新机会与命令处理。
- 普通 ActionPlan 首次进入 Editable 时在来源窗口账本预留预算，锁定时转为 Spent；反应计划不经过该账本。窗口不保存动作、Intent 或空间 Reservation 集合，只保存按 ActionPlanId 可审计的预算预留明细。
- `CloseTurnWindowCommand`、预算耗尽或窗口拥有者死亡会立即停止当前窗口接受本 Tick 后续提交；窗口在本 Tick 末正式关闭，下一个窗口最早在下一 Tick 打开。
- 关闭窗口时只撤销属于该窗口的并发提交授权并发射 `TurnWindowClosedEvent`；不清零肾上腺素。
- 窗口切换不查询任何 ActionPlan 是否完成，也不取消、移动、锁定或结算全局时间轴中的动作。关闭后的窗口账本只为既有 Editable 计划保留到其锁定或终态；预算释放不会重新打开窗口。
- 胜负系统先于新窗口打开运行；战斗结束后不得再创建窗口。当前窗口、并发授权和未来窗口排程由战斗结束 Finalizer 统一关闭；它与 ActionPlan、Intent、MovementSegment、Reservation 和 ActorLane 的终态清理属于同一个固定阶段，但各系统仍只通过自己的生命周期接口修改自身状态。

#### 3.2.2 回合与时间线的关系

回合不停止 `BattleSimulation` 的 Tick。`TurnWindow` 只定义“此刻谁可以提交新动作”以及这些提交共享多少预算，不定义动作归属或执行边界。

**关键约束：** 非窗口拥有者只有在获得明确并发授权时才能创建 Attack/Guard/Move 等普通 ActionPlan；Block/Dodge 则只通过有效 ReactionOpportunity 创建，不检查当前窗口或并发授权。任何 ActionPlan 一旦进入全局时间轴，后续执行都不再检查创建时权限。窗口 A 提交的动作可以在窗口 B、C 期间开始或命中，也可以与这些窗口新提交的动作和高阶反应交互。

```
时间线视角：
  Tick:  0    100   200   300   400   500   600
         [窗口A][窗口B][窗口C][窗口A...]  ← 只控制提交权限
                [主角并发行动激活]
                ├── Guard 0.4s ──┤
                ├── 反击 0.4s ──┤
                           [A 在窗口A提交的重攻击-----------命中]
                                      └─ ReactionOpportunity
                                         [B 的 Block：固定绑定命中 Tick]
                         ↓
                所有 ActionPlan 位于同一连续时间轴
                → 不考虑提交窗口，由统一 Resolution 管线判定交互
```

#### 3.2.3 并发行动系统

```csharp
public sealed class ConcurrentActionSystem
{
    private readonly TurnWindowManager _turnWindowManager;
    private readonly CommandAuthority _commandAuthority;
    private readonly ConcurrentActionDefinition _definition;
    private readonly MetaResourceState _metaResource;
    private readonly UnitId _playerUnitId;
    private readonly LogicEventOutbox _outbox;

    public bool IsActive { get; private set; }
    public WindowId ActiveWindowId { get; private set; }

    public CommandResult TryActivate(
        ControllerId issuer,
        UnitId playerUnitId,
        WindowId expectedWindowId,
        long tick)
    {
        TurnWindow window = _turnWindowManager.Current;
        if (window == null || !window.IsOpen || !window.IsAcceptingSubmissions)
            return CommandResult.Reject("NO_OPEN_WINDOW");
        if (window.WindowId != expectedWindowId)
            return CommandResult.Reject("STALE_OR_CLOSED_WINDOW");
        if (!_commandAuthority.CanControl(issuer, playerUnitId))
            return CommandResult.Reject("ISSUER_CANNOT_CONTROL_UNIT");
        if (playerUnitId != _playerUnitId)
            return CommandResult.Reject("INVALID_CONCURRENT_ACTOR");
        if (IsActive) return CommandResult.Reject("ALREADY_ACTIVE");
        if (window.OwnerUnitId == playerUnitId)
            return CommandResult.Reject("PLAYER_OWNS_WINDOW");
        if (!_metaResource.TrySpend(_definition.MetaResourceCost))
            return CommandResult.Reject("INSUFFICIENT_META_RESOURCE");

        IsActive = true;
        ActiveWindowId = window.WindowId;
        _outbox.Emit(tick, sequence => new ConcurrentActionActivatedEvent(
            tick, sequence, window.WindowId, playerUnitId, window.OwnerUnitId));
        return CommandResult.Accept();
    }

    public bool GrantsSubmission(UnitId unitId, ActionType type, WindowId windowId) =>
        IsActive && ActiveWindowId == windowId &&
        unitId == _playerUnitId &&
        type is ActionType.Attack or ActionType.Guard or ActionType.Move;

    public void OnWindowClosed(WindowId windowId, long tick)
    {
        if (!IsActive || ActiveWindowId != windowId) return;
        IsActive = false;
        _outbox.Emit(tick, sequence => new ConcurrentActionDeactivatedEvent(
            tick, sequence, windowId, _playerUnitId));
    }
}
```

`ConcurrentActionSystem` 只授予当前窗口内新增 Attack/Guard/Move 或增加其预算占用的权限，不直接扣除时间预算或调用调度器。激活时必须先验证发行者可控制该主角且 `expectedWindowId` 等于当前窗口；能力费用由 `ConcurrentActionDefinition` 或等价权威规则配置读取，命令不得携带费用。普通动作与并发动作必须走同一个 `ScheduleEditor.TryApply()`，这样窗口预算、ActorLane、拒绝原因和回放记录只有一套实现。窗口关闭后已提交的主角 ActionPlan 继续存在；其 Move/Remove 只要求控制权与 Editable 状态，新增预算则仍需当时有效的普通动作授权。Block/Dodge 不通过该授权系统，也不能借此绕过 ReactionOpportunity。

局外资源如果会影响本场战斗，必须在战斗初始化时以数值快照注入逻辑世界；当前值与消费结果进入规范化快照、哈希和语义事件，由 Tick 0 重演校验，不能在逻辑层直接调用外部存档或平台接口。

#### 3.2.4 通用反应机会系统

`ReactionOpportunitySystem` 与 `ConcurrentActionSystem` 是两条正交权限：

- `ConcurrentActionSystem` 回答“该主角能否在别人的窗口提交普通动作”。
- `ReactionOpportunitySystem` 回答“该单位能否针对一条已经公开的具体威胁提交一个高阶反应”。

后者不得保存 `_playerUnitId`，也不得读取 `IsPlayerControlled`。它按 `SourceAttackPlanId → DefenderUnitId → ReactionOpportunityId` 稳定顺序创建、更新和关闭机会，并调用统一 `ReactionPlanner.TryAcceptReaction()`。`ActionSetDefinition` 决定单位拥有哪些 Block/Dodge 规格；ControllerBinding 决定谁能替单位作选择；AI 和玩家的差异只存在于选择策略，不存在于规则权限。

反应计划仍使用全局 `ActionPlanId`、ActorLane、状态机、Reservation、Intent、终态协调器和事件 Outbox。不得为反应另建即时回调、协程或在攻击结算时临时扣资源；否则同 Tick 多攻击、回放顺序和 AI/玩家公平性都会失去单一事实来源。

---

### 3.3 【P0-关键】逻辑层表现调用事件化

#### 3.3.1 建立 LogicEventOutbox（不可变事件批次）

```csharp
public abstract record LogicEvent(long Tick, long Sequence);

public sealed record DamageChannelResolution(
    DamageChannelId ChannelId,
    float RawAmount,
    float AfterPassiveResistance,
    float FinalAmount,
    ActionPlanId? MitigatingDefensePlanId);

public sealed record DamageResolvedEvent(
    long Tick, long Sequence,
    long ConflictGroupKey,
    UnitId SourceUnitId, UnitId TargetUnitId,
    ActionPlanId ActionPlanId, ActionSpecId ActionSpecId,
    IReadOnlyList<DamageChannelResolution> Channels,
    float TotalDamage, bool IsCritical,
    int IncomingMomentumUnits, int FinalMomentumUnits,
    GridPoint TargetCell) : LogicEvent(Tick, Sequence);

public sealed record InteractionResolvedEvent(
    long Tick, long Sequence,
    long ConflictGroupKey, ContactKey ContactKey,
    InteractionType Type,
    ActionPlanId AttackerPlanId, ActionPlanId DefenderPlanId,
    UnitId AttackerUnitId, UnitId DefenderUnitId)
    : LogicEvent(Tick, Sequence);

public enum DefenseMitigationOutcome
{
    GuardReduced,
    GuardIneffective,
    Blocked,
    PartiallyBlocked,
    BlockIneffective
}

public sealed record DefenseMitigationResolvedEvent(
    long Tick, long Sequence,
    long ConflictGroupKey,
    ContactKey ContactKey,
    ActionType DefenseType, // Guard 或 Block
    UnitId DefenderUnitId,
    ActionPlanId DefenderPlanId,
    IReadOnlyList<DamageChannelResolution> Channels,
    int IncomingMomentumUnits,
    int FinalMomentumUnits,
    DefenseMitigationOutcome Outcome) : LogicEvent(Tick, Sequence);

public sealed record DodgeResolvedEvent(
    long Tick, long Sequence,
    long? ConflictGroupKey, // 没有接触时为空；每个反应计划只发一次
    ReactionOpportunityId ReactionOpportunityId,
    UnitId DefenderUnitId, ActionPlanId DefenderPlanId,
    GridPoint FromCell, GridPoint DestinationCell,
    bool DestinationCommitted,
    string? FailureReason, // 提交成功时为空；失败使用稳定原因码
    IReadOnlyList<ActionPlanId> AvoidedAttackerPlanIds,
    IReadOnlyList<ActionPlanId> StillHittingAttackerPlanIds,
    IReadOnlyList<ActionPlanId> RetainedUndodgeableAttackerPlanIds, // 最终仍命中集合的子集
    ActionPlanId? CounterTargetPlanId) : LogicEvent(Tick, Sequence);

public sealed record ReactionOpportunityOpenedEvent(
    long Tick, long Sequence,
    ReactionOpportunityId ReactionOpportunityId,
    ActionPlanId SourceAttackPlanId,
    UnitId DefenderUnitId,
    long TriggerTick,
    IReadOnlyList<ReactionOption> Options) : LogicEvent(Tick, Sequence);

public sealed record ReactionOpportunityClosedEvent(
    long Tick, long Sequence,
    ReactionOpportunityId ReactionOpportunityId,
    ReactionOpportunityCloseReason Reason) : LogicEvent(Tick, Sequence);

public sealed record ReactionOptionExpiredEvent(
    long Tick, long Sequence,
    ReactionOpportunityId ReactionOpportunityId,
    ActionSpecId ReactionActionSpecId,
    long ResponseDeadlineTick) : LogicEvent(Tick, Sequence);

public sealed record ReactionAcceptedEvent(
    long Tick, long Sequence,
    ReactionOpportunityId ReactionOpportunityId,
    ActionPlanId ReactionPlanId,
    int ReservedAdrenaline) : LogicEvent(Tick, Sequence);

public sealed record ReactionTriggeredEvent(
    long Tick, long Sequence,
    ReactionOpportunityId ReactionOpportunityId,
    ActionPlanId ReactionPlanId,
    ActionType ReactionType,
    GridPoint? DodgeDestination) : LogicEvent(Tick, Sequence);

public sealed record AdrenalineLedgerChangedEvent(
    long Tick, long Sequence,
    UnitId UnitId,
    long CycleId,
    AdrenalineChangeKind ChangeKind, // Accrued/Reserved/Consumed/Refunded/CycleReset/BattleEndCleared
    int AvailableBefore,
    int AvailableAfter,
    ActionPlanId? ReservationPlanId,
    int ReservationAmount) : LogicEvent(Tick, Sequence);

public sealed record TargetImpactAggregatedEvent(
    long Tick, long Sequence,
    long ConflictGroupKey, UnitId TargetUnitId,
    IReadOnlyList<ActionPlanId> SourcePlanIds,
    IReadOnlyList<DamageChannelResolution> Channels,
    float TotalDamage, long TotalImpactUnits,
    GridDirection? ResultantDirection,
    long ResultantMomentumUnits) : LogicEvent(Tick, Sequence);

public sealed record StateChangedEvent(
    long Tick, long Sequence,
    UnitId UnitId, UnitState From, UnitState To)
    : LogicEvent(Tick, Sequence);

public sealed record ActionPlanLockedEvent(
    long Tick, long Sequence,
    ActionPlanId ActionPlanId,
    UnitId OwnerUnitId,
    long ScheduleRevision)
    : LogicEvent(Tick, Sequence);

public sealed record ActionPlanAutoDeferredEvent(
    long Tick, long Sequence,
    ActionPlanId ActionPlanId,
    StartBlockerReason BlockerReason,
    long PreviousStartTick,
    long RetryAtTick,
    long NewStartTick,
    int AutomaticDeferralCount,
    IReadOnlyList<ActionPlanId> RippledPlanIds,
    long ScheduleRevision)
    : LogicEvent(Tick, Sequence);

public sealed record ActionPlanTerminalEvent(
    long Tick, long Sequence,
    ActionPlanId ActionPlanId, UnitId OwnerUnitId,
    ActionPlanState TerminalState,
    ActionTerminationReason? Reason)
    : LogicEvent(Tick, Sequence);

public sealed record MovementStartedEvent(
    long Tick, long Sequence,
    UnitId UnitId, GridPoint From, GridPoint To,
    long StartTick, long EndTick)
    : LogicEvent(Tick, Sequence);

public sealed record ForcedDisplacementResolvedEvent(
    long Tick, long Sequence,
    UnitId TargetUnitId,
    GridPoint From, GridPoint To,
    GridDirection Direction,
    int RequestedSteps, int AppliedSteps,
    long MomentumUnits, long ConflictGroupKey,
    ForcedDisplacementStopReason StopReason,
    IReadOnlyList<ActionPlanId> InvalidatedPlanIds)
    : LogicEvent(Tick, Sequence);

public sealed record TurnWindowOpenedEvent(
    long Tick, long Sequence, WindowId WindowId, UnitId OwnerUnitId, int BudgetTicks)
    : LogicEvent(Tick, Sequence);

public sealed record TurnWindowClosedEvent(
    long Tick, long Sequence, WindowId WindowId,
    UnitId OwnerUnitId, TurnWindowCloseReason Reason)
    : LogicEvent(Tick, Sequence);

public sealed record ConcurrentActionActivatedEvent(
    long Tick, long Sequence, WindowId WindowId,
    UnitId PlayerUnitId, UnitId WindowOwnerUnitId)
    : LogicEvent(Tick, Sequence);

public sealed record ConcurrentActionDeactivatedEvent(
    long Tick, long Sequence, WindowId WindowId, UnitId PlayerUnitId)
    : LogicEvent(Tick, Sequence);

public sealed record BattleEndedEvent(
    long Tick, long Sequence, string ResultCode)
    : LogicEvent(Tick, Sequence);

public sealed record CommandRejectedEvent(
    long Tick, long Sequence,
    long CommandSequence, string ReasonCode)
    : LogicEvent(Tick, Sequence);

public sealed record EventBatch(
    long Tick,
    IReadOnlyList<LogicEvent> Events);

public sealed class LogicEventOutbox
{
    private readonly List<LogicEvent> _current = new();
    private long _nextEventSequence = 1; // 整场战斗单调递增，永不按 Tick 清零
    public long NextEventSequence => _nextEventSequence;

    public void Emit(long tick, Func<long, LogicEvent> factory)
        => _current.Add(factory(_nextEventSequence++));

    public EventBatch Flush(long tick)
    {
        LogicEvent[] events = _current
            .OrderBy(e => e.Sequence)
            .ToArray();
        _current.Clear();
        return new EventBatch(tick, events); // 空 Tick 也必须返回批次
    }
}
```

事件只携带不可变值和稳定 ID，不持有 `CombatUnitLogic`、MonoBehaviour 或 ScriptableObject 引用。表现层在 `Step()` 完成后消费整个 `EventBatch`；事件消费期间不得重入逻辑模拟。

#### 3.3.2 逻辑层所有表现调用改为事件发射

**改造前（PhysicsEngine）：**
```csharp
if (GameFeelManager.Instance != null)
{
    GameFeelManager.Instance.ShowDamageNumber(target.transform.position, damage, isBigHit);
    GameFeelManager.Instance.ScreenShake(intensity, 0.3f);
    GameFeelManager.Instance.HitStop(0.1f);
}
var targetBounce = target.GetComponent<UnitBounce>();
if (targetBounce != null) targetBounce.OnImpact(intensity);
```

**改造后（ResolutionCommitter 提交单条伤害接触后）：**
```csharp
_outbox.Emit(tick, sequence => new DamageResolvedEvent(
    tick, sequence,
    resolution.ConflictGroupKey,
    attacker.UnitId, target.UnitId,
    actionPlan.ActionPlanId, action.ActionSpecId,
    resolution.ChannelResults,
    resolution.TotalDamage,
    isCritical,
    resolution.IncomingMomentumUnits,
    resolution.FinalMomentumUnits,
    target.GridPosition));
```

表现层的 `CombatFeedbackMapper` 根据 `DamageResolvedEvent` 决定伤害数字、动画、震屏和顿帧。修改表现参数不需要重新编译或修改逻辑程序集。

**改造前（CombatArbiter）：**
```csharp
if (GameFeelManager.Instance)
{
    GameFeelManager.Instance.HitStop(0.1f);
    GameFeelManager.Instance.ShowStatusText(pDef.Owner.transform.position, "PARRY", Color.cyan);
}
```

**改造后（ResolutionCommitter；CombatArbiter 本身保持纯计算）：**
```csharp
_outbox.Emit(tick, sequence => new InteractionResolvedEvent(
    tick, sequence,
    resolution.ConflictGroupKey, resolution.ContactKey,
    InteractionType.Block,
    attackerPlan.ActionPlanId, defenderPlan.ActionPlanId,
    attacker.UnitId, defender.UnitId));
```

逻辑层不再发出 `ScreenShakeRequested`、`HitStopTriggered` 或 `SlowMotionTriggered`。若某种“慢动作”会改变战斗 Tick 推进，它就不是表现效果，必须作为显式战斗规则单独设计；首版慢动作仅改变视觉播放速度，不改变模拟时钟。

#### 3.3.3 消除 AttackIntent 中的视觉位置依赖

**问题代码：**
```csharp
// AttackIntent.ExecuteSuccess() 中
var movement = target.GetComponent<UnitMovement>();
if (movement != null && movement.IsMoving)
{
    var visualGrid = GridManager.Instance.WorldToGrid(target.transform.position);  // ← 读 Transform
    var visualOcc = target.GetProjectedOccupancy(visualGrid, target.FacingDirection);
    // 用视觉位置做命中判定 ← 这是根本性错误
}
```

**修正方案：** 命中判定必须基于逻辑时间点的确定状态，并显式建模移动段，而不是回读视觉插值位置。

```csharp
public sealed record MovementSegment(
    ActionPlanId ActionPlanId,
    int StepIndex,
    UnitId UnitId,
    GridPoint From,
    GridPoint To,
    GridDirection Direction,
    int StepWeightUnits,
    long StartTick,
    long EndTick);

public IReadOnlyList<UnitId> QueryAttackTargets(
    AttackPatternSpec pattern,
    UnitId attackerId,
    GridPoint attackerPosition,
    GridDirection attackerFacing,
    long impactTick)
{
    DirectionalTriangleSet relative =
        pattern.Directions[(int)attackerFacing]; // O(1) 纯整数查表，不旋转
    IReadOnlyList<TrianglePoint> area =
        TranslateToLogicPosition(relative.Triangles, attackerPosition);
    return _logicGrid.GetUnitsInAreaOrdered(area, ignoreUnitId: attackerId);
}
```

首版采用离散提交语义：`StartTick <= tick < EndTick` 时 Occupancy 仍在 `From`，`To` 保持 Reservation；到 `EndTick` 时原子提交到 `To`。`CombatArbiter` 可以读取活跃 `MovementSegment`，按攻击区域是否覆盖 `From/To` 判定 Escape 或 Intercept。这样无需连续子格坐标，也能覆盖移动窗口中途发生的攻击。

同一路径包含多个 step 时，每一步都有独立 `MovementSegment`。首版不为 Reservation 建立独立的抢占排序：本 Tick 的冻结请求先由 Logic 网关按派生的 `SourcePriority → ControllerId（StringComparer.Ordinal）→ ProducerOrdinal` 规范化，拒绝重复或回退的生产者序号，再分配全局唯一 `CommandSequence`；处理器只按该序号依次执行完整的 ScheduleEdit/反应接受事务。先成功提交的命令取得路径 Reservation，后续冲突命令稳定拒绝，不得抢占或回滚此前已经提交的计划。失败只回滚当前命令事务内的预算、Lane、ActionPlan、Intent 和 Reservation 候选变化。只要可信来源事实和请求内容保持不变，改变输入集合的原始枚举顺序不得改变赢家、拒绝结果、事件或快照。

普通 Move 的每个 Segment 使用计划首次创建时固定的 `ResolvedBaseStepTicks`，其持续时间严格为 `StepWeightUnits * ResolvedBaseStepTicks`，不得根据 Transform 距离或动画时长重算；但 Editable 移动链的预测起点变化时，必须由权威排程事务重算路径边数、路径权重和 Segment/Reservation 投影，锁定后固定。Dodge 不生成一串加速 MovementSegment：反应接受事务只为固定 TriggerTick 预留一个满足 Dodge Pattern/最大距离的 Destination；到 TriggerTick，在任何 Dodge 换位前冻结旧格接触，再在冲突图构建前原子把 Occupancy 从 From 提交到 Destination 并释放预留。攻击同时消费旧接触和最终新格接触：普通接触按新格复核，Undodgeable 保留旧格已成立的接触；按 ContactKey 去重后求解。提交失败则单位留在原格并让反应以 `TargetInvalid` 终止，不得用 Dodging 状态补偿为免伤。

---

### 3.4 【P0-关键】所有逻辑状态迁移到 Tick 系统

#### 3.4.0 `Update()` 的保留边界

`Update()` / `LateUpdate()` 仍可用于以下职责：

- 输入设备采样：读取鼠标、键盘、手柄，生成 `CommandRequest` 并提交到已注册的 `CommandIngress`
- UI 刷新：显示时间线、按钮状态、暂停菜单
- 视觉插值：根据逻辑状态和 `VisualTime` 平滑移动模型
- 相机、特效、音效、伤害数字、震屏、顿帧等表现反馈
- 由唯一的 `BattleRuntimeBootstrap` 采样帧时间并调用当前模式的适配器；New 模式下才用 `Time.unscaledDeltaTime` 累积待执行 Tick 并调用纯逻辑 `BattleSimulation.Step()`

禁止在 `Update()` / `LateUpdate()` 中执行以下职责：

- 修改单位血量、资源、状态、位置、朝向
- 执行命中、格挡、闪避、碰撞、胜负判定
- 调度或取消会影响战斗结果的 Intent
- 让 AI 基于 `Time.time` / `Time.unscaledTime` 做决策

**判断标准：** 如果一段代码会改变回放结果，它必须进入 `BattleSimulation.Step()`；如果只改变玩家看到的表现，它可以留在 Unity 生命周期中。

`BattleRuntimeBootstrap` 是场景中唯一通过 `Update()` 选择并推进顶层战斗时钟的组件，具体模式契约见 `2.1.1`。`UnityBattleDriver` 只是被 Bootstrap 调用的 New 模式适配器，自身不得实现第二个顶层推进 `Update()`；它维护独立逻辑累加器。`Time.timeScale`、视觉顿帧和动画慢放不得改变 Tick 数量；用户暂停由驱动器在 Tick 边界停止调用 `Step()`。暂停期间可以操作菜单，但输入层不得生成或积压战斗命令；恢复后从下一个正常 Tick 继续。

#### 3.4.1 CombatUnit 的资源管理移出 Update()

**改造前：**
```csharp
private void Update()
{
    float dt = Time.deltaTime;
    // 肾上腺素衰减    ← 设计变更：不再衰减
    // 体力恢复         ← 设计变更：移除体力
    CurrentAdrenaline -= 5f * dt;
    CurrentStamina += regenRate * dt;
}
```

**改造后：**
```csharp
// CombatUnitLogic — 纯 C# 类，不再继承 MonoBehaviour
public class CombatUnitLogic
{
    public UnitStats Stats;
    public UnitStateMachine StateMachine;

    // 肾上腺素不再有每 Tick 衰减
    // 时间预算由当前 TurnWindow 管理，不存放在单位上
    // 自己的下一回合窗口打开时，由 TurnWindowManager 清零上一个人周期的肾上腺素

    public void OnTick(long tick)
    {
        // 目前无需每 Tick 更新（资源不再自动恢复）
        // 保留此方法作为扩展点：
        // - Buff/Debuff 的 Tick 效果（由 BuffSystem 处理）
        // - 状态机的持续时间检查
        StateMachine.Advance(tick);
    }
}
```

`CombatUnit` 作为过渡期 MonoBehaviour 可以暂时保留注册、绑定和表现桥接职责，但这些职责不得修改 `CombatUnitLogic` 的战斗状态。最终由 `CombatUnitView` 替代它对表现层的引用。

#### 3.4.2 战斗胜利条件检测移入 Tick

**改造前（BattleManager.Update）：**
```csharp
private void Update()
{
    CheckWinCondition(); // 每帧 FindObjectsByType → 极低效
}
```

**改造后：** 作为每个 Tick 提交伤害与死亡之后、回合切换之前的阶段。优先维护按 `FactionId` 分组的存活计数；如果暂时遍历单位，也必须按 `UnitId` 稳定排序。控制者、阵营归属、阵营关系和 Encounter 胜负目标是四个不同概念：胜负只读取单位 FactionId 与 `VictoryDefinition`，不能读取 `FactionDisposition` 来临时组队，也不能读取 `IsPlayerControlled`、Controller 来源或当前窗口拥有者。

```csharp
private void CheckWinCondition(long tick)
{
    VictoryDefinition rule = _encounter.Victory;
    bool alliedAlive = false, hostileAlive = false;

    foreach (var unit in _logicWorld.AllUnitsOrdered)
    {
        if (unit.Stats.CurrentHealth <= 0) continue;
        if (rule.AlliedFactionIds.Contains(unit.FactionId))
            alliedAlive = true;
        if (rule.HostileFactionIds.Contains(unit.FactionId))
            hostileAlive = true;
    }

    if (alliedAlive && hostileAlive) return;

    _pendingBattleResult =
        !alliedAlive && !hostileAlive ? rule.DrawResultCode :
        !alliedAlive                   ? rule.DefeatResultCode :
                                         rule.VictoryResultCode;
}
```

`AlliedFactionIds` / `HostileFactionIds` 在 Builder 中已按 `FactionId` Ordinal 规范化为集合语义，并已验证 Allied 组内部互为 Allied、每个跨组关系为 Hostile；Hostile 组内部仍可按矩阵彼此 Allied/Neutral/Hostile。示例中的 `Contains` 必须由确定性只读集合或等价索引实现，不能依赖输入枚举顺序。未列入两组的目标外阵营不阻止此消灭条件完成；它们仍保留关系矩阵中的 Allied/Neutral/Hostile 关系用于目标资格，不能因不计入胜负就被当作 Neutral。双方在同一 Resolution/死亡阶段全部失去存活单位时必须得到配置的 DrawResultCode，不能用 if 顺序偶然判成胜利或失败。

胜负系统只确定结果，不直接发射 `BattleEndedEvent`。该事件由战斗结束 Finalizer 在全部终态清理完成后最后发射，保证表现层收到结束事件时，最终快照已经不存在仍可执行的计划、Intent、MovementSegment、Reservation 或 Lane 项。

#### 3.4.3 `BattleSimulation.Step()` 完整流程

```csharp
public StepResult Step(long tick, FrozenCommandBatch externalBatch)
{
    // 战斗结束后的误调用是稳定空操作：不验证新 Tick、不处理命令，
    // 不推进 Tick、RNG、ID 或 Sequence，并复用结束 Tick 的最终快照。
    if (_battleEndState.IsEnded)
        return BuildAlreadyEndedResult();

    // 活动战斗仍执行严格 Tick 契约；结束后的幂等分支不采纳传入 Tick。
    if (externalBatch.TargetTick != tick ||
        externalBatch.Requests.Any(r => r.Request.TargetTick != tick))
        throw new InvalidOperationException("Step received a request for another Tick");

    // 玩家与 AI 的请求都必须在上一个 Step 完成后经各自注册入口投递。
    // 本 Step 开始时一次性冻结当前 Tick 批次；此后新请求只能进入未来 Tick。
    IReadOnlyList<SourcedCommandRequest> frozenRequests =
        _commandIngressRegistry.FreezeCurrentTick(externalBatch, tick);
    long batchBaseScheduleRevision = _scheduleEditor.ScheduleRevision;

    // 0. 先提交全局时间轴在本 Tick 到达的前置边界：
    //    移动位置提交、Reservation 释放；自然完成也经统一终态协调器
    _actionLifecycle.ApplyPreCommandBoundariesOrdered(tick);

    // 1. 再推进状态到期与持续效果；单位顺序固定为 UnitId
    foreach (CombatUnitLogic unit in _logicWorld.AllUnitsOrdered)
        unit.Advance(tick);
    _buffSystem.AdvanceAllOrdered(tick);

    // 2. 持续效果可能致死；死亡和胜负必须先于窗口打开和本 Tick 新命令
    _deathSystem.ProcessOrdered(tick);
    _victorySystem.Evaluate(tick);
    if (_victorySystem.HasPendingResult)
    {
        IReadOnlyList<CommandEnvelope> frozenCommands =
            _commandGateway.NormalizeAndAssignSequence(
                frozenRequests, tick);
        _commandProcessor.RejectFrozenBatchOrdered(
            frozenCommands, tick, "BATTLE_ENDED_BEFORE_COMMAND_PHASE");
        return FinalizeBattleEndAndBuildStepResult(tick);
    }

    // 3. 仅在确认战斗尚未结束后打开已排定的新窗口；
    //    若待打开窗口的拥有者已在前述阶段死亡，管理器按稳定顺序跳过该单位；
    //    成功打开前先递增拥有者个人周期并清零 AvailableAdrenaline
    _turnWindowManager.OpenScheduledWindowIfDue(tick);

    // 4. 命令前只刷新此前已经公开的机会和来源失效状态；
    //    新的 Attack 仍可能被本 Tick 排程编辑移动或删除，不能提前 Telegraph。
    //    tick == deadline 的既有选项仍可在本命令阶段最后响应。
    _reactionOpportunities.RefreshExistingBeforeCommandsOrdered(tick);

    // 5. 玩家与 AI 都只处理上一个 Step 后已经投递、此刻已冻结的请求；
    //    网关按同一规范键排序并分配全局唯一 CommandSequence。
    IReadOnlyList<CommandEnvelope> commands =
        _commandGateway.NormalizeAndAssignSequence(
            frozenRequests, tick);

    // 6. ScheduleEditCommand 先校验 ScheduleRevision、控制权及必要的 Window scope，
    //    对 Editable 普通计划执行原子 Add/Move/Remove、向右避让、依赖重算和预算预留；
    //    高阶反应校验 Opportunity scope，预留肾上腺素/目的格并直接创建 Locked 计划。
    //    所有命令仍严格按 CommandSequence 处理；每个排程批次自身原子。
    foreach (CommandEnvelope command in commands)
        _commandProcessor.Process(command, tick, batchBaseScheduleRevision);

    //    本 Tick 命令处理完后，关闭刚到截止点且仍未接受的机会
    _reactionOpportunities.FinalizeDeadlinesOrdered(tick);

    // 7. 执行前启动门禁：按 UnitId/StartTick/ActionPlanId 检查 StartTick == tick
    //    的 Editable 普通计划。Startable 才原子冻结 Timing/Path/Reservation、
    //    TurnBudget Reserved -> Spent、Locked -> Running 并发出锁定事件。
    //    有有限 RetryAtTick 的状态/空间阻塞通过同一 ScheduleEvaluator 做系统自动延期，
    //    只向右 ripple Editable 依赖闭包并令 ScheduleRevision 恰好 +1；
    //    不能延期者在 Editable 阶段经统一终态入口结束并释放未消费预留。
    //    任一内部提交不一致明确失败，不允许留下 Locked-but-not-Running 中间态。
    //    随后启动本 Tick 到期且仍合法的固定 Locked 反应计划；反应绝不自动延期。
    //    应用全部动作阶段边界：
    //    攻击在 StartTick 进入 Windup，并在此时公开 Telegraph/ReactionOpportunity；
    //    在 ImpactTick 进入 Recovery 并产生一次 AttackIntent；
    //    Guard 进入/离开 Active；Block/Dodge 在绑定的 TriggerTick 产生一次反应 Intent；
    //    不检查该计划最初由哪个窗口提交，也不再次扣预算
    _actionLifecycle.EvaluateStartGatesAndCommitDuePlansOrdered(tick);
    _reactionOpportunities.OpenForNewTelegraphsOrdered(tick);

    // 8. 取出所有全局 ActionPlan 在本 Tick 产生的交互 Intent；
    //    排序键包含 Tick/Priority/IntentSequence
    IReadOnlyList<CombatIntent> intents = _intentQueue.DrainOrdered(tick);

    // 9. 在任何 Dodge 换位前，先从统一只读空间快照与冻结 Intent 捕获旧接触；
    //    再按稳定键预检本 Tick 到期 Dodge；仅实际换位成功时，在同一事务中
    //    终止依赖旧起点的 Editable Move 闭包、释放预留并提交目的格；失败不清理移动。
    //    记录旧格、新格、机会/计划 ID、失效移动 PlanId、按窗口释放额和失败原因，
    //    使后续构图合并冻结旧接触与全部提交后的新接触；提交器内部不得逐 Dodge 重采样旧世界
    IReadOnlyList<DodgeTriggerCommit> dodgeCommits =
        _dodgeTriggerCommitter.CommitDueOrdered(intents, tick);

    // 10. 从 Dodge 提交后的只读快照建立规范化冲突图；
    //     Dodge 提交记录参与旧/新接触复核、Undodgeable 保留及 ContactKey 去重；
    //     其余查询/求解不修改世界；
    //     按连通分量同时计算接触级、动作级和目标聚合结果
    ConflictGraph conflictGraph =
        _conflictGraphBuilder.Build(intents, dodgeCommits, _logicWorld, tick);
    IReadOnlyList<CombatResolution> resolutions =
        _combatArbiter.ResolveAll(conflictGraph, _logicWorld, tick);

    // 11. Dodge 位置不再重复提交。先按稳定 ResolutionSequence 提交伤害并完成
    //     本 Tick 全部目标的合力聚合，再跨所有冲突组生成每单位唯一的强制位移请求。
    //     不能在单个冲突组、单个目标或单条接触提交时立即改写位置。
    IReadOnlyList<ForcedDisplacementRequest> displacementRequests =
        _resolutionCommitter.CommitDamageAndBuildForcedDisplacementRequestsOrdered(
            resolutions, tick);

    // 12. 强制位移只读取不可变网格快照，在临时占位空间逐格同时求解完整距离；
    //     不调用 Pathfinder、普通 Move、MovementSegment 或 Reservation 仲裁器。
    //     输入排序只规范输出，不能决定同落点争抢、交换或依赖循环的赢家。
    ForcedDisplacementBatch displacementBatch =
        _forcedDisplacementSolver.ResolveAll(
            displacementRequests,
            _logicGrid.CaptureImmutableSnapshot(),
            tick);

    // 13. 先按 ActionPlanId 终止基于旧起点或被最终 footprint 抢占 Reservation
    //     的移动计划，并由统一终态协调器释放全部未来 Segment/Reservation；
    //     再通过网格原语一次性验证、统一移除旧 footprint、统一写入新 footprint。
    //     任一批量验证失败都是 InvariantViolation，禁止退化为逐单位提交。
    _forcedDisplacementCommitter.TerminateInvalidatedMovementPlansOrdered(
        displacementBatch, tick);
    _logicGrid.ApplyBatchRelocation(displacementBatch.Relocations);
    _forcedDisplacementCommitter.EmitResolvedEventsOrdered(
        displacementBatch, tick);

    // 14. 批量位置已经提交后，再按稳定键提交状态、控制与其余动作终态请求。
    //     Block/Guard 的完全/部分抵抗已由求解结果固定；已冻结并进入冲突图的
    //     本 Tick Intent 已完成求解，所有终态清理只影响未冻结/未来对象。
    _resolutionCommitter.CommitStateControlAndRemainingTerminalsOrdered(
        resolutions, tick);

    //     按 UnitId 把规范聚合的最终伤害/Clash/成功反应事实交给账本，
    //     一次性量化奖励并裁到 Available 上限。
    _adrenalineLedgers.ApplyAccrualFactsOrdered(
        _resolutionCommitter.BuildAdrenalineAccrualFacts(), tick);
    _actionLifecycle.ApplyPostResolutionBoundariesOrdered(tick);

    // 15. 强制位移最终位置、位移事件、状态与控制都已提交后才处理死亡；
    //     按 UnitId、ActionPlanId 将死者全部非终态计划
    //     通过同一协调器终止（包括 Editable、Locked 与 Running），与窗口切换无关
    _deathSystem.ProcessOrdered(tick);

    // 16. 胜负先于窗口推进；若本 Tick 结束战斗，先完成全局终态清理
    _victorySystem.Evaluate(tick);
    if (_victorySystem.HasPendingResult)
        return FinalizeBattleEndAndBuildStepResult(tick);

    // 17. 战斗仍继续时，正式关闭已请求关闭的窗口并排定下一窗口
    _turnWindowManager.FinalizeRequestedClose(tick, scheduleNext: true);

    // 18. AI 读取与本 Step 对玩家公开的同一只读决策快照，
    //     只经注册入口把请求投递到 tick + 1；它不能在当前 Tick 回写或抢跑。
    DecisionSnapshot decisionSnapshot =
        _snapshotBuilder.BuildPublicDecisionSnapshot(tick);
    _aiSystem.ObserveAndEnqueueNextTickOrdered(
        decisionSnapshot, checked(tick + 1));

    return BuildStepResult(tick);
}

private StepResult FinalizeBattleEndAndBuildStepResult(long tick)
{
    // 只执行一次，并使用固定顺序：
    // 1. 按 UnitId 锁定全部 ActorLane，禁止新提交；
    // 2. 按 ActionPlanId 对全部非终态计划调用同一个终态协调器，
    //    以 BattleEnded 原因清理其从属对象并保留只读历史；
    // 3. 验证不存在指向终态计划的活动 Intent、MovementSegment、
    //    Reservation、Lane 项或调度索引，不另建第二套计划清理路径；
    // 4. 清空未来命令 Tick 桶，关闭全部 ReactionOpportunity，
    //    清理肾上腺素/目的格预留，关闭当前窗口、撤销并发授权并清除未来窗口排程；
    // 5. 最后发射唯一一次 BattleEndedEvent。
    _battleEndFinalizer.FinalizeOrdered(
        tick, _victorySystem.ResultCode, ActionTerminationReason.BattleEnded);

    StepResult result = BuildStepResult(tick, StepStatus.BattleEnded);
    _battleEndState.StoreFinalResult(tick, result.Snapshot);
    return result;
}

private StepResult BuildAlreadyEndedResult()
{
    return new StepResult(
        StepStatus.AlreadyEnded,
        EventBatch.Empty(_battleEndState.EndedAtTick),
        _battleEndState.FinalSnapshot);
}

private StepResult BuildStepResult(
    long tick, StepStatus status = StepStatus.Advanced)
{
    // 只读验证：任何活动索引都不得再引用终态 ActionPlan。
    _stepInvariantChecker.AssertAll(tick);

    // 清理及不变量检查完成后，仅归档本 Tick 新冻结且不再影响未来的记录。
    // 空归档批次不扫描旧历史、不改变历史摘要；此步骤不发射玩法事件。
    _historyArchive.AppendFinalizedOrdered(tick);

    // 每 Tick 输出完整活动状态 + 历史记录数/摘要，不复制已归档明细。
    EventBatch eventBatch = _outbox.Flush(tick);
    LogicSnapshot snapshot = _snapshotBuilder.BuildCanonical(tick, _historyArchive.Summary);
    return new StepResult(status, eventBatch, snapshot);
}
```

**阶段顺序不变量：** 旧移动段在 `EndTick` 的位置提交发生在新命令校验之前；状态到期、持续效果、死亡和胜负发生在到期窗口打开之前，因此控制恰在 T 到期时，StartTick=T 的计划可在同 Tick 通过门禁；待打开窗口的拥有者若在该阶段死亡，不得产生该单位的 `TurnWindowOpenedEvent`；自身窗口打开时的 Adrenaline Cycle 递增与 Available 清零发生在本 Tick 机会/命令处理之前，既有反应预留不取消；目标 Tick=T 的排程编辑必须在 T 的启动门禁之前完成，批次冻结后到达的输入不得回写本 Tick；门禁系统自动延期发生在显式命令之后、任何普通计划锁定之前，成功事务按稳定键逐个令 ScheduleRevision +1；`tick == 所选选项 ResponseDeadlineTick` 的既有反应仍可在本命令阶段接受，之后该选项立即过期；新的攻击只有原子完成 Locked/预算消费/Running 后才 Telegraph，Tick N 新公开的机会对玩家与 AI 都最早产生 Tick N+1 请求；Dodge Trigger 的位置提交先于同 Tick 攻击空间复核，Block/Guard 抵抗先于目标聚合；目标聚合先产生本 Tick 全局唯一强制位移批次，强制位移求解忽略 Reservation，并在计划失效清理后通过 `ApplyBatchRelocation` 一次提交；状态/控制和死亡只读取该批最终位置，同 Tick 致死单位也先完成位移再由死亡系统移除占位；玩家和 AI 命令使用同一处理路径；所有到期 ActionPlan 都参与门禁或固定反应判定且不按提交窗口过滤；事件只在本 Tick 所有逻辑提交完成后对外可见。

`Step()` 的调用契约要求 `externalBatch` 是 `CommandIngressRegistry` 为当前 Tick 生成的不可伪造冻结批次；调用方不能直接构造其中的来源、发行者或生产者序号。入口层必须对未来 Tick 请求分桶保存，对过期请求生成稳定拒绝结果；严禁静默丢弃请求。开发与测试版本遇到错误 Tick 应立即失败，正式输入入口则应生成稳定的入口拒绝或 `CommandRejectedEvent`。

#### 3.4.4 战斗结束终态契约

战斗结束是全局模拟终止边界，不是普通窗口关闭。检测到胜负的当前 Tick 必须先完成该 Tick 已进入提交阶段的 Resolution，然后由唯一 `BattleEndFinalizer` 完成清理并输出最终事件批次与快照：

1. 所有 ActorLane 按 `UnitId` 锁定；所有非终态 ActionPlan 按 `ActionPlanId` 调用统一 `ActionPlanTerminalCoordinator`，转为 `Terminated(BattleEnded)`。已经因完成、交互或死亡进入终态的计划保持其原终态和原因。
2. 协调器复用普通终态协议：计划记录继续保留在只读历史归档中，最终快照通过历史记录数与摘要覆盖这些记录，诊断时可按该记录数读取对应历史前缀；计划从活动计划、Lane 队列与调度索引移除。计划拥有的未冻结/未来 Intent、MovementSegment、空间/肾上腺素 Reservation 与机会绑定按稳定键清理，不得把视觉插值完成当作前置条件。
3. Finalizer 在计划循环后执行统一闭合性检查；不得再用全局扫描重复改写计划状态或二次发射终态事件。窗口、并发授权和命令 Tick 桶等非计划对象仍由其各自的 Finalizer 钩子清理。
4. 如果战斗在命令阶段前结束，本 Tick 已冻结的外部请求仍先通过网关规范化并分配 `CommandSequence`，再按该序号发出 `BATTLE_ENDED_BEFORE_COMMAND_PHASE` 拒绝事件；重复/回退的 `ProducerOrdinal` 按入口错误稳定拒绝且整组不得产生赢家。未来 Tick 桶中的请求直接清空，不进入回放的已处理命令记录。
5. 当前 TurnWindow 使用 `BattleEnded` 原因关闭；并发授权立即撤销，全部 ReactionOpportunity 关闭，未来窗口排程清空。未触发反应的肾上腺素预留删除且不返还 Available；Editable 普通计划的预算预留随计划清理归零，但战斗已经结束，不重新开放或转移预算，Locked/Running 的已消费预算不退款。
6. `ActionPlanTerminalEvent` 等清理事实按稳定顺序发射，`BattleEndedEvent` 必须是该 Tick 最后一个逻辑事件，并且整场战斗只出现一次。
7. Finalizer 完成后缓存结束 Tick 和规范化最终快照。驱动器应停止推进；若调用方仍误调 `Step()`，返回 `StepStatus.AlreadyEnded + 空 EventBatch + 同一最终快照`，不推进逻辑 Tick、RNG、ID、Sequence，也不处理或记录传入命令。
8. 新命令应在命令入口被稳定拒绝为 `BATTLE_ALREADY_ENDED`，不得进入 Tick 桶或回放命令流。开发版本可以额外断言驱动器误调用，但正式运行语义仍以幂等空操作为准。

这套规则保留了结束瞬间的审计信息，同时保证所有活动索引和空间预留都闭合；它不要求场景或表现立即销毁，因此结束动画仍可继续消费最终事件和快照。

---

### 3.5 【P0-关键】引入单位状态机

状态机必须早于资源模型和回合系统全量迁移。当前 `IsActing`、`InWindup`、`InRecovery`、`IsMoving`、`IsStaggered`、`IsKnockedDown` 等 bool 标志会让 Tick 化和事件化产生大量临时兼容逻辑；先收敛为单一状态枚举，可以显著降低后续改造的返工。

#### 3.5.1 状态定义

```csharp
public enum UnitState
{
    Idle,           // 当前没有执行动作；是否接受新计划由 ActorLane 判断
    Windup,         // 攻击前摇
    Recovery,       // 攻击后摇
    Moving,         // 移动中
    Guarding,       // 普通防御计划中；是否 Active 读取 ActionPlan Tick 区间
    Blocking,       // 高阶格挡反应计划中；只在 TriggerTick 完全抵抗
    Dodging,        // 高阶闪避反应计划中；只在 TriggerTick 原子换格
    Staggered,      // 硬直
    KnockedDown,    // 击倒
    Recovering,     // 起身恢复中
    Dead            // 死亡
}

public sealed record StateTransitionSpec(
    UnitState Target,
    int DurationTicks = 0,
    StateTransitionSpec NextOnExpire = null);

public sealed class UnitStateMachine
{
    private readonly UnitId _unitId;
    private readonly LogicEventOutbox _outbox;

    public UnitState CurrentState { get; private set; } = UnitState.Idle;
    public UnitState PreviousState { get; private set; } = UnitState.Idle;
    public long StateStartTick { get; private set; }
    public long StateEndTick { get; private set; } = long.MaxValue;
    public StateTransitionSpec NextOnExpire { get; private set; }

    public bool CanExecuteImmediately => CurrentState == UnitState.Idle;

    /// <summary>
    /// 单位当前状态是否能承受未经特殊交互消解的直接 Hit。
    /// 该属性不控制 Intent 收集、冲突图连边或 Attack-Dodge / Attack-Block。
    /// </summary>
    public bool CanReceiveDirectHit =>
        CurrentState is UnitState.Idle
                     or UnitState.Windup
                     or UnitState.Recovery
                     or UnitState.Moving
                     or UnitState.Guarding
                     or UnitState.Blocking
                     or UnitState.Dodging;

    public bool TryTransition(StateTransitionSpec transition, long currentTick)
    {
        UnitState newState = transition.Target;
        if (!IsValidTransition(CurrentState, newState))
            return false;

        PreviousState = CurrentState;
        CurrentState = newState;
        StateStartTick = currentTick;
        StateEndTick = transition.DurationTicks > 0
            ? currentTick + transition.DurationTicks
            : long.MaxValue;
        NextOnExpire = transition.DurationTicks > 0
            ? transition.NextOnExpire
            : null;

        UnitState from = PreviousState;
        _outbox.Emit(currentTick, sequence =>
            new StateChangedEvent(currentTick, sequence, _unitId, from, newState));
        return true;
    }

    public void Advance(long currentTick)
    {
        if (StateEndTick == long.MaxValue || currentTick < StateEndTick || NextOnExpire == null)
            return;

        StateTransitionSpec next = NextOnExpire;
        if (!TryTransition(next, currentTick))
            throw new InvalidOperationException(
                $"Invalid timed transition {CurrentState} -> {next.Target}");
    }

    private static bool IsValidTransition(UnitState from, UnitState to)
    {
        if (from == UnitState.Dead || from == to) return false;
        if (to == UnitState.Dead) return true;

        return (from, to) switch
        {
            (UnitState.Idle, UnitState.Windup or UnitState.Moving
                or UnitState.Guarding or UnitState.Blocking or UnitState.Dodging
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            (UnitState.Windup, UnitState.Recovery
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            (UnitState.Recovery, UnitState.Idle
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            (UnitState.Moving, UnitState.Idle
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            (UnitState.Guarding, UnitState.Idle
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            (UnitState.Blocking, UnitState.Idle
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            (UnitState.Dodging, UnitState.Idle
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            (UnitState.Staggered, UnitState.Recovering) => true,
            (UnitState.KnockedDown, UnitState.Recovering) => true,
            (UnitState.Recovering, UnitState.Idle
                or UnitState.Staggered or UnitState.KnockedDown) => true,
            _ => false
        };
    }

    public float GetProgress(long currentTick)
    {
        if (StateEndTick == long.MaxValue || StateStartTick == StateEndTick) return 0f;
        long total = StateEndTick - StateStartTick;
        long elapsed = currentTick - StateStartTick;
        return Math.Clamp((float)elapsed / total, 0f, 1f);
    }
}
```

`Guarding`、`Blocking` 和 `Dodging` 都返回 `CanReceiveDirectHit == true`。状态本身不授予免伤：Guard 必须由对应计划的 Active 区间提供部分抵抗；Block 必须同时匹配 `ReactionOpportunityId`、`TriggerTick` 和 `Blockable` 标签；Dodge 必须先成功提交目的格，并仅以新位置重新判定结果。这样攻击在防御生效前、恢复期或仍覆盖 Dodge 新位置时都能正常命中。首版样例继续让 `Staggered`、`KnockedDown` 和 `Recovering` 不承受尚未消解的直接 Hit；这是独立控制规则，不应被用作防御动作实现。若以后允许倒地追击，应显式修改该策略及回归测试。

#### 3.5.2 改造 StateChangeIntent

```csharp
// Intent 已经持有整数 Tick，不再查找 BattleTimeline 或二次转换秒
public override void ExecuteSuccess()
{
    bool changed = Owner.StateMachine.TryTransition(Transition, CurrentTick);

    if (!changed)
        throw new InvalidOperationException(
            $"Invalid state transition for {Owner.UnitId}: " +
            $"{Owner.StateMachine.CurrentState} -> {Transition.Target}");
}
```

状态事件由状态机内部统一发射，显式 Intent 与自动到期走同一路径。首版攻击链由已解析计划固定为 `Windup [StartTick, ImpactTick) → Recovery [ImpactTick, EndTick) → Idle`：在 `ImpactTick` 的状态推进中进入 `Recovery`，同 Tick 的 `AttackIntent` 仍按计划参与仲裁；不得因为当前状态已是 `Recovery` 而丢弃命中。到期时不再额外排入重复的状态 Intent。`Dead` 是终态，不允许恢复到 `Idle`。首版继续使用单一互斥状态；如果未来设计允许同一单位边移动边攻击，再升级为“行动状态 + 控制状态”的正交状态机，不提前增加复杂度。

---

### 3.6 【P1-重要】输入系统改为命令队列模式

#### 3.6.1 命令定义

```csharp
public enum CommandSourceKind { Player, AI, System }

public abstract record CommandScope;
public sealed record WindowCommandScope(WindowId ExpectedWindowId) : CommandScope;
public sealed record ScheduleEditScope(
    long ExpectedScheduleRevision,
    WindowId? ExpectedWindowId) : CommandScope;
public sealed record ReactionCommandScope(
    ReactionOpportunityId ReactionOpportunityId) : CommandScope;

// UI / AI 唯一可以创建的战斗意图；不包含身份、优先级、序号或规则费用。
public sealed record CommandRequest(
    long TargetTick,
    CommandScope Scope,
    BattleCommand Payload);

// 公开给驱动器的冻结批次是一个不透明值；构造器和可信来源事实只对 Logic 可见。
public sealed class FrozenCommandBatch
{
    public long TargetTick { get; }
    internal IReadOnlyList<SourcedCommandRequest> Requests { get; }
    internal FrozenCommandBatch(
        long targetTick,
        IReadOnlyList<SourcedCommandRequest> requests) { /* ... */ }
}

internal sealed record SourcedCommandRequest(
    ControllerId Issuer,
    CommandSourceKind SourceKind,
    long ProducerOrdinal,
    CommandRequest Request);

// 只由 CommandGateway 创建，生产者无法构造。
internal sealed record CommandEnvelope(
    long TargetTick,
    CommandScope Scope,
    ControllerId Issuer,
    CommandSourceKind SourceKind,
    int SourcePriority,       // 由版本化规则映射派生
    long ProducerOrdinal,     // 由绑定入口分配
    long CommandSequence,     // 由网关整场单调分配
    BattleCommand Payload);

public abstract record BattleCommand;

// 普通与并发动作共用 3.1.2 的 ScheduleEditCommand；其 Operations 可原子
// AddOrdinaryPlan / MoveEditablePlan / RemoveEditablePlan。不存在第二个草稿对象。

// 高阶反应不携带 Tick；Block/Dodge 的 TriggerTick 从机会的来源攻击推导。
public sealed record ReactionCommand(
    UnitId DefenderUnitId,
    ActionSpecId ReactionActionSpecId,
    GridPoint? DodgeDestination) : BattleCommand;

public sealed record CloseTurnWindowCommand(UnitId ActorUnitId) : BattleCommand;
public sealed record ActivateConcurrentCommand(UnitId PlayerUnitId) : BattleCommand;
```

选择单位、取消选择、尚未提交的拖拽轨迹、吸附辅助线和预览失败等只属于 `ProjectHero.UnityView` 输入状态，不进入逻辑命令日志。一次成功提交或被规则拒绝的 `ScheduleEditCommand` 会改变/尝试改变战斗结果，必须像其他命令一样记录；只记录最终 Operations，不记录鼠标移动过程。

设备输入默认投递到 `CurrentTick + 1`。关闭窗口和激活并发仍使用 `WindowCommandScope(ExpectedWindowId)`。普通排程编辑使用 `ScheduleEditScope(ExpectedScheduleRevision, ExpectedWindowId?)`：成本中性的 Move/Remove 依赖计划状态与控制权，Add 或预算增加还要求非空且仍有效、并与预算来源一致的窗口 ID；修订号不匹配返回 `STALE_SCHEDULE_REVISION`。反应请求携带 `ReactionCommandScope(ReactionOpportunityId)`；如果机会已关闭、来源威胁失效或错过所选动作的截止 Tick，分别返回稳定的 `REACTION_OPPORTUNITY_CLOSED`、`SOURCE_THREAT_INVALID` 或 `REACTION_TOO_LATE`。这些 scope 只校验命令采纳时的权限和并发版本，不成为 ActionPlan 生命周期边界。

`CommandIngressRegistry` 是命令信任边界。它根据初始化后的 `ControllerBinding` 为每个玩家或 AI 创建一个注册入口；入口把固定的 `ControllerId`、`CommandSourceKind` 绑定到请求，并分配该入口严格递增的 `ProducerOrdinal`。UI/AI 只能持有提交接口，不能创建 `SourcedCommandRequest` / `CommandEnvelope`，不能注册 `System` 来源，也不能填写 `SourcePriority`、`CommandSequence` 或并发能力费用。

首版版本化映射固定为 `Player = 0`、`AI = 10`、`System = 20`。`System` 请求只能由 Logic 内部入口产生，并使用规则层保留的稳定 Controller 标识（不出现在 Encounter 的外部 `ControllerBinding` 中）；修改映射属于玩法规则变更，必须更新 RulesVersion、定义哈希和 golden snapshot。

`CommandGateway` 在 Tick 冻结后执行以下唯一规范化流程：

1. 验证来源注册、目标 Tick 和该来源的生产者序号状态。
2. 从版本化规则中的 `CommandSourceKind -> SourcePriority` 映射派生优先级。
3. 按 `SourcePriority -> ControllerId（StringComparer.Ordinal）-> ProducerOrdinal` 排序。原始集合枚举顺序不得作为隐式末级键。
4. 同一 `(SourcePriority, ControllerId, ProducerOrdinal)` 出现多次时，以 `DUPLICATE_COMMAND_ORDINAL` 拒绝整个碰撞组，不允许“先枚举者获胜”；低于该入口已冻结水位的序号以 `COMMAND_ORDINAL_REGRESSION` 拒绝。入口级错误使用组级稳定诊断，不依赖重复成员的枚举顺序。
5. 对剩余请求按上述顺序分配整场唯一、单调递增且永不复用的 `CommandSequence`，生成内部 `CommandEnvelope`。处理器之后只按 `CommandSequence` 执行。

入口级拒绝发生在 Envelope 生成前，因此不伪造 `CommandSequence`：网关按规范组键顺序为每个非法键只发出一个 `CommandIngressRejectedEvent`，载荷包含 Controller、SourceKind、ProducerOrdinal、原因和碰撞数量，不选择或回显某个“获胜”Payload。已生成 Envelope 后的规则拒绝才使用 `CommandRejectedEvent(CommandSequence, ReasonCode)`。

`ProducerOrdinal` 的下一个值、已冻结水位、未来 Tick 桶、全局下一个 `CommandSequence` 和 `ScheduleRevision` 都会影响未来结果，必须进入规范化快照与哈希。普通运行时由入口生成序号；重复/回退校验主要用于回放载入验证和损坏输入的闭合失败。首版开发者回放从 Tick 0 重建这些状态，不从中途快照恢复。预算、Lane、ActionPlan、Reservation 等任何竞争资源都不得再使用第二套排序。

#### 3.6.2 命令处理

```csharp
internal sealed class BattleCommandProcessor
{
    public void Process(
        CommandEnvelope envelope,
        long tick,
        long batchBaseScheduleRevision)
    {
        CommandResult result;
        if (!_commandAuthority.CanControlPayload(
                envelope.Issuer, envelope.Payload))
        {
            result = CommandResult.Reject("ISSUER_CANNOT_CONTROL_UNIT");
        }
        else result = envelope.Payload switch
        {
            ScheduleEditCommand edit when
                envelope.Scope is ScheduleEditScope scheduleScope =>
                _scheduleEditor.TryApply(
                    envelope.Issuer, edit, scheduleScope,
                    tick, batchBaseScheduleRevision),

            ReactionCommand reaction when
                envelope.Scope is ReactionCommandScope reactionScope =>
                _reactionPlanner.TryAcceptReaction(
                    reaction,
                    reactionScope.ReactionOpportunityId,
                    tick),

            CloseTurnWindowCommand closeWindow when
                envelope.Scope is WindowCommandScope closeScope =>
                _turnWindowManager.RequestClose(
                    closeScope.ExpectedWindowId,
                    closeWindow.ActorUnitId,
                    TurnWindowCloseReason.Manual),

            ActivateConcurrentCommand activate when
                envelope.Scope is WindowCommandScope concurrentScope =>
                _concurrentAction.TryActivate(
                    envelope.Issuer,
                    activate.PlayerUnitId,
                    concurrentScope.ExpectedWindowId,
                    tick),

            _ => CommandResult.Reject("COMMAND_SCOPE_MISMATCH")
        };

        if (!result.Accepted)
        {
            _outbox.Emit(tick, sequence => new CommandRejectedEvent(
                tick, sequence, envelope.CommandSequence, result.ReasonCode));
        }
    }
}
```

`CommandAuthority` 使用初始化时建立的 `ControllerId -> UnitId 集合` 校验每种载荷涉及的受控单位；载荷中的 `ActorUnitId` / `DefenderUnitId` / `PlayerUnitId` 不是权限证明。普通动作费用、反应肾上腺素费用和并发能力费用都从规则侧定义读取，拒绝事件记录网关生成的 `CommandSequence`。ScheduleEdit 配 Window/Reaction scope、ReactionCommand 配 Schedule/Window scope 或 ReactionCommand 选择非 Block/Dodge 规格都必须稳定拒绝。处理器不使用“持续取队列直到为空”的重入模式；`Step()` 开始时冻结本 Tick 的请求批次，逻辑事件订阅者或表现层随后产生的请求只能进入未来 Tick。

#### 3.6.3 TacticsController 改造

```csharp
// 不再直接设置 IgnoreUnitClicks 布尔标志
// 而是通过输入模式状态机管理

public enum InputMode
{
    Normal,
    TargetSelection,
    TimelinePlacement,
    TimelineReorder,
    ReactionSelection,
    DodgeDestinationSelection
}

public class InputModeManager
{
    private InputMode _currentMode = InputMode.Normal;
    private readonly Stack<InputMode> _modeStack = new();

    public void PushMode(InputMode mode)
    {
        _modeStack.Push(_currentMode);
        _currentMode = mode;
    }

    public void PopMode()
    {
        _currentMode = _modeStack.Count > 0 ? _modeStack.Pop() : InputMode.Normal;
    }

    public InputMode CurrentMode => _currentMode;
}
```

普通时间线不是只读列表。它可基于快照和 Logic 预览 API 提供以下首版操作：在未来 Tick 放置 Attack/Guard/Move、关键帧/整数 Tick 吸附、拖动改变时间与 Lane 顺序、删除 Editable 计划、预览向右避让，以及显示移动链位置/路径/时长/预算的连锁重算。UI 只能把一次确认后的差异编码为原子 `ScheduleEditCommand`；不能直接改集合，也不能把每个鼠标移动写进回放。Locked/Running/终态计划及所有反应计划显示为只读，锁定线在已完成 Tick N 的快照上位于 N+1。

`ReactionSelection` 只能由仍开放的机会事件/快照进入；Block 选定后直接构造 ReactionCommand，Dodge 再进入 `DodgeDestinationSelection` 选择 Logic 已判定可用的离散格。时间线只展示来源 ImpactTick 和各选项截止 Tick，不提供拖拽 BlockTick/EvadeTick 的交互。机会接受、过期或来源取消时，表现层必须退出对应输入模式，但不得自行修改资源、计划或位置。

---

### 3.7 【P1-重要】逻辑世界与视觉世界分离

#### 3.7.1 双层 CombatUnit

```csharp
// 逻辑层 — 纯 C# 数据类
public sealed class CombatUnitLogic
{
    public UnitId UnitId { get; init; }
    public UnitDefinitionId DefinitionId { get; init; }
    public FactionId FactionId { get; init; }
    public GridPoint GridPosition { get; internal set; }
    public GridDirection FacingDirection { get; internal set; }
    public UnitStateMachine StateMachine { get; init; }
    public UnitStats Stats { get; init; }
    public VolumeSpecId VolumeSpecId { get; init; }
    public ActionSetId ActionSetId { get; init; }
}

// 表现层 — MonoBehaviour 桥接
public class CombatUnitView : MonoBehaviour
{
    [SerializeField] private string encounterSlotId;
    public UnitId UnitId { get; private set; }

    private UnitMovement _movement;
    private UnitAnimator _animator;
    private UnitSnapshot _latestSnapshot;

    public void Bind(UnitId unitId, UnitSnapshot initialSnapshot)
    {
        UnitId = unitId;
        _latestSnapshot = initialSnapshot;
        SyncVisualPosition();
    }

    public void Consume(LogicEvent evt)
    {
        switch (evt)
        {
            case MovementStartedEvent move when move.UnitId == UnitId:
                _movement.PlayMove(move.From, move.To, move.StartTick, move.EndTick);
                break;
            case DamageResolvedEvent damage when damage.TargetUnitId == UnitId:
                _animator.PlayHitReaction(damage.FinalMomentumUnits);
                break;
            case StateChangedEvent state when state.UnitId == UnitId:
                _animator.TransitionTo(state.To);
                break;
        }
    }

    public void ApplySnapshot(UnitSnapshot snapshot) => _latestSnapshot = snapshot;

    private void LateUpdate()
    {
        if (_latestSnapshot.State == UnitState.Moving)
            _movement.UpdateVisualInterpolation(_latestSnapshot);
        else
            SyncVisualPosition();
    }
}
```

`CombatUnitView` 的场景配置只保存 `EncounterSlotId`；战斗初始化器通过 Authoring 生成的槽位映射解析出本场 `UnitId` 后调用 `Bind()`。View 只保存解析后的 `UnitId`、事件播放状态和只读快照，不公开可写的 `CombatUnitLogic` 引用，也不把运行时数字 ID 手填进场景。ScriptableObject 仍可作为编辑器配置，但在战斗创建时转换为 `ActionSpec`、`AttackPatternSpec` 和 `VolumeSpec` 后才进入逻辑世界。

#### 3.7.2 消除 FindObjectsByType 和 GetComponent 查询

所有逻辑层的 Unit 引用通过 `LogicWorld` 集中管理：

```csharp
public sealed class LogicWorld
{
    private readonly Dictionary<UnitId, CombatUnitLogic> _units = new();
    private readonly SortedSet<UnitId> _orderedUnitIds = new(
        Comparer<UnitId>.Create((a, b) => a.Value.CompareTo(b.Value)));

    public void RegisterUnit(CombatUnitLogic unit)
    {
        if (_units.ContainsKey(unit.UnitId))
            throw new InvalidOperationException($"Duplicate UnitId: {unit.UnitId}");
        _units[unit.UnitId] = unit;
        _orderedUnitIds.Add(unit.UnitId);
    }

    public void UnregisterUnit(UnitId unitId)
    {
        if (_units.TryGetValue(unitId, out var unit))
        {
            _units.Remove(unitId);
            _orderedUnitIds.Remove(unitId);
        }
    }

    public CombatUnitLogic GetUnit(UnitId id) => _units.GetValueOrDefault(id);

    public IEnumerable<CombatUnitLogic> AllUnitsOrdered =>
        _orderedUnitIds.Select(id => _units[id]);

    public IEnumerable<CombatUnitLogic> AllAliveOrdered =>
        AllUnitsOrdered.Where(u => u.Stats.CurrentHealth > 0);
}
```

Dictionary 只用于按 ID 查询，任何影响逻辑结果或事件顺序的遍历都通过显式有序索引进行。死亡单位可以保留在世界中供回放查询，但从行动授权、占位和存活视图中移除。

---

### 3.8 【P1-重要】网格系统逻辑化

网格逻辑化需要提前到 P1。命中判定、占位、移动 Reservation、AI 寻路、回放快照都依赖同一份确定性网格数据；如果继续让核心逻辑依赖 `GridManager.Instance` 和 Unity 世界坐标，纯逻辑测试与回放会被卡住。

**固定邻居枚举契约：** `LogicGrid.GetNeighborsOrdered(GridPoint origin)` 必须按 `GridDirection` 已冻结的底层整数值升序枚举 12 个候选方向，即 `East(0) → EastNorth(1) → NorthEast(2) → North(3) → NorthWest(4) → WestNorth(5) → West(6) → WestSouth(7) → SouthWest(8) → South(9) → SouthEast(10) → EastSouth(11)`；越界或不合法邻居只从结果中跳过，不改变其余候选的相对顺序。Pathfinder 只能从该序列展开邻居；当同一父节点产生其他条件完全相同的候选时，保留规范邻居顺序中先出现者。不得读取 `Dictionary`、`HashSet`、资产加载或调用方传入集合的枚举顺序作为额外平局键。`GridDirection` 数值及上述顺序属于版本化玩法规则并进入 `BattleDefinitionHash`；修改任一项必须更新 RulesVersion、定义哈希与相应 golden snapshot。

**固定路径成本与搜索契约：** 当前项目已经有 12 向路径成本，不应改写成方格地图的“直行/斜向成本”。首版 `PathCostRules` 沿用旧 `Pathfinder` 的含义：偶数方向 `East/NorthEast/NorthWest/West/SouthWest/SouthEast` 的 `StepWeightUnits = 1`，奇数方向 `EastNorth/North/WestNorth/WestSouth/South/EastSouth` 的 `StepWeightUnits = 2`。因此：

```text
PathWeightUnits(path) = checked(sum(StepWeightUnits(edge.Direction)))
HeuristicWeightUnits(a, b) =
  dy + max(0, (dx - dy) / 2)
where dx = abs(a.X - b.X), dy = abs(a.Y - b.Y)
```

`GridPoint` 使用 doubled coordinates，合法点满足 `X + Y` 为偶数；上式只用非负整数与向下整除，并在无障碍图上按同一 1/2 权重给出下界。坐标减法与绝对值先扩展为 checked `long`，禁止对 `int.MinValue` 直接取绝对值。A* 的 `g/h/f` 全部使用 checked `long`。开放集使用最小且完整的稳定键 `(f, h, GridPoint.X, GridPoint.Y)`，依次按 `f → h → X → Y` 升序取节点；不再引入 `g` 或 `DiscoveryOrdinal/InsertSequence` 作为末级键，因为 `f` 与 `h` 相等时 `g = f - h` 已相等，而坐标字典序已经构成稳定平局规则。同一坐标只有候选 `g` 更小时才更新；候选 `g` 相等时保留已有父节点。规范邻居顺序负责稳定候选产生顺序，开放集坐标键负责稳定跨节点取出顺序。不得使用浮点数、对象哈希、普通容器枚举顺序或 `MoveSpeed` 参与选路。

`PathSearchRules(MaxExpandedNodes, MaxPathWeightUnits, MaxPathEdges)` 属于 `BattleRules`。首版冻结为：

```text
MaxExpandedNodes   = 4096
MaxPathWeightUnits = 256
MaxPathEdges       = 192
```

三个上限统一使用包含式边界：`ExpandedNodes <= 4096`、`PathWeightUnits <= 256`、`PathEdges <= 192` 均合法，只有候选值严格大于对应上限才触发失败。不得用 `<` 提前拒绝恰好位于上限的路径。首版不设置独立 `MaxSearchDistance`；搜索只访问 `EncounterDefinition.GridBoundary` 明确声明的合法点，边界外候选直接过滤，不再沿用“距起点坐标 ±100”的隐式窗口。

检查位置与结果固定如下：

1. 建立开放集前先校验起点，再校验终点。起点不是合法 doubled-coordinate 点、位于 Encounter 边界外或不满足调用者允许的起点占位条件时返回 `PATH_INVALID_START`；起点合法后，终点不合法、越界或不满足目的地体积条件时返回 `PATH_INVALID_DESTINATION`。两者同时非法时固定由起点原因胜出；不得进入 A* 后退化为 `PATH_NOT_FOUND`。
2. 从开放集取出一个非过期节点后，先执行 `ExpandedNodes = checked(ExpandedNodes + 1)`。起点计为第一个展开节点，过期队列项不计数；若结果 `> 4096`，立即以 `PATH_SEARCH_NODE_LIMIT_EXCEEDED` 结束，且不得先检查该节点是否为目标或展开其邻居。
3. 生成单个候选边时，依次执行 checked 坐标/`g`/`h`/`f` 运算、计算 `CandidateEdgeCount`、计算 `CandidatePathWeightUnits`、检查 Encounter 网格边界与占位。任一 checked 运算溢出立即以 `PATH_COST_OVERFLOW` 结束整个搜索。
4. `CandidateEdgeCount > 192` 时记录 `PATH_EDGE_LIMIT_EXCEEDED` 并丢弃该候选；否则，`CandidatePathWeightUnits > 256` 时记录 `PATH_WEIGHT_LIMIT_EXCEEDED` 并丢弃该候选。恰好等于上限的候选继续执行边界、占位和正常松弛检查。
5. Edge/Weight 超限只裁掉对应候选，不阻止其他合法分支继续搜索。开放集最终耗尽仍未找到目标时，按 `PATH_EDGE_LIMIT_EXCEEDED → PATH_WEIGHT_LIMIT_EXCEEDED → PATH_NOT_FOUND` 返回本次搜索曾记录的最高优先级原因。

预检优先级为 `PATH_INVALID_START > PATH_INVALID_DESTINATION`；进入搜索后的全局稳定失败优先级为 `PATH_COST_OVERFLOW > PATH_SEARCH_NODE_LIMIT_EXCEEDED > PATH_EDGE_LIMIT_EXCEEDED > PATH_WEIGHT_LIMIT_EXCEEDED > PATH_NOT_FOUND`。候选结果不能退化为 `null`；ScheduleEvaluator、AI、预览和回放必须消费同一判别结果。`PathCostRules`、启发函数版本、开放集平局规则、检查位置、失败优先级、网格边界和三个固定上限全部进入 `RulesVersion` / `BattleDefinitionHash`。任务 01 仍记录真实 Encounter 的典型/最大值，用于验证余量和性能，但不得据此在实现阶段静默改写首版常量；任何修改都必须更新规则版本、定义哈希和 golden snapshot。旧代码的 `1000` 次或 `±100` 坐标窗口只保留为 characterization 证据，不能成为 New 路径的备用限制。

路径选择与移动速度正交：`MoveSpeed` 不改变边权、启发值或路径赢家，只在 Move 首次进入 `Editable` 时量化 `ResolvedBaseStepTicks`。每条边的 Segment 时长为 `StepWeightUnits * ResolvedBaseStepTicks`，所以 A* 的路径总权重、时间预算与 MovementSegment 总时长使用同一尺度；`ResolvedPathEdgeCount` 只用于段数、索引和审计，不能代替成本。

**固定离散几何消费契约：** `LogicGrid`、Pathfinder、命中区域查询、Dodge 目的格校验和占位计算只消费 2.3.1 已生成的 12 向规范整数表。运行时只允许按 `GridDirection` 索引并做整数平移；旧 `GridMath.Rotate` 在兼容迁移完成前可留作 Legacy 路径，但不得被 New/Shadow Logic 调用，也不得用其结果覆盖权威表。

#### 3.8.1 GridManager 拆分

```csharp
// 逻辑层 — 纯数据网格
public sealed record BatchRelocation(
    UnitId UnitId,
    GridPoint ExpectedFrom,
    GridPoint To);

public class LogicGrid
{
    private readonly Dictionary<TrianglePoint, UnitId> _occupancyMap = new();
    private readonly Dictionary<TrianglePoint, Reservation> _reservationMap = new();

    public void RegisterOccupancy(UnitId unitId, IReadOnlyList<TrianglePoint> volume) { }
    public void UnregisterOccupancy(UnitId unitId) { }
    public bool IsOccupied(TrianglePoint point, UnitId? ignoreUnitId = null) { }
    public IReadOnlyList<UnitId> GetUnitsInAreaOrdered(
        IReadOnlyList<TrianglePoint> area,
        UnitId? ignoreUnitId = null) { }

    public ReservationResult TryReservePath(
        UnitId unitId,
        ActionPlanId actionPlanId,
        IReadOnlyList<MovementSegment> segments) { }

    // 只提交已经由强制位移求解器确定的最终位置；不做寻路、逐步移动或赢家选择。
    // 先验证全部来源与最终 footprint，随后统一移除旧占位、统一写入新占位。
    public void ApplyBatchRelocation(
        IReadOnlyList<BatchRelocation> relocations) { }
}

// 表现层 — MonoBehaviour 桥接
public class GridView : MonoBehaviour
{
    public LogicGrid LogicGrid { get; private set; }
    public float HexSize = 1.0f;

    public Vector3 GridToWorld(GridPoint gp) { /* 使用 Unity Vector3 */ }
    public GridPoint WorldToGrid(Vector3 wp) { /* ... */ }
    public TrianglePoint WorldToTriangle(Vector3 wp) { /* ... */ }

    private void OnDrawGizmos() { /* 可视化逻辑网格的 Occupancy */ }
}
```

世界坐标转换不属于逻辑网格。鼠标射线和 `WorldToGrid` 留在 `GridView`，生成 `GridPoint` 后再进入命令；逻辑层只处理离散格坐标。区域查询结果按 `UnitId` 排序。普通命令之间的 Reservation 冲突服从统一的规范化命令处理顺序：先成功提交者持有预留，后续冲突命令稳定拒绝；不得再用独立排序键抢占已经提交的 Reservation。`ApplyBatchRelocation` 是强制位移专用的原子占位提交原语：调用前必须完成全局同时求解和冲突 Reservation 的计划终止；它不调用 `TryReservePath`，也不把 Reservation 当作目的格赢家规则。若任一 `ExpectedFrom`、来源 footprint、最终 footprint 或批次不相交不变量失败，方法必须在零网格写入的情况下报告 `InvariantViolation`。

---

### 3.9 【P1-重要】AI 系统 Tick 化

```csharp
public sealed class AIControllerLogic
{
    public UnitId ControlledUnitId;

    private long _nextThinkTick;
    private const long THINK_INTERVAL_TICKS = 6;  // 0.1s @ 60 Ticks/s

    public CommandRequest? ThinkForNextTick(
        long completedTick,
        DecisionSnapshot publicSnapshot)
    {
        long targetTick = checked(completedTick + 1);

        // 只读取刚完成 Step 对玩家公开的同一机会快照。
        ReactionChoice? reaction = ChooseReactionFromStableCandidates(
            publicSnapshot.ReactionOpportunitiesFor(ControlledUnitId),
            publicSnapshot.GetUnit(ControlledUnitId),
            _rng);

        if (reaction is not null)
        {
            return new CommandRequest(
                targetTick,
                new ReactionCommandScope(reaction.OpportunityId),
                new ReactionCommand(
                    ControlledUnitId,
                    reaction.ActionSpecId,
                    reaction.DodgeDestination));
        }

        if (targetTick < _nextThinkTick) return null;
        _nextThinkTick = targetTick + THINK_INTERVAL_TICKS;

        TurnWindowSnapshot window = publicSnapshot.CurrentWindow;
        if (window == null || !window.IsAcceptingSubmissions ||
            window.OwnerUnitId != ControlledUnitId)
            return null;

        UnitSnapshot unit = publicSnapshot.GetUnit(ControlledUnitId);
        // 每个动作先按 AllowedTargetRelations 过滤，再按 ActionSpecId / 目标 UnitId 排序。
        BattleCommand command = ChooseCommandFromStableCandidates(
            unit, window, publicSnapshot.FactionRelations, _rng);

        return new CommandRequest(
            targetTick,
            new WindowCommandScope(window.WindowId),
            command ?? new CloseTurnWindowCommand(ControlledUnitId));
    }
}
```

AI 不直接调用 ActionScheduler、ReactionPlanner 或 TurnWindowManager，也不使用 `UnityEngine.Random`。Step N 完成后，AI 只读取与玩家同时可见的 `DecisionSnapshot`，创建 `TargetTick = N + 1` 的 `CommandRequest`，随后通过该 AI 控制器的已注册入口提交；`ControllerId`、AI 来源和 `ProducerOrdinal` 由入口绑定，`SourcePriority` 与 `CommandSequence` 由网关生成。Step N+1 只处理在开始时已经冻结的玩家/AI 请求，因此 AI 不能读取 N+1 新开的机会后在同 Tick 抢先反应。

普通规划仍按 `THINK_INTERVAL_TICKS` 运行，并只在拥有窗口权限时提出 Attack/Guard/Move。攻击候选必须对每个 ActionSpec 调用与命令处理器相同的 `FactionRelationResolver + AllowedTargetRelations`；AI 不保存一个由 Controller 类型推导的永久“敌人目标”，且即使预筛选通过，处理器仍会重新校验。反应检查每个 Tick 读取上一个 Step 已公开、与玩家相同的 `ReactionOpportunity` 快照；候选按 `OpportunityId → ActionSpecId → Destination` 排序，按预计分通道伤害、动量、肾上腺素费用、目的格和恢复占用评分，最后提交标准 ReactionCommand。是否允许 Block/Dodge 只看有效威胁、单位 ActionSet 和其他统一规则，不按敌我或 `CommandSourceKind` 分支。所有随机选择使用版本化 `DeterministicRng`，其当前状态、普通 `_nextThinkTick` 与已评估机会水位都进入规范化快照与哈希。

---

### 3.10 【P2-改善】Buff / 状态效果系统

```csharp
public abstract class StatusEffect
{
    public EffectId EffectId;
    public StatusEffectSpecId SpecId;
    public long EffectSequence;
    public long AppliedAtTick;
    public long DurationTicks;

    public bool IsExpired(long currentTick) => currentTick >= AppliedAtTick + DurationTicks;

    public abstract void OnApply(CombatUnitLogic unit);
    public abstract void OnTick(CombatUnitLogic unit, long currentTick);
    public abstract void OnRemove(CombatUnitLogic unit);
}

public sealed class BuffSystem
{
    private readonly SortedDictionary<UnitId, List<StatusEffect>> _unitBuffs = new(
        Comparer<UnitId>.Create((a, b) => a.Value.CompareTo(b.Value)));

    public void ApplyBuff(CombatUnitLogic unit, StatusEffect effect, long currentTick)
    {
        effect.AppliedAtTick = currentTick;
        effect.OnApply(unit);
        if (!_unitBuffs.ContainsKey(unit.UnitId))
            _unitBuffs[unit.UnitId] = new List<StatusEffect>();
        _unitBuffs[unit.UnitId].Add(effect);
    }

    public void AdvanceAllOrdered(long tick)
    {
        foreach (var (unitId, buffs) in _unitBuffs)
        {
            // 顺序：AppliedAtTick → EffectSequence；变更先放入延迟队列
            foreach (StatusEffect effect in buffs.OrderBy(x => x.AppliedAtTick)
                                                       .ThenBy(x => x.EffectSequence))
            {
                if (effect.IsExpired(tick))
                    _pendingRemovals.Add((unitId, effect));
                else
                    effect.OnTick(_logicWorld.GetUnit(unitId), tick);
            }
        }
        ApplyPendingChangesOrdered(tick);
    }
}
```

持续时间采用半开区间 `[AppliedAtTick, AppliedAtTick + DurationTicks)`；到期 Tick 先移除，不再额外触发一次 `OnTick`。Buff 回调新增或删除效果时写入延迟队列，避免遍历中修改集合。

---

### 3.11 【P1】从 Tick 0 重演的开发者回放

首版只交付用于开发、测试和问题复现的回放能力：载入一份记录后可播放、暂停、调整播放速度并从 Tick 0 重新开始。它不承担玩家回放库、任意 Tick 拖动、观战、分享、中途存档/恢复或跨版本迁移。

```csharp
public sealed record ReplayHeader(
    int ReplayFormatVersion,
    string RulesVersion,
    string BattleDefinitionHash,
    int TicksPerSecond,
    EncounterDefinitionId EncounterId,
    BattleRuntimeInputs RuntimeInputs,
    ulong InitialStateHash);

// 只用于序列化已由可信入口绑定、尚未由网关分配全局序号的事实。
// 首版权威回放输入只接受 Player 来源；AI/System 若被记录，只能作为诊断轨迹。
public sealed record RecordedCommandRequest(
    ControllerId Issuer,
    CommandSourceKind SourceKind,
    long ProducerOrdinal,
    CommandRequest Request);

public sealed class BattleReplay
{
    public ReplayHeader Header;
    public List<TickRecord> Records = new(); // 包含空 Tick

    public sealed record TickRecord(
        long Tick,
        IReadOnlyList<RecordedCommandRequest> ReplayInputRequests,
        IReadOnlyList<LogicEvent> Events,
        ulong SnapshotHash);
}
```

回放时使用相同规则版本、配置哈希、Encounter 和完整初始运行时输入新建 `BattleSimulation`，先验证首个 `Step()` 之前的初始规范化哈希等于 `InitialStateHash`，再从 Tick 0 开始，在记录所标注的原始提交 Tick 经专用回放入口注入相同的玩家可信请求事实。回放载入器必须根据 Header 对应的 `ControllerBinding` 重新验证 `ControllerId` / `CommandSourceKind == Player`、重复与回退的 `ProducerOrdinal`，再由 `CommandGateway` 重新派生优先级和生成 `CommandSequence`；不得把序列化 DTO 当成运行时授权，也不得直接记录并信任生产者构造的 `CommandEnvelope`。初始 RNG 种子与局外资源等输入必须随 Header 序列化，不能从当前存档或 Singleton 重新读取。AI 与 System 请求由逻辑重新生成并通过快照哈希、事件序列校验；调试模式可以额外记录它们及内部 Envelope 用于差异定位，但不得再次注入而造成双重执行。

时间线拖拽的本地草稿、鼠标轨迹、吸附与预览结果不属于回放输入；只有确认后进入注册入口的 `ScheduleEditCommand` 才记录，其 Operations、ExpectedScheduleRevision、TargetTick 与 ProducerOrdinal 必须原样重演。启动门禁、系统自动延期和锁定转换都不是外部命令，而是固定 Step 阶段从权威状态必然生成的逻辑事实；回放通过 `ActionPlanAutoDeferredEvent`、`ActionPlanLockedEvent`、`AutomaticDeferralCount`、LockedAtTick、ScheduleRevision 和逐 Tick 快照验证其一致性，绝不能再次注入系统延期而执行两次。

`ReplayFormatVersion` 只版本化文件结构、字段语义和哈希编码；`RulesVersion` 版本化玩法规则，两者不得混用。首版对任一不兼容版本都明确拒绝，不承担格式或规则迁移。

#### 3.11.1 确定性验收标准

回放不是最后才补的功能，而是每阶段重构的验收工具。至少需要记录以下快照：

```csharp
public sealed record LogicSnapshot
{
    public long Tick;
    public BattleEndSnapshot BattleEnd;               // Active/Ended、EndedAtTick、ResultCode
    public IReadOnlyList<UnitSnapshot> Units;       // 按 UnitId；含 FactionId、分通道抵抗、Adrenaline Available/Cycle
    public IReadOnlyList<StatusEffectSnapshot> Effects; // 按 UnitId/AppliedAtTick/EffectSequence
    public TurnWindowManagerSnapshot WindowManager; // 当前窗口、顺序索引、下一窗口 Tick
    public ConcurrentActionSnapshot ConcurrentAction; // 授权及其版本化内部状态
    public BattleResourceSnapshot Resources;        // TurnBudget Available/Reserved/Spent + Adrenaline 账本
    public long ScheduleRevision;                    // 每次成功的命令排程事务或系统自动延期事务恰好 +1
    public IReadOnlyList<ActionPlanSnapshot> Plans; // 未归档计划的完整字段；含 State、编辑/锁定 Tick、AutomaticDeferralCount、Timing、预算来源与 TriggerBinding
    public HistorySummary History;                // 不可变 RecordCount + Digest；不嵌入历史明细或可变归档引用
    public IReadOnlyList<ReactionOpportunitySnapshot> ReactionOpportunities;
    public IReadOnlyList<ActorLaneSnapshot> ActorLanes; // 按 UnitId
    public IReadOnlyList<IntentSnapshot> Intents;   // Tick/Priority/IntentSequence
    public IReadOnlyList<MovementSegmentSnapshot> MovementSegments; // ActionPlanId/StepIndex
    public IReadOnlyList<ReservationSnapshot> Reservations;
    public IReadOnlyList<AiControllerSnapshot> AiControllers; // 含 nextThinkTick 等未来决策状态
    public CommandIngressRegistrySnapshot CommandIngresses; // 注册、序号水位、未来 Tick 桶
    public RngSnapshot Rng;
    public long NextUnitId;
    public long NextWindowId;
    public long NextActionPlanId;
    public long NextReactionOpportunityId;
    public long NextEffectId;
    public long NextCommandSequence;
    public long NextIntentSequence;
    public long NextResolutionSequence;
    public long NextEventSequence;
    public long NextEffectSequence;
}
```

`LogicSnapshot` 是只读的规范化观察模型：它必须覆盖所有会影响未来结果的权威状态，以便哈希在偏差发生的第一个 Tick 就失败；后续每个系统都必须扩展相应快照与确定性测试。但它不是完整的序列化 `SimulationState`，不得提供 `BattleSimulation.Restore(LogicSnapshot)` 或依赖其重建模拟。未来若确实需要任意跳转，应另行设计版本化完整状态、迁移规则和每 N Tick 检查点，不能把当前观察 DTO 悄悄升级成存档格式。

活动状态的完整字段与已归档历史的 `HistorySummary` 共同参与每 Tick 校验；“覆盖全部权威状态”不要求重复展开历史明细。历史归档资格、摘要与诊断边界统一按 3.11.2 执行。

每次重构后运行同一条命令序列：

1. 固定纯数据 `BattleDefinition`、规则版本和 RNG 种子
2. 在相同原始提交 Tick 输入相同的 Player `RecordedCommandRequest` 序列，并让 AI/System 由逻辑重建、网关重新生成内部 `CommandEnvelope`
3. 推进相同 Tick 数，包括空 Tick 和只有命令的 Tick
4. 对比每个检查点的规范化 `LogicSnapshot` 哈希
5. 对比关键 Tick 的事件类型、载荷和 `EventSequence`
6. 对多方仲裁的 Intent、接触边和目标集合做排列测试；验证冲突组、接触结果、动作终态、目标聚合、事件与哈希均不受输入排列影响
7. 重复运行至少 100 次，验证最终哈希与事件流完全一致
8. 在 ActionPlan 仍为 Editable/Locked/Running 时切换窗口，验证窗口切换本身不改变计划、ScheduleRevision、Intent、Lane 和 Reservation
9. 验证窗口拥有提交权限但 Lane 锁定时命令被拒绝，以及单位当前非 Idle 但 Lane 允许排队时命令可被接受
10. 让窗口 A 提交的攻击在窗口 C 命中，并与窗口 B/C 提交的防御或攻击发生既定交互
11. 在存在 Editable/Locked/Running 计划、未来 Intent、MovementSegment 和 Reservation 时结束战斗，验证计划按 ActionPlanId 进入 `Terminated(BattleEnded)`、活动索引清空、`BattleEndedEvent` 只出现一次且为最后事件
12. 结束后重复调用 `Step()`，验证返回 `AlreadyEnded`、空事件批次和同一最终快照，且 Tick、RNG、ID 与所有 Sequence 均不变化
13. 对同一组带可信来源键的请求改变原始集合枚举顺序，验证规范化命令顺序、预算/Reservation 赢家、拒绝、事件与哈希一致
14. 注入重复或回退的 `ProducerOrdinal`，验证碰撞组无赢家、拒绝码稳定，且两次从相同初始输入启动的 Tick 0 重演拥有一致的入口水位与后续 `CommandSequence`
15. 尝试伪造来源优先级、控制其他 Controller 的单位、用旧窗口激活并发能力或在载荷中声明费用，验证均不能越过入口/规则边界
16. 分别以自然完成、显式取消入口、目标失效、来源威胁取消、Clash、Intercept、控制打断、Owner 死亡和战斗结束使计划进入终态；验证都只经过同一协调器，且 Step 结束时没有活动索引、Lane、Intent、MovementSegment、空间/肾上腺素 Reservation 或机会绑定指向该计划
17. 在移动段 `EndTick` 前终止计划，验证单位停留在最后已提交逻辑格、目的格 Reservation 释放，原 `EndTick` 不再提交位置；改变视觉插值进度不改变结果
18. 对同一计划重复或以冲突原因请求终态，验证第一次结果保持、生命周期事件只出现一次；Editable 普通计划释放未消费预算预留，Locked/Running 计划不退款，后续 Lane 不前移，ID/Sequence 不回卷且快照哈希稳定
19. 攻击公开前尝试提交 Block/Dodge 或在反应请求中伪造 TriggerTick，验证无法创建计划；Tick N 公开后玩家与 AI 都只能投递 Tick N+1 请求，并使用相同 ReactionOpportunity/ReactionCommand 路径
20. 对不同 Windup 的多个选项分别在截止 Tick、截止 Tick 后和来源攻击取消后提交反应，验证逐选项过期、一次性机会关闭、接受/拒绝及肾上腺素预留/释放完全符合规则
21. 同一 TriggerTick 用 Block 承受多个可格挡接触，验证所有合格伤害分量和动量均为 0；混入 Unblockable/BypassActionResistance 分量时只保留不合格载荷
22. 用 Dodge 移出单体攻击、移入另一个攻击、仍留在 AOE 以及目的格失效，验证结果来自 TriggerTick 后空间复核而不是 Dodging 状态免伤
23. 用 Guard 对多伤害通道和动量分别配置部分抵抗，验证通道结算顺序、Q10 量化、事件明细和聚合结果；将 Guard 换为 Block 时验证仅合格维度完全归零
24. 在其他单位窗口之间由多接触伤害、Clash 和成功 Block/Dodge 积累并消费肾上腺素，验证每单位每 Tick/组/计划只按冻结粒度聚合奖励且 Available 受上限约束；再打开该单位自身下一窗口，验证只在窗口打开边界清零 Available、窗口关闭不清零、既有预留仍可触发且旧周期取消不能返还到新周期
25. 从 Tick N 快照构造目标 N+1 的排程编辑，验证 Step N+1 先推进状态到期并原子应用编辑，再评估 StartTick=N+1 的启动门禁；Startable 才原子 `Editable -> Locked -> Running`，批次冻结后才产生的输入只能目标 N+2，不能改写已经锁定的计划
26. 对未来普通计划执行放置、重排、删除和移动链编辑，验证只有 Editable 计划可变、重叠只向右避让、删除不自动左吸、依赖闭包的路径/位置/时长/预算/Reservation 同时重算，失败批次零局部写入且 ScheduleRevision 不增加
27. 用旧 ScheduleRevision、尝试编辑 Locked/Running/反应计划或让排程越过最大视野，验证分别稳定拒绝；本地拖拽轨迹不进入回放，成功提交的 ScheduleEditCommand 从 Tick 0 重演后得到相同计划 ID、修订号、锁定事件和最终哈希
28. 将玩家/AI Controller 与 Allied/Neutral/Hostile 关系、Victory 目标外阵营交叉配置，验证控制权变化不改变关系、目标资格或胜负分组；分别消灭 Allied、Hostile 和同 Tick 双方全灭，验证结果严格来自 VictoryDefinition 的 Defeat/Victory/Draw 规则，目标外阵营存活不阻止结果且其 Disposition 不被覆盖
29. 令 Staggered/KnockedDown/Recovering 恰在 StartTick 到期，验证状态推进先于门禁且计划同 Tick 原子 `Editable -> Locked -> Running`；预算只消费一次，任何快照都不存在 Locked 但未 Running 的普通计划
30. 令带有限恢复 Tick 的控制或空间 Reservation 阻塞到期普通计划，验证同一 ScheduleEvaluator 把该计划移到 RetryAtTick、仅向右 ripple Editable 依赖闭包、保持 LastRequestedStartTick 与预算 Reserved，并令 AutomaticDeferralCount/ScheduleRevision 恰好各增加一次
31. 分别触发无有限恢复 Tick、自动延期次数/视野超限、永久目标/路径失效和候选重算失败，验证使用冻结的终态原因、释放 Editable 预算/空间预留且 ScheduleRevision 不增加；Locked/Running/反应计划均不移动，控制令固定反应以 InterruptedByControl 终止且不退款
32. 从 Tick 0 重演含系统自动延期的场景，验证不注入系统命令仍产生完全相同的 AutoDeferred/Locked 事件、ripple 列表、修订号与快照；构造 Lane/状态所有权矛盾时必须以稳定 InvariantViolation 明确失败而非伪装成玩法终态
33. 对三阵营关系矩阵改变输入枚举顺序，验证规范哈希与 Self/同阵营 Allied/跨阵营 Allied-Neutral-Hostile 查询一致；缺失、重复、反向重复或悬空关系必须阻止构建。再分别配置 Hostile-only、允许友伤和可攻击 Neutral 的单体/AOE，验证命令、Telegraph、Impact、ReactionOpportunity、AI 与回放都使用同一 `AllowedTargetRelations`，Controller 变化不改变结果
34. 对强制位移分别覆盖第一步/中途撞边界或静态障碍、静止单位阻挡、同落点争抢、两单位交换、`A → B → 空格`、三单位循环、多格 footprint 部分非法、部分距离成功、无独立请求时禁止连锁推人、覆盖 Reservation 并完整终止/清理计划、被位移单位旧起点 Move 失效，以及同 Tick 致死并击退；对请求和 Resolution 做全排列并重复重演 100 次，验证最终位置、AppliedSteps、停止原因、失效计划列表、位移事件与 SnapshotHash 完全一致

只要快照不一致，就说明改动影响了战斗结果，必须明确这是预期玩法变更还是重构回归。资源模型替换等玩法变更不强求旧快照不变：应保留旧版 characterization test，同时为新规则建立新的 golden snapshot 和不变量测试。

确定性验收范围是同一规则版本、同一配置和兼容运行环境下的可重复模拟。首版回放只从 Tick 0 重演，不提供中途存档恢复或任意跳转，更不承担跨版本存档迁移；版本或配置哈希不匹配时应明确拒绝。未来若建设迁移流程，应作为独立项目设计和验收。

---

### 3.11.2 活动快照、不可变历史与增量摘要

首版保留完整历史用于审计，但不在每个 Tick 重新复制、排序或哈希全部历史。`LogicSnapshot` 保留所有仍会影响未来结果的完整活动状态，加上固定大小的 `HistorySummary(RecordCount, Digest)`；测试与诊断按需构建 `DiagnosticSnapshot`，读取该 RecordCount 对应的不可变历史前缀。它不是第二套权威状态，也不是恢复存档；不得把完整诊断快照接入生产 Step 热路径或逐 Tick 缓存。

**归档资格与时机：**

1. 记录必须在本 Tick 全部结算、终态清理及 Step 末只读不变量检查完成后，确认不再变化且不再提供未来逻辑必需状态，才能冻结归档。终态协调器登记本 Tick 归档候选；归档器只处理增量候选，不扫描历史注册表找新记录，也不改变计划身份、终态、ID/Sequence 或事件顺序。
2. 计划终态记录保留完整 Timing、作用域、终态原因、TerminalTick 等审计字段，并在清理完成后归档一次。其他历史类型必须先定义同样明确的冻结条件；关闭窗口若仍有未消费预留、后续退款或消费可能，其账本仍属于活动快照。Accepted 但尚未触发/终止的反应机会等同理，不能仅凭“窗口关闭”或“已发生”就归档。
3. 归档记录为深度不可变值，不保留可变对象/集合别名；生产 API 不允许改写。活动状态与归档之间不得漏记录或重复归档。若未来判定需要某项历史派生信息，须把必要的紧凑状态显式保留在活动快照中，不能靠摘要替代业务查询或重新扫描完整历史。
4. 归档阶段位于每个有效 Step 构建快照之前，也适用于结束 Tick；它不发射玩法事件，`BattleEndedEvent` 仍是结束 Tick 的最后事件。`AlreadyEnded` 返回缓存结果，不再归档或更新摘要。初始归档为空且拥有固定种子摘要，参与首个 Step 前的 InitialStateHash。

**规范编码与增量校验：**

归档总顺序固定为 `ArchivedAtTick -> RecordKind 的版本化整数值 -> 该类型稳定实例键`；类型内复合键必须由任务 03 冻结，不能使用发现顺序。同一 Tick 收集完成后才规范排序并追加，各记录只追加一次。RecordCount 是归档追加计数，不占用玩法 ID 或 EventSequence；旧快照保存摘要值与记录数，后续追加不改变旧快照对应的历史前缀。

```text
H0 = Hash(Encode("HistorySeed", ReplayFormatVersion))
Hi = Hash(Encode("HistoryAppend", H(i-1), i, ArchivedAtTick, RecordKind, StableKey, RecordPayload))
SnapshotHash(T) = Hash(Encode("LogicSnapshot", ReplayFormatVersion, T,
                              CanonicalActiveState(T), RecordCount, H(RecordCount)))
```

Hash 算法与摘要宽度、域标记、整数大小/字节序、字符串编码、可空值、字段顺序、集合排序和长度前缀由任务 03 固定并纳入 `ReplayFormatVersion`。修改格式必须更新版本与对应 golden，不要求新摘要等于旧版“平铺全部历史”的哈希。没有新归档记录时复用同一历史摘要；有新增时只处理新增记录。活动状态首版仍完整规范化校验，不同时引入活动对象脏标记缓存。由同一初始输入从 Tick 0 重演必须逐 Tick 得到相同记录数、历史摘要、活动状态哈希和事件。

**诊断与参照验证：**

- 调试/测试参照路径可重新读取完整归档，独立按上述编码与顺序从 H0 重算摘要，并与增量结果逐检查点比较；两者比较的是同一摘要协议，不能把不同编码的哈希硬比。
- 归档前后字段完整性与深度不可变性要单独验证，包括修改原对象/集合不影响归档、重复归档被拒绝、排列不影响结果、可变账本不得提前归档、空 Tick 不读取旧记录、结束后不追加，以及篡改测试副本/丢失记录会被参照重算检测。不能让增量实现与参照路径共用一份缓存结果而形成假通过。
- 差异报告先标明活动状态或历史摘要不一致，再按归档记录序号及事件定位首个不同项；按需展开历史，不降低原有字段与事件校验覆盖。

**性能与存储边界：**

任务 01 冻结目标设备、构建模式、Tick 率、典型/最大活动规模及性能阈值；任务 03 建立快照/归档访问计数夹具，任务 05 接入真实终态，任务 11 完成长战斗验证。保持活动单位、计划、预留规模可比，对比第 1、10、30 分钟的 Tick 与快照构建耗时 P50/P95/P99、每 Tick 分配、GC 次数/停顿、历史条数/字节数、总内存和回放数据增长。必须证明无新增历史时每 Tick 读取/复制/哈希旧归档记录数为 0；新增时仅处理本批，不能因历史变长恢复全量扫描。性能阈值须在测量前确定，不得用事后解释替代通过标准。

增量摘要不消除历史本身的存储成本。首版允许完整历史在内存中保存一次，并与逐 Tick 回放记录的存储成本分别计量；超出约定时长/规模的内存预算时，另行评估分块写入回放文件及近期缓存/索引。本轮不要求实现磁盘分页、任意跳转、存档恢复或活动状态增量哈希，不得为了省内存丢失审计记录。

---

## 四、分阶段实施路线

### 第一阶段：确定性契约与基线验收（1 周）

| 任务 | 产出 | 方式 |
|------|------|------|
| 动作时序规则定稿 | 规则决策记录 | 秒只用于配置；Attack/Guard/Move/Block/Dodge 各有明确 TimingSpec、成本与 Tick 边界 |
| 滚动排程与锁定规则定稿 | 状态图、事务与 UI 能力清单 | 单一 ActionPlan；Editable→Locked→Running；N+1 状态到期/编辑先于启动门禁；保留放置/重排/删除/向右避让/移动链重算 |
| 执行前启动失败规则定稿 | 门禁结果矩阵、自动延期事务与终态映射 | Startable 原子锁定/消费/启动；Retryable 复用 ScheduleEvaluator；固定反应不延期；次数/视野有上限 |
| 阵营与胜负规则定稿 | Faction 关系矩阵、目标掩码、Victory 定义与 Draw 矩阵 | FactionId 来自 Encounter 槽位；静态对称关系全量配置；目标资格统一解析；ControllerBinding 不决定敌我；双方同 Tick 全灭显式 Draw |
| 回合窗口与动作独立性定稿 | 状态流与不变量 | 窗口只控制提交；ActionPlan 默认跨窗口存在 |
| 肾上腺素生命周期定稿 | 规则决策记录 | 跨对手窗口保留；伤害/结果奖励规范聚合并受上限约束；反应接受时预留、触发时消费，只在单位自己的下一窗口打开时清零 |
| 伤害与动量契约定稿 | 通道表、标签表与迁移表 | `DamageComponents[]` 与 `MomentumPacket` 正交；旧 ImpactType 显式拆为伤害通道与冲击 Profile |
| 高阶反应契约定稿 | Opportunity 状态图与截止规则 | 攻击公开后生成；命令不填 TriggerTick；玩家/AI/任意单位共用同一权限与计划路径 |
| 移动段与 Reservation 规则定稿 | 判定表 | 明确 Escape / Intercept 和冲突回滚 |
| 多方仲裁规则定稿 | Q10 方向表、冲突图、取消范围、多方动量与目标聚合表 | 验证夹击/AOE 语义及全部输入排列不影响结果 |
| 同时强制位移协议定稿 | 空间规则表、依赖图、停止原因、Reservation 抢占与提交顺序 | 全 Tick 单批逐格临时求解；部分距离保留；最终位置原子批量提交；不复用普通 Move/Pathfinder |
| 当前战斗基线记录 | characterization test / 日志 | 记录 2-3 个典型战斗片段 |
| 程序集与配置迁移基线 | 依赖闭包、资产清单、旧 ID 冲突表和 asmdef 草案 | 先建立 Logic；Authoring 在完整依赖闭包搬迁后建立；UnityView 最后建立 |

### 第二阶段：程序集边界、完整定义与纯 C# 最小内核（2-3 周）

| 任务 | 产出 | 影响范围 |
|------|------|----------|
| 创建 `ProjectHero.Logic` 和 EditMode 测试程序集 | Unity 以独立 asmdef 编译的纯逻辑内核 | 强制禁止 UnityEngine 引用；测试无需启动战斗场景 |
| 保留旧程序集兼容适配器 | 一条真实配置转换切片 | 读取 `Assembly-CSharp` 旧类型的代码暂留旧程序集，并单向引用 Logic |
| 搬迁 Authoring 依赖闭包并创建 `ProjectHero.Authoring` | 可编译的单向 Authoring 边界 | 保留 `.meta`；禁止自定义 asmdef 引用预定义 `Assembly-CSharp` |
| 完成全资产稳定 ID 迁移 | 显式旧 ID → Logic ID 映射与冲突报告 | `QuickSlash` 等局部同名条目不得自动合并或运行时补 ID |
| 创建 `BattleDefinitionBuilder` 与确定性初始化器 | 闭合定义、配置哈希、真实 fixture、Faction 关系解析器、Slot → Unit/Faction 映射 | 当前 Encounter 的全部引用必须可构建；关系矩阵缺失/重复/悬空、非法目标掩码、非法胜负分组或其他配置错误时整体失败 |
| 创建 `BattleSimulation.Step()` | 纯逻辑模拟入口 | 固定阶段顺序，空 Tick 也输出结果 |
| 创建单调逻辑 ID 与 `DeterministicRng` | 确定性基础设施 | ID/RNG 状态进入快照 |
| 创建 `LogicEventOutbox` | 不可变事件批次 | 语义事件，不包含表现参数 |
| 创建规范化 `LogicSnapshot` | 活动快照、归档接口与增量历史摘要 | 活动集合稳定排序，历史只在新增时规范化；完整诊断按需构建 |
| 实现最小垂直切片 | 可运行纯 C# 原型 | 窗口A提交攻击→按速度解析前摇→窗口B打开→ImpactTick 命中→Recovery→死亡 |
| 添加 Guard/Block/Dodge、三方 Clash、夹击和 AOE 用例 | 规则验证 | 验证 Guard 部分抵抗、Block 同 Tick 完全抵抗、Dodge 空间复核及多体仲裁 |

**进入下一阶段的门槛：** Logic 不引用 UnityEngine，Authoring 不引用 `Assembly-CSharp`，程序集图无环；所有当前 Encounter 使用的配置都有显式稳定 ID，能够零错误构建完整 `BattleDefinition`，并以相同定义、Encounter、种子和运行时输入重复初始化 100 次得到相同 Slot 映射与初始快照；同一模拟输入和种子重复运行 100 次哈希一致；攻击只在 `ImpactTick` 产生一次 Intent，速度只改变前摇且不改变后摇，Impact 前中断不命中；Guard/Move 的 Tick 与成本可由定义唯一推导；普通计划使用单一 ActionPlan 身份，目标 Tick 的 ScheduleEdit 先于同 Tick锁定，只触及 Editable 且原子回滚；没有来源攻击不能创建 Block/Dodge，合法反应的 TriggerTick 只能等于来源 ImpactTick；旧二体相向结果兼容；三方 Clash、反向夹击、Guard 部分抵抗、Block 多接触完全抵抗与 Dodge 空间复核的全部排列测试通过；新窗口可在旧 ActionPlan 未完成时打开，且旧计划的 Tick、Lane、ScheduleRevision 和 Intent 不被窗口切换修改。

### 第三阶段：运行时所有权壳与 Shadow 验证（0.5-1 周）

| 任务 | 说明 |
|------|------|
| 创建唯一 `BattleRuntimeBootstrap` | 场景中只有它通过 `Update()` 选择并推进顶层战斗时钟；模式在创建战斗前固定 |
| 将 `CombatDemo` / 旧 Timeline 改为 Legacy 被调用适配器 | 主场景先以 Legacy 启动，保持现有玩法和 `Time.deltaTime` 语义；旧组件不再自行推进顶层 Timeline |
| 分类并门控旧 Unity 回调 | `CombatUnit` / `BattleManager` / Enemy AI 等登记为 Legacy 从属写入者，只在 Legacy/Shadow 启用；表现回调单独列出；New 全部禁用旧写入组 |
| 创建最小 New 驱动适配器 | 由 Bootstrap 显式调用任务 03 的 Step；自身没有 Unity 生命周期入口，不提前迁移完整表现层 |
| 建立 `Legacy` / `Shadow` / `New` 三模式 | Legacy 不建新模拟；Shadow 仅 Legacy 写入；New 的旧 `AdvanceTime()` 调用数为 0 |
| 创建隐藏验证场景 | 使用任务 02B 的真实定义、Encounter、运行时输入和初始化 API，不用测试夹具冒充接入 |
| 创建 `ShadowComparisonReport` | 记录输入、Tick、事件/快照哈希与差异分类；已批准差异必须按用例和字段窄化 |
| 加入启动期失败检查 | 多 Bootstrap、自推进旧 Driver、Shadow 写场景或战斗中热切换均明确失败 |

**进入下一阶段的门槛：** 主场景经 Bootstrap 的 Legacy 模式运行时行为与基线一致；隐藏验证场景的三种模式互斥测试通过；Shadow 新模拟对 Unity 状态、旧逻辑状态和表现反馈的写入次数为 0；执行顺序测试证明只读检查点位于全部已登记 Legacy 写入之后；任何结果都被归类为“必须相等”“按新规则验证”“有明确清零任务的暂不可比较字段”或窄范围“已批准差异”，不存在宽泛忽略白名单。

### 第四阶段：状态机、滚动排程、命令批次、逻辑网格与 AI（3-4 周）

| 任务 | 说明 |
|------|------|
| `CombatUnit` 状态标志位 → `UnitStateMachine` | 8 个 bool → 1 个 enum |
| `StateChangeIntent` 改用状态机 | 完整转换矩阵；Dead 为终态；自动转换也发事件 |
| `TacticsController` 改用输入模式 + 命令批次 | 移除 `IgnoreUnitClicks` 标志 |
| 创建 `CommandRequest` + `CommandIngress` + `CommandGateway` + `BattleCommandProcessor` | 入口绑定 Controller/来源/ProducerOrdinal；网关生成内部 Envelope 与唯一序号 |
| 创建全局 ActorLane / ActionPlan / ScheduleEditor | 跨窗口存在；普通计划滚动编辑且只向右避让；执行前门禁可确定性自动延期；锁定/预算消费/启动原子；固定反应无冲突插入且不能平移；同一 ID 贯穿生命周期 |
| 创建 `ReactionOpportunitySystem` / `ReactionPlanner` | 攻击锁定启动后生成机会；ScheduleEdit/Window/Reaction scope 分离；Block/Dodge 进入同一 ActionPlan 轨道 |
| 在 `GridManager` 中分离 `LogicGrid` | 占位、Reservation、MovementSegment 全部逻辑化 |
| 增加同时强制位移求解与批量换位 | 任务 06 提供 `ApplyBatchRelocation` 原语，任务 08 负责全局求解、计划失效、事件与同 Tick 死亡顺序 |
| AI 改为命令生产者 | 固定 Tick、确定性 RNG；监听同一 ReactionOpportunity 并提交同一 ReactionCommand，不直接调度 |
| 添加快照回归用例 | 验证状态机/命令/网格迁移不改变既有结果 |
| 持续扩展 Shadow 场景与比较规则 | 每完成一个子系统都重跑相应 PlayMode/Shadow 证据；不得只凭 EditMode 夹具推迟集成发现 |

### 第五阶段：资源模型与回合窗口迁移（1-2 周）

| 任务 | 说明 |
|------|------|
| 移除 `CombatUnit` 中的体力/专注玩法依赖 | 字段可短期保留，逻辑不再读取 |
| 引入整数 `TurnBudgetTicks` | Attack/Guard/Move 在 Editable 时预留、启动门禁成功时原子消费；系统自动延期继续 Reserved，锁定前终态释放；不允许负预算 |
| 引入新 `Adrenaline` 生命周期 | 不随时间衰减；按冻结公式聚合伤害/结果奖励并限制 Available；反应预留/触发消费；按自身下一窗口打开规则归零 |
| 创建 `TurnWindow` / `TurnWindowManager` | 只管理提交授权、窗口预算与稳定切换，不持有动作 |
| 创建 `ConcurrentActionSystem` | 只授权 Attack/Guard/Move 等普通动作；不能授权或创建 Block/Dodge |
| 迁移玩法快照 | 为新资源规则建立新的 golden snapshot |

### 第六阶段：New 模式切换、可编辑时间线、表现层重构、开发者回放与性能基准（3-4 周）

| 任务 | 说明 |
|------|------|
| 完成 New 驱动与场景初始化桥 | 基于任务 03B 的被调用适配器接入任务 02B 完整定义、命令批次、事件与快照；不得新增第二个 `Update()` |
| 重构 `PhysicsEngine` / `CombatArbiter` / `AttackIntent` | 移除表现直调和视觉位置依赖；Resolution 统一提交并输出语义事件 |
| `BattleManager` 逻辑迁入 Step 阶段 | 死亡与胜负先于窗口切换；旧写入路径停止运行 |
| 创建 `CombatFeedbackMapper` | 将语义事件映射为伤害数字、震屏、顿帧 |
| 创建 `CombatUnitView` | 表现层桥接 |
| 重构 `UnitMovement` | 改为纯视觉插值 |
| 迁移普通时间线编辑器 | 基于只读快照和 Logic 预览恢复未来放置、吸附、重排、删除、向右避让与移动链重算；确认时只发原子 ScheduleEditCommand |
| 重构 `GameFeelManager` | 消费语义事件；视觉时间不影响逻辑时钟 |
| 添加 `UnitAnimator` | 状态机驱动的动画控制器 |
| 完善并接入 `BuffSystem` | 扩展配置与表现桥接；复用第四阶段建立的 Tick 驱动最小内核 |
| 主场景默认切换为 New | 切换前跑完整 Shadow 场景；切换后旧 `BattleTimeline.AdvanceTime()` 调用数为 0，Legacy 仅作为任务 11 前的可回切启动路径保留 |
| 开发者回放（从 Tick 0 重演） | 格式版本 + Header + 初始哈希 + 玩家可信请求 + AI/System 重建 + 事件/快照校验；支持载入、播放/暂停、速度调整与重新开始 |
| 性能基准对比 | Tick 耗时、GC、单位数量扩展测试 |

玩家回放库、分享/观战、任意 Tick 拖动、周期性完整状态检查点以及战斗中存档/恢复不属于本阶段，也不属于本轮重构的完成条件。

整体排期按单人全职估算约 **12-16 周**，不含新增玩法和大规模内容制作。相较只读时间线方案，新增工期来自 ScheduleEditor/预览同源求值、Editable 移动依赖重算、预算 Reserved→Spent、回放修订冲突和 Unity 时间线编辑迁移。各任务包顶部的“建议规模”只表示前置接口已经冻结时的净实施工作量，不等同于整体日历工期，也不能简单相加后替代本估算。整体排期包含跨任务集成、Unity 场景与 Prefab 修复、全量回归、试玩反馈和风险缓冲；其中任务 02B 的 Authoring 依赖闭包、全资产 ID 迁移、完整定义构建与初始化验证单独估算为 **3-6 天**；任务 03B 的唯一运行时所有者、三模式壳、隐藏验证场景和最小 Shadow 报告估算为 **2-4 天**；任务 10 按“New 驱动补全与模式切换 2-4 天”加“场景、可编辑时间线、UI、表现迁移与回归 5-8 天”估算为 **7-12 天**。这些工作不得相互隐藏或通过省略验证压缩。每一阶段不把“纯原型通过”等同于“现有 Unity 场景迁移完成”。

### 每阶段验收标准

| 验收项 | 标准 |
|------|------|
| 可运行性 | 主战斗场景可进入、可行动、可结束 |
| 纯逻辑边界 | Unity 将 `ProjectHero.Logic` 作为独立 asmdef 编译，且程序集无 UnityEngine 引用 |
| Authoring 边界 | `ProjectHero.Authoring` 只引用允许的自定义程序集与 Unity 模块，不引用预定义 `Assembly-CSharp`，程序集依赖无环 |
| 配置完整性 | 当前 Encounter 引用的全部配置均有显式稳定 ID；完整定义零错误构建，不存在跳过资产、名称回退或运行时默认值 |
| 初始化确定性 | 相同定义、Encounter、种子和运行时输入重复初始化，Slot → Unit 映射、初始快照、下一个 ID 与配置哈希一致 |
| 离散几何确定性 | 当前 Pattern/Volume 的 East 与 EastNorth 基准经受检整数公式无损展开为规范 12 向表；非法点或缺失基准稳定拒绝；Logic 命中、占位、Dodge 与寻路只查表和平移，不调用运行时三角函数或浮点舍入 |
| 寻路成本确定性 | 偶数/奇数方向严格使用 1/2 `StepWeightUnits`；A*、Move 时间与预算共用 `PathWeightUnits`；MoveSpeed 不影响选路；同成本路径、搜索上限、失败码和 checked 溢出结果可重复且参与定义哈希 |
| 阵营模型与胜负 | 每个槽位/运行时单位有稳定且首版不可变的 FactionId；不同阵营关系矩阵静态、对称、闭合并规范哈希；Self/Allied/Neutral/Hostile 目标掩码被命令、AOE、反应与 AI 共用；控制权不决定关系；VictoryDefinition 分组合法，胜/负/双方全灭 Draw 均有确定测试 |
| 运行时唯一性 | 每个战斗场景恰有一个 `BattleRuntimeBootstrap` 负责顶层时钟；Legacy/New/Shadow 适配器均无自主时钟推进生命周期 |
| 模式互斥 | Legacy 不调用新 Step；New 不调用旧 `AdvanceTime()` 且禁用全部 Legacy 从属写入者；Shadow 只有完整 Legacy 运行组写权威状态和播放反馈 |
| Shadow 隔离 | Shadow 新模拟使用独立世界，不绑定 View、不修改 Unity/旧逻辑状态；比较在只读 LateUpdate 检查点执行，差异按窄范围规则分类并可追溯，切换前暂不可比较字段为 0 |
| 切换可逆性 | 删除 Legacy 前完成“结束当前战斗并以另一模式新建战斗”的回切演练；不承诺或实现战斗中状态迁移 |
| 快照一致性 | 非玩法变更阶段固定命令输出一致；玩法变更建立新版 golden snapshot |
| 重复运行 | 同种子、同命令重复 100 次，检查点哈希与事件流一致 |
| 开发者回放 | 从 Header 新建模拟并由 Tick 0 重演；载入、播放/暂停、速度调整和重新开始得到相同事件与检查点哈希 |
| 窗口独立性 | 新窗口可在旧 ActionPlan 未完成时打开；窗口切换不修改计划、Intent、Lane 或 Reservation |
| 命令原子性 | 拒绝命令不改变预算或排程；ScheduleEdit 失败完整回滚且修订号不变；预算永不为负 |
| 启动门禁 | 到期普通计划只可能原子启动、确定性自动延期或以冻结原因终止；临时阻塞复用同一 ScheduleEvaluator，Locked/Running/反应不重排，内部矛盾明确失败 |
| 攻击时序 | 前摇按 `ReferenceActionSpeed / ActionSpeed` 一次性量化；命中只发生于 `ImpactTick`；后摇不受速度影响；预算成本等于前摇加后摇 |
| 普通动作时序 | Guard 的 Windup/Active/Recovery、Move 的每权重单位 Tick/Recovery 与预算成本均由专属 TimingSpec 一次性量化；权重 2 的边持续时间和预算都是权重 1 边的两倍 |
| 反应时序 | Block/Dodge 只能引用攻击公开后生成的 Opportunity；Tick N 公开后玩家/AI 最早都投递 N+1；命令不能提供 Tick；TriggerTick 严格等于来源 ImpactTick，固定区间不得被 Lane 平移 |
| 反应公平性 | 任意单位的可用性只取决于 ActionSet、机会、状态/Lane 与肾上腺素；玩家和 AI 使用同一公开快照、输入提前量、命令、校验、费用、拒绝码与计划路径 |
| 防御语义 | Guard 仅在 Active 区间提供部分伤害/动量抵抗；Block 在 TriggerTick 对全部合格接触完全抵抗；Dodge 只通过原子换格和空间复核避伤 |
| 伤害统一 | 接触使用稳定 DamageChannelId 分量与独立 MomentumPacket；事件/快照可追溯 Raw、被动抵抗、动作抵抗和最终动量 |
| 肾上腺素周期 | 跨其他单位窗口保留；伤害按单位/Tick、Clash 按单位/组、成功反应按计划各聚合一次并受 Available 上限约束；接受反应时预留、触发时消费；只在单位下一自身窗口打开时清零，不能重复奖励/预留或从取消中套利 |
| 跨窗口交互 | 过去窗口提交的动作可与当前及未来窗口动作发生全部既定交互 |
| 计划终态闭合性 | 任意终态来源都经统一协调器；Step 结束时没有活动 Lane/索引/Intent/MovementSegment/Reservation/资源预留指向终态计划；Editable 释放未消费预算预留，Locked 后不退款；清理幂等、不回卷、不隐式左移 |
| 战斗终态 | 结束 Tick 按 ActionPlanId 复用统一协调器；活动计划/Lane/Intent/MovementSegment/空间与肾上腺素 Reservation/ReactionOpportunity 清空，计划历史保留并记录 `BattleEnded` 原因 |
| 结束幂等性 | 结束后误调 Step 返回 `AlreadyEnded` 与同一最终快照，不推进任何逻辑计数器或重复事件 |
| 状态机 | 转换矩阵、自动到期、打断和 Dead 终态测试通过 |
| 多方仲裁 | Q10 有向动量、冲突图和取消范围符合规则；Intent/边/目标任意排列得到相同接触、终态、聚合、事件与哈希 |
| 同时强制位移 | 全 Tick 请求在临时空间逐格同时求解；边界/障碍停在最后合法格，同落点/交换/循环全失败，通向空格的独立依赖链可移动，不连锁推人；Reservation 被稳定抢占并完整清理；最终位置仅经一次原子批量提交，排列与 100 次重演结果一致 |
| 时钟隔离 | 相同命令下改变 `Time.timeScale` 不改变逻辑快照 |
| 表现兼容 | 伤害数字、震屏、移动插值、UI 不丢失 |
| 性能 | 不新增高频 `FindObjectsByType` / `GetComponent` 热路径 |
| 性能基准 | 按 3.11.2 在固定设备/规模下比较第 1/10/30 分钟的 P50/P95/P99、分配/GC、历史和总内存；预先冻结阈值，无新增历史时旧记录访问数为 0 |

---

## 五、架构收益总结

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| **战斗资源** | 体力 + 专注 + 肾上腺素 + 时间（混乱） | 时间 + 肾上腺素（清晰） |
| **回合模型** | 伪实时（Update 驱动 AI） | 回合制表面 + 实时底层 |
| **主角并发行动** | 不支持 | 原生支持（ConcurrentActionSystem） |
| **通用高阶反应** | 玩家特例/预排防御区间 | 任意单位通过 ReactionOpportunity 响应已公开威胁，玩家与 AI 同规则 |
| **伤害与冲击** | ImpactType、伤害类别与防御减免混用 | 分通道 DamageComponents 与独立 MomentumPacket；Guard/Block 分别抵抗 |
| **逻辑可测试性** | 必须运行 Unity 场景 | Unity EditMode 下的纯逻辑单元测试，无需启动战斗场景 |
| **回放确定性** | 受帧率、Time.timeScale 影响 | 开发者回放可在同规则版本与兼容环境下从 Tick 0 重复验证 |
| **逻辑帧一致性** | 部分 Update 部分 Tick | 战斗结果全部 Tick 驱动 |
| **表现层独立性** | 逻辑调用表现 API | 事件驱动，单向依赖 |
| **时间线规划** | UI 直接修改旧调度对象，能力丰富但难回放 | 保留未来放置/重排/删除/向右避让/移动链重算；单一 ActionPlan 经原子命令编辑，并在门禁通过后原子锁定/启动 |
| **输入健壮性** | 布尔标志，易遗漏 | 输入模式 + 不可变命令批次 |
| **调试体验** | 状态散落在 8 个 bool 中 | 单一状态枚举 + 转换日志 |
| **性能** | 每帧 FindObjectsByType | 缓存列表 + 按需检测 |

---

## 六、风险与注意事项

1. **渐进式重构**：不推倒重来。每个阶段产出独立可运行的中间版本。
2. **表现层兼容**：在 `CombatUnitView.Bind()` 阶段，可以同时保留旧的 `CombatUnit` 字段作为桥接，逐步迁移。
3. **`Update()` 边界**：允许输入、UI、视觉插值留在 Unity 生命周期中，但不得修改战斗结果。
4. **确定性基线**：每阶段都要跑固定命令序列和快照对比，避免“看起来没坏”的隐性回归。
5. **性能基准**：先记录当前场景基线，再比较 Tick 耗时与分配量，避免用“没有高频查询”等替代真实测量。
6. **同 Tick 多方仲裁**：多方同帧交互必须从只读快照构建冲突图并同时求解；稳定排序只用于规范化输出，不能代替有向动量、取消范围和目标聚合规则。
7. **团队共识**：建议团队评审此方案后，选定各阶段的优先级和排期。
8. **局外资源接口**：主角并发行动能力消耗的"局外资源"需要在 meta-progression 系统中明确定义接口，避免耦合。
9. **RNG 确定性**：AI 决策和随机元素需要使用固定种子的 RNG，确保回放一致性。
10. **回放版本边界**：回放分别记录文件格式版本、规则版本和配置哈希；首版不做跨版本兼容或迁移，任一不兼容都应明确拒绝，不能静默产生不同结果。
11. **程序集迁移顺序**：自定义 asmdef 不能引用预定义 `Assembly-CSharp`。任务 02 只建立 Logic/Tests 并把旧类型适配器留在旧程序集；任务 02B 按依赖闭包搬迁 Authoring 后再创建 asmdef；任务 10 最后搬迁 UnityView。任何阶段都不得靠复制旧类型或建立反向引用“临时过编译”。
12. **浮点范围**：首版允许生命与伤害保留 float，但禁止 `deltaTime` 驱动，并对分支阈值和快照格式做集中测试。
13. **窗口与动作解耦**：窗口切换代码不得遍历、取消、锁定或重排 ActionPlan；显式 ScheduleEdit 可以修改 Editable 普通计划，执行前门禁可以按冻结规则触发系统自动延期，两者成功都增加 ScheduleRevision；除此之外不得隐式改排程。必须用自动化测试防止后续维护者重新引入传统“回合结算动作”的假设。
14. **Lane 队列膨胀**：ActionPlan 可以跨多个窗口排到未来，ActorLane 必须限制最大排队数量、单批编辑规模、依赖闭包和最大时间视野，并给出稳定拒绝原因。
15. **跨窗口交互规模**：仲裁必须处理所有本 Tick 有效动作，不能只查看当前窗口参与者；应对未来计划数量和冲突组规模建立性能基准。
16. **战斗结束闭合性**：胜负检测只声明结果，唯一 Finalizer 负责按 ActionPlanId 调用普通终态协调器、关闭授权并最后发出 `BattleEndedEvent`；不得复制计划从属对象清理，也不得让各系统各自重复执行一部分。
17. **动作时序与寻路版本边界**：`ReferenceActionSpeed`、`ReferenceMoveSpeed`、各 TimingSpec、默认 Recovery、`PathCostRules`、启发函数/开放集平局规则、`PathSearchRules`、网格边界、CommandIngressLead、反应 Windup/Trigger、Editable 重排/启动门禁/锁定顺序、`MaxAutomaticDeferralsPerPlan`、舍入方式、动作阶段和同 Tick 顺序都属于玩法规则；修改任一项必须更新规则版本、定义哈希与 golden snapshot。
18. **配置迁移完整性**：`QuickSlash` 等旧局部 ID 可能跨资产同名而语义不同，必须用显式迁移表逐项确认。任何当前 Encounter 引用的缺失、歧义或非法定义都应阻止构建和战斗启动，不能由任务 10 运行时猜测、跳过或补默认值。
19. **命令信任边界**：生产者不能填写身份、来源优先级、`ProducerOrdinal`、`CommandSequence`、规则费用或反应 TriggerTick。注册入口绑定 Controller 与来源，网关拒绝序号碰撞/回退并生成唯一顺序；处理器按 payload 判别验证 `ExpectedWindowId` 或 `ReactionOpportunityId`。规范化快照必须包含入口水位，开发者回放通过 Tick 0 重演重建它，而不是从中途快照恢复。
20. **普通终态闭合性**：不能只在战斗结束时清理计划从属对象。每个终态来源都必须复用同一个幂等协调器，并在 Step 边界检查无孤儿引用；移动打断保留最后已提交逻辑格。Editable 计划释放未消费预算预留，Locked 后终态不退款；两者都不等同于事务回滚或时间轴自动压缩。
21. **运行时所有权唯一性**：旧 `CombatDemo.Update()`、新 Driver 和临时验证脚本不能各自推进顶层战斗时钟。唯一 Bootstrap 必须在启动时拒绝重复所有者，并按模式整体门控已登记的 Legacy 从属写入者；代码库允许两套内核暂时存在，不允许两套顶层调度或权威写入。UI/视觉回调不属于战斗时钟所有者，但仍不得写逻辑。
22. **Shadow 假绿/伪差异风险**：新旧规则本来就存在资源、时序和防御语义差异，不能用整类事件、血量或位置忽略项掩盖问题；分散 Legacy `Update()` 也可能因采样顺序制造伪差异。比较必须在显式排序的只读 LateUpdate 检查点进行，区分基础设施一致性、新规则不变量、限期清零的暂不可比较字段和逐用例批准差异，并记录首个非预期偏差。
23. **切换与回退边界**：运行模式在战斗创建前固定，禁止运行中把 Legacy 状态迁入 New 或反向热切换。任务 10 切到 New 后先保留无调用的 Legacy 启动路径；只有任务 11 完成重新开局式回切演练、稳定性验证和场景/Prefab 引用扫描，才能删除它。
24. **Shadow 非干扰与开销**：双模拟可能改变帧率并间接扰动仍依赖帧节奏的 Legacy 代码。等价性证据优先来自固定帧时间的隐藏场景；实时诊断超预算时必须令比较失败/无效并记录耗时与 backlog，不能通过跳 Tick、篡改 Legacy 时间输入或降级比较来保持绿灯。正式发行默认不启用 Shadow。
25. **反应机会可达性**：机会晚于 TelegraphTick 打开、选项截止早于一次输入往返，都会制造“UI 看得见但永远按不到”的伪选择。Builder 必须校验 CommandIngressLead/MinimumReactionLead，运行时按选项截止；玩家和 AI 都不能绕过。
26. **固定反应与 Lane 冲突**：Block/Dodge 的 Start/Trigger/EndTick 不能像普通动作一样附加到 Lane 尾部。首版只允许插入无重叠空隙并稳定拒绝冲突；自动抢占或重排会改变既有计划，必须另立玩法规则。
27. **Dodge 空间竞态**：目的格从接受到 Trigger 之间可能与其他 Reservation/占位竞争。必须在同一事务预留、按 CommandSequence 决定赢家，并在 Trigger 先原子提交再重建接触；不能以 Dodging 状态掩盖提交失败。
28. **肾上腺素跨周期预留**：自身窗口打开会清零 Available，但不能破坏已接受反应。预留必须带 CycleId；旧周期预留可以履约，取消时不得返还为新周期 Available，否则可跨周期复制资源。
29. **伤害迁移歧义**：旧 `ImpactType` 同时影响冲击并被当作伤害语义，旧护甲/魔抗也可能已折叠。迁移必须逐项拆为 DamageChannel、ImpactProfile、标签和被动抵抗；无法确认的资产应阻止定义构建，不能默认为物理。
30. **AI 反应公平性**：AI 每 Tick 可以读取机会，但不能直接创建计划或获知未 Telegraph 的队列。AI 与玩家使用相同 Opportunity、ActionSet、截止、资源和命令入口；难度只能影响选择策略，不能成为规则权限旁路。
31. **执行前门禁的 off-by-one 风险**：锁定线显示 `CurrentTick + 1` 不等于在当前 Tick 立即锁定下一 Tick。必须以测试证明目标 Tick=T 的冻结 ScheduleEdit 和状态到期先完成，随后才评估 StartTick=T；门禁通过才原子锁定/启动，批次冻结后的迟到输入只能目标 T+1。
32. **排程修订竞争**：UI 预览可能在提交前过期。每批编辑都必须携带 ExpectedScheduleRevision，并与目标 Step 冻结时的 BatchBaseScheduleRevision 比较；跨 Tick 旧修订整批拒绝。同一冻结批次允许互不相交 Lane 的事务按 CommandSequence 成功，触及已改写依赖闭包的后处理事务稳定拒绝；禁止静默合并或只应用一条命令内部的部分 Operations。
33. **预算预留跨窗口审计**：关闭窗口后仍可能存在 Editable 计划的预算预留。窗口账本必须保留到全部预留锁定或释放，且释放不能重开已关闭窗口、转移到其他窗口或制造可重复消费额度。
34. **连锁重算成本与可理解性**：显式排程编辑及系统自动延期的向右避让和移动链重算都只能影响依赖闭包内的 Editable 普通计划，必须设置规模上限，并以同一求值器完成预览、命令提交和系统候选提交。删除不自动左吸，避免玩家未操作的空隙突然消失。
35. **阵营模型被简化成玩家/敌人布尔值**：`IsPlayerControlled`、Controller 类型和当前窗口归属都不能替代 `FactionId` 或关系矩阵。Builder 必须拒绝缺失/重复/反向重复的阵营对、非法 TargetRelationMask，以及与关系矩阵矛盾的 Victory 分组；命令、AOE、反应和 AI 必须共用同一解析器。双方同 Tick 全灭必须先聚合死亡再按 Draw 规则判定，不能依赖遍历或 if 顺序。
36. **自动延期失控**：临时状态若没有权威结束 Tick，或每 Tick“跳过再试”，会制造无限 Editable 计划和不稳定修订。门禁只能接受有限 RetryAtTick，并受每计划次数、最大视野和依赖闭包上限约束；失败必须进入确定终态或不变量错误，不能静默滞留。
37. **Pattern/Volume 运行时浮点旋转回流**：旧 `GridMath.Rotate` 使用三角函数和浮点舍入；若 New/Shadow Logic 继续调用它，同一资产可能因平台或调用路径差异产生不同命中区/占位区。Builder 必须从 East/EastNorth 基准用受检整数公式生成规范 12 向表并把最终表纳入定义哈希；运行时只准查表和平移，边界测试与源码扫描共同阻止旧旋转依赖回流。
38. **强制位移被顺序提交或误复用普通移动**：若逐单位写网格、按 UnitId/动量选争抢赢家、把 Reservation 当墙，或让 Pathfinder/MovementSegment 处理击退，结果都会依赖提交顺序并破坏同 Tick 空间语义。必须从不可变快照建立一个全局批次，在临时空间同时求解，先稳定终止受影响空间计划，再由 `ApplyBatchRelocation` 一次性换位；停止原因、失效计划和最终事件都必须进入 golden 与回放证据。

---

*方案编写日期：2026-07-04*
*更新时间：2026-08-20（冻结同时强制位移协议、Reservation 抢占、计划失效原因、原子批量换位与同 Tick 死亡顺序）*
