# Project Hero 游戏架构优化方案

## 核心目标

1. **逻辑与表现完全解耦** — 逻辑层零依赖 UnityEngine 视觉 API（GameObject、Transform、Camera、Animator 等）
2. **完全基于逻辑帧判定** — 所有会改变战斗状态的逻辑在离散 Tick 中执行，杜绝 `Update()` 推进逻辑状态
3. **可测试、可回放、可序列化** — 逻辑层纯 C# 数据，可在无 Unity 环境下单元测试
4. **回合制表面 × 实时制底层** — 单位轮流行动、时间预算分配；底层实时 Tick 支持主角并发行动与动作交互

## 落地修订摘要

本方案的架构方向保持不变，但实施策略做如下修订：

1. **`Update()` 不是禁区，逻辑状态变更才是禁区。** 输入采样、UI 刷新、视觉插值、相机、特效仍可运行在 `Update()` / `LateUpdate()`；它们只能产生命令或表现，不能直接修改战斗逻辑状态。
2. **P0 范围收紧。** 首批改造只建立兼容壳、事件总线、单位注册表、Tick 阶段骨架、状态机和确定性测试夹具；资源模型与回合系统在基础设施稳定后迁移。
3. **状态机前置。** 当前多个 bool 标志会放大重构风险，必须先收敛为 `UnitStateMachine`，再迁移动作、资源和回合逻辑。
4. **网格逻辑化提前。** 命中、占位、寻路和回放都依赖网格确定性，`LogicGrid` 不应拖到纯表现层重构之后。
5. **每阶段必须有回放/快照验收。** 同一初始状态 + 同一命令序列 + 同一 RNG 种子，在 N 个 Tick 后必须得到完全一致的单位位置、血量、状态与事件日志。

---

## 〇、游戏设计基准（优先于代码改造）

在写任何代码之前，先确立以下设计决策。这些决策直接影响架构形态。

### 0.1 战斗资源模型

**只保留两种战斗资源：时间 与 肾上腺素。**

| 资源 | 获得方式 | 消耗方式 | 生命周期 |
|------|----------|----------|----------|
| **时间（Time Budget）** | 回合开始时获得固定时长 | 每个动作消耗其 BaseTime | 回合内递减，回合结束清零 |
| **肾上腺素（Adrenaline）** | 造成伤害、受到伤害、成功闪避、拼刀 | （根据动作设计决定） | 不随时间衰减；在该单位最近一次行动回合结束后归零 |

**移除：** 体力（Stamina）、专注点（Focus）、护甲重量（ArmorWeight）等传统 RPG 资源。

**格挡 / 闪避的重新设计：**
原先格挡和闪避消耗专注点（Focus）。专注点移除后，这两个动作的定位需要重新考量：
- **格挡和闪避仅在主角激活并发行动能力时可用**（即只在敌方回合内、主角获得并发行动权后才可使用）
- 它们只消耗时间（从活跃回合的时间预算中扣除），不消耗其他资源
- 在自己的回合内，主角不需要格挡/闪避——因为此时没有敌方攻击需要防御
- 敌方单位不使用格挡/闪避（或极度简化，仅作为 AI 的被动反应参数）

**设计理由：**
- 时间作为唯一行动资源，直观且统一——每个动作本质上就是"花费时间做某事"
- 肾上腺素作为"这一轮打得怎么样"的累积度量，鼓励进攻，且不会跨轮累积导致雪球效应
- 资源模型极简化后，玩家决策聚焦于"有限时间内做什么"而非"管理多个资源池"
- 格挡/闪避作为并发行动专属动作，强化了主角能力的战术价值——激活并发行动不仅是"多一次行动机会"，更是唯一能使用防御反应动作的窗口

### 0.2 回合制表面 + 实时制底层

```
表面（玩家看到的世界）：
  ┌─────────────────────────────────────────────────────┐
  │  单位A的回合 → 单位B的回合 → 单位C的回合 → ...      │
  │  每回合获得时间预算，动作消耗时间                     │
  │  时间预算耗尽 → 回合自动结束                          │
  └─────────────────────────────────────────────────────┘

底层（逻辑帧的真实世界）：
  ┌─────────────────────────────────────────────────────┐
  │  Tick 0 ── Tick 1 ── Tick 2 ── ... ── Tick N       │
  │  所有单位的时间线事件共存于同一条 BattleTimeline      │
  │  主角激活并发行动后，可与当前回合单位同时排入动作      │
  └─────────────────────────────────────────────────────┘
```

**为什么需要实时底层？**

核心原因是**主角的并发行动能力**：主角使用局外资源激活能力后，**获得在当前活跃单位（通常为敌方）的回合内行动的权利**。这意味着两个单位的动作时间窗口可能重叠在同一时间线上，需要一个能处理并发动作和动作间交互的底层系统。

**并发行动 ≠ 中断/偷回合：**
- 不是"打断敌人的回合，换成主角的回合"
- 不是"偷取敌方单位的时间预算"
- 而是"主角获得在当前回合内的**并发行动权**"——回合仍属于原单位，但主角现在也可以在里面排入动作
- 两个单位的动作共享同一个时间预算，并发执行

```
具体示例：
  单位A的回合（时间预算 3.0s）
    ├── 单位A排入 轻攻击 (0.6s)         → 剩余 2.4s
    ├── 单位A排入 重攻击 (1.2s)         → 剩余 1.2s
    ├── [★ 主角激活并发行动能力]         → 花费局外资源，获得并发行动权
    │     主角排入 闪避 (0.3s)           → 剩余 0.9s
    │     主角排入 反击 (0.4s)           → 剩余 0.5s
    ├── 单位A排入 移动 (0.5s)            → 剩余 0.0s
    └── 时间预算耗尽 → 回合结束 → 肾上腺素归零
  单位B的回合开始...
```

**关键设计要点：**
1. 并发行动权需要消耗局外资源激活
2. 激活后，主角可以在当前回合剩余时间内排入动作，直至时间预算耗尽
3. 主角和回合拥有者的动作可以在时间线上重叠，`CombatArbiter` 负责在重叠帧判定交互
4. **格挡和闪避仅在并发行动激活期间可用**——在自己的回合内不需要它们，在敌方回合内它们是珍贵的防御反应窗口
5. 回合结束时，并发行动权自动结束

### 0.3 回合结构

```
一次正常的敌方回合（主角未激活并发行动）：
  ┌──────────────────────────────────────────┐
  │ 1. 回合开始 — 获得 TimeBudget（如 3.0s）  │
  │ 2. AI 排布动作 — 消耗时间预算              │
  │ 3. 时间线执行 — 动作按序触发               │
  │ 4. 回合结束 — TimeBudget 耗尽或 AI 主动结束 │
  │ 5. 肾上腺素归零                            │
  └──────────────────────────────────────────┘

主角激活并发行动后的回合：
  ┌──────────────────────────────────────────┐
  │ 1. 回合开始 — AI 获得 TimeBudget          │
  │ 2. AI 排布动作                             │
  │ 3. [★ 主角激活并发行动]                    │
  │ 4. 主角与 AI 交替/同时排入动作              │
  │    主角可排入：攻击、移动、格挡、闪避       │
  │ 5. 回合结束 — TimeBudget 耗尽              │
  │ 6. 并发行动权自动结束 → 主角回到观察状态    │
  │ 7. 肾上腺素归零                            │
  └──────────────────────────────────────────┘
```

**规划与执行可以交错（"实时规划"）：** 玩家在时间预算内随时添加新动作，而非一次性排完。这样设计是为了：
- 允许玩家根据前一动作的结果调整后续动作
- 让并发行动中的防御反应（"在敌人挥刀到一半时格挡"）成为可能——玩家看到敌方攻击起手后，在并发行动中排入格挡/闪避，二者在时间线上重叠，由 Arbiter 判定

### 0.4 交互判定优先级矩阵

### 0.4 交互判定优先级矩阵

当两个动作在同一逻辑帧相交时，按以下优先级判定：

```
查询优先级（从高到低）：
  1. Attack vs Dodge        → 闪避优先（攻方取消，守方获得反击窗口）
  2. Attack vs Block        → 格挡优先（攻方弹刀，双方取消）
  3. Attack vs Attack       → 拼刀判定（比较动量，胜者击退败者）
  4. Attack vs Move (end)   → 拦截（移动方被击中，倒地）
  5. Attack vs Move (start) → 逃脱（移动方脱离攻击范围，不受伤害）
```

**设计原则：** 防御动作（Dodge/Block）优先于进攻动作。主动防御 > 被动判定。

同一优先级内，如果涉及多方（如 3 个 Attack 同时命中同一目标），按 Attack 动量排序，高动量 Attack 优先判定。

### 0.5 并发行动系统设计

#### 0.5.1 核心概念

主角拥有一种与游戏局外资源（meta-progression currency）挂钩的能力：**并发行动（Concurrent Action）**。

```
非主角回合 → 主角处于"观察状态"，不能排入任何动作
       ↓
主角支付局外资源 → 激活并发行动 → 主角获得在当前回合内的行动权
       ↓
主角可以排入：攻击、移动、格挡、闪避（这些动作消耗当前回合的时间预算）
       ↓
回合结束 → 并发行动权自动结束 → 主角回到观察状态
```

**关键约束：**
- 主角只能在自己的回合**之外**激活并发行动（在敌方回合或盟友回合）
- 并发行动激活后，主角动作消耗当前活跃回合的时间预算
- 当时间预算耗尽，回合结束，并发行动权也同时结束
- **格挡和闪避仅在并发行动激活期间可用**

#### 0.5.2 为什么格挡/闪避是并发行动专属？

| 场景 | 是否需要格挡/闪避 | 原因 |
|------|-------------------|------|
| 主角自己的回合 | 否 | 此时只有主角在行动，没有需要防御的敌方攻击 |
| 敌方回合（未激活并发行动） | 否 | 主角处于观察状态，无法行动 |
| 敌方回合（并发行动已激活） | **是** | 敌方正在攻击，主角可以实时防御反应 |

这一定位让并发行动能力成为战斗的核心战术资源——激活它不仅意味着"多一次行动机会"，更意味着**获得了防御反应的资格**。玩家需要在敌方攻击起手时精准判断：是格挡（低风险低回报）、闪避（高风险高回报，成功后获得反击窗口）、还是硬吃伤害并反击（最大化输出但承受伤害）。

#### 0.5.3 并发行动中的时间预算共享

```
时间预算不属于任何一个单位——它属于"当前回合"。
回合拥有者和并发行动者都从中消费。

时间预算 = 3.0s（单位A的回合开始获得）
  ├── 单位A：轻攻击 (-0.6s)  → 剩余 2.4s
  ├── 单位A：重攻击 (-1.2s)  → 剩余 1.2s
  ├── [主角激活并发行动]
  ├── 主角：闪避 (-0.3s)     → 剩余 0.9s
  ├── 主角：轻攻击 (-0.4s)   → 剩余 0.5s
  ├── 单位A：移动 (-0.5s)    → 剩余 0.0s
  └── 回合结束
```

时间线上的并发重叠：

```
Tick:   0     36    108    126    150    180
        [A 轻攻击 0.6s][A 重攻击 1.2s        ]
                       [主角闪避0.3s][主角攻击0.4s]
                                                  [A 移动0.5s]
                                                              → 回合结束
```

在同一 Tick 内重叠的动作（如 A 的重攻击与主角的闪避在 Tick 108-126 区间重叠），由 `CombatArbiter` 进行交互判定。

---

## 一、现状分析

### 1.1 已有优秀设计（应保留并强化）

| 设计 | 说明 |
|------|------|
| **BattleTimeline** | 60 Ticks/s 的离散逻辑帧系统，带 `VisualTime` 平滑插值 |
| **CombatIntent 体系** | 所有行动分解为意图，在时间线上调度执行 |
| **TimelinePriority** | 同一 Tick 内按优先级排序执行（System→State→Defense→Attack→Cleanup） |
| **CombatArbiter** | 意图间交互判定（Clash/Parry/Dodge/Intercept/Escape） |
| **CommitMoveStepIntent** | 逻辑位置提交与视觉移动分离的雏形 |
| **Reservation 系统** | 移动中的临时占位，防止重叠移动 |
| **ActionLibrarySO / AttackPattern / UnitVolume** | ScriptableObject 数据驱动的设计 |

### 1.2 需要移除的设计（与新的资源模型冲突）

| 移除项 | 原因 |
|--------|------|
| Stamina 系统（`CurrentStamina`, `MaxStamina`, `StaminaCost`, 体力恢复） | 被时间预算替代 |
| Focus 系统（`CurrentFocus`, `MaxFocus`, `FocusCost`） | 被肾上腺素替代 |
| `ArmorWeight`, `ArmorDefense`, `MagicResistance` | 简化为伤害减免百分比（直接配表） |
| `IsExhausted` 状态 | 体力移除后不再存在 |
| `Swiftness` 属性（体力影响的部分） | 简化为固定速度属性 |

### 1.3 核心问题清单

#### 问题 A：逻辑层直接依赖视觉层

```
PhysicsEngine.ResolveCollision()
  ├── GameFeelManager.Instance.ShowDamageNumber()   ← 伤害数字
  ├── GameFeelManager.Instance.ScreenShake()         ← 屏幕震动
  ├── GameFeelManager.Instance.HitStop()             ← 顿帧
  └── target.GetComponent<UnitBounce>().OnImpact()   ← 弹跳动画

CombatArbiter.ApplyInteraction()
  ├── GameFeelManager.Instance.ShowStatusText()      ← 状态文字
  ├── GameFeelManager.Instance.HitStop()             ← 顿帧
  └── timeline.TriggerSlowMotion()                   ← 慢动作

AttackIntent.ExecuteSuccess()
  └── target.GetComponent<UnitMovement>()            ← 读取视觉位置做命中判定
      └── target.transform.position                   ← 直接读 Transform
```

**后果：** 逻辑不可脱离 Unity 测试；回放时表现层副作用干扰逻辑；新增表现效果需要改逻辑代码。

#### 问题 B：业务逻辑散落在 Update() 中

```
CombatUnit.Update()
  ├── 肾上腺素衰减 (Time.deltaTime)    ← 设计已变更：不再衰减
  └── 体力恢复 (Time.deltaTime)        ← 设计已变更：移除体力

BattleManager.Update()
  └── CheckWinCondition() (每帧 FindObjectsByType)

EnemyAIController.Update()
  ├── 目标搜索 (每 0.1s)
  └── MakeDecision()

UIManager.Update()
  └── 暂停状态轮询
```

**后果：** 逻辑执行频率不统一（有的按帧、有的按秒、有的按 Tick）；无法精确复现；`FindObjectsByType` 性能差。

#### 问题 C：战斗单位状态管理分散

`CombatUnit` 有 8 个 boolean 标志位，无状态机：
```csharp
IsActing, InWindup, InRecovery, IsMoving,
IsStaggered, IsKnockedDown, IsRecoveringAction, IsExhausted(computed)
```

状态转换逻辑散落在 `StateChangeIntent`、`PhysicsEngine`、`ActionScheduler` 中。

#### 问题 D：输入系统通过布尔标志通信

```csharp
// TacticsController 设置
InputManager.Instance.IgnoreUnitClicks = true;

// TacticsController 复位
InputManager.Instance.IgnoreUnitClicks = false;
```

极易遗漏复位，导致输入永久失效。

#### 问题 E：缺少统一事件总线

仅有的几个事件全部挂在 `BattleTimeline` 上：
- `OnScheduleChanged`
- `OnTickProcessed`
- `OnDodgeSuccessRequestMove`

无通用事件分发机制，跨系统通信靠直接调用。

#### 问题 F：Singleton 过量使用

`BattleManager`、`InputManager`、`GridManager`、`UIManager`、`GameFeelManager` 全部是 MonoBehaviour Singleton，强耦合，无法替换或 Mock。

---

## 二、目标架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        表现层 (Visual Layer)                      │
│  UnitMovement, GameFeelManager, GridVisuals, UnitVolumeVisuals   │
│  HUDManager, TimelineEditorUI, ActionButton, GridCursor          │
│  NextActionPreviewSystem, UnitAnimator, UnitBounce              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │              表现事件订阅 (VisualEventBus)                  │  │
│  │  订阅逻辑事件 → 驱动动画/特效/音效/UI                       │  │
│  └────────────────────────────────────────────────────────────┘  │
│                              ↑                                    │
│                        只读 + 事件订阅                            │
│                              ↑                                    │
├──────────────────────────────────────────────────────────────────┤
│                        逻辑层 (Logic Layer)                       │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │  LogicWorld   │  │ LogicEventBus│  │  BattleTimeline      │   │
│  │  (Grid/Occ)   │  │ (事件分发)    │  │  (Tick 驱动)         │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
│                                                                  │
│  ┌──────────────┐  ┌──────────────────────────────────────────┐ │
│  │  TurnManager │  │         逻辑帧循环 (ProcessTick)          │ │
│  │  (回合分配)   │  │  1. 取出当前帧 Intent 队列               │ │
│  └──────────────┘  │  2. 处理输入命令队列                       │ │
│                    │  3. CombatArbiter.Resolve() — 交互判定     │ │
│  ┌──────────────┐  │  4. ExecuteSuccess() — 执行未取消的 Intent │ │
│  │  UnitState   │  │  5. 状态机 Update() — 单位状态推进         │ │
│  │  Machine     │  │  6. 回合管理 Tick() — 时间预算/回合切换    │ │
│  └──────────────┘  │  7. 胜利条件检测                           │ │
│                    │  8. 发射 LogicEvent                         │ │
│  ┌──────────────┐  └──────────────────────────────────────────┘ │
│  │PhysicsEngine │                                                │
│  │(纯计算)       │  ┌──────────────┐  ┌──────────────────────┐   │
│  └──────────────┘  │  AIBrain     │  │  InputCommand        │   │
│                    │  (决策)       │  │  Queue               │   │
│  ┌──────────────┐  └──────────────┘  └──────────────────────┘   │
│  │ ActionSched  │                                                │
│  │ uler         │  ┌──────────────┐  ┌──────────────────────┐   │
│  └──────────────┘  │  CombatArb   │  │  BuffSystem          │   │
│                    │  iter        │  │  (Tick 驱动)          │   │
│                    └──────────────┘  └──────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

### 架构原则

1. **逻辑层零 UnityEngine 依赖**（不含 `MonoBehaviour`、`GameObject`、`Transform`、`Camera`）
2. **所有会改变逻辑状态的代码在 `ProcessTick()` 中执行**，`Update()` / `LateUpdate()` 仅用于输入采样、UI、视觉插值和表现效果
3. **逻辑层只产出事件，不调用表现层**
4. **表现层只订阅事件 + 只读查询逻辑状态，不写入**
5. **输入通过命令队列进入逻辑层**，不在事件回调中直接修改逻辑状态
6. **回合是时间预算的分配机制，不阻塞底层 Tick 运转**
7. **确定性优先于表现即时性**：当逻辑位置与视觉插值不一致时，命中、占位、回放一律以逻辑状态为准

---

## 三、具体优化方案

### 3.0 落地优先级修订

| 优先级 | 目标 | 包含内容 | 不包含内容 |
|------|------|----------|------------|
| **P0-地基** | 让现有战斗能在受控 Tick 骨架下继续运行 | `LogicEventBus`、单位注册表、`UnitStateMachine`、Tick 阶段骨架、表现事件订阅、确定性测试夹具 | 大规模资源模型替换、完整回合系统、纯 C# 世界全量迁移 |
| **P1-玩法核心** | 迁移真正影响战斗结果的系统 | 时间预算、肾上腺素、`TurnManager`、命令队列、`LogicGrid`、AI Tick 化、回放记录 | 高级 Buff、完整序列化、联机准备 |
| **P2-完善** | 降低耦合并补齐长期能力 | `CombatUnitView`、表现层重构、Buff 系统、完整回放/存档、性能基准 | 新玩法扩展 |

**实施原则：** 每一阶段结束时都必须保留一个可运行中间版本；如果一个改动会同时触碰资源、回合、状态机和表现层，应拆成两个阶段。

### 3.1 【P1-重要】资源模型重构

#### 3.1.1 新的 CombatUnitLogic 属性定义

```csharp
public class UnitStats
{
    // 基础属性（简化后）
    public float Strength = 10f;       // 影响物理伤害
    public float Dexterity = 10f;      // 影响动作速度/闪避
    public float Constitution = 10f;   // 影响最大生命值
    public float Wisdom = 10f;         // 影响肾上腺素效率
    public float Intelligence = 10f;   // 影响特殊能力

    // 派生属性
    public float MaxHealth => Constitution * 20f;
    public float ActionSpeed => 10f + Dexterity * 0.5f;  // 替代 Swiftness
    public float DamageReduction => 0f;  // 由装备/Buff 直接提供，不再计算护甲

    // 战斗资源
    public float CurrentHealth = 100f;
    public float TimeBudget = 0f;       // 当前回合剩余时间（秒）
    public float MaxTimeBudget = 3.0f;  // 每回合获得的时间预算
    public float CurrentAdrenaline = 0f;
}

// 肾上腺素规则：
// - 获得：造成伤害 +damage*0.1，受到伤害 +damage*0.2，成功闪避 +5，拼刀 +10
// - 不随时间衰减
// - 在该单位最近一次行动回合结束后归零
```

#### 3.1.2 时间预算的 Tick 消耗

```csharp
// ProcessTick 中的阶段
private void ProcessTurnBudget(long tick)
{
    var activeUnit = _turnManager.CurrentActiveUnit;
    if (activeUnit == null) return;

    // 动作本身通过 Intent 消耗时间预算
    // TimeBudget 在 Intent.ExecuteSuccess() 中扣减
    // 而非每 Tick 自动扣减

    // 检查时间预算是否耗尽
    if (activeUnit.Stats.TimeBudget <= 0 && activeUnit.StateMachine.CanAct)
    {
        _turnManager.EndCurrentTurn(tick);
    }
}
```

#### 3.1.3 动作的时间消耗

```csharp
// 动作消耗时间预算，而非体力
public class Action
{
    public string Name;
    public ActionType Type;
    public float BaseTime;      // 消耗的时间（秒）— 同时决定动作持续时长和时间预算消耗
    public float BaseDamage;
    public ImpactType ImpactType;
    public float ForceMultiplier = 1.0f;
    public AttackPattern Pattern;
    // 移除：StaminaCost — 被 BaseTime 替代
}
```

**设计理由：** 时间即是资源也是成本。一个"重攻击"自然消耗更多时间，这直观且统一。不需要额外的 StaminaCost 来区分动作的成本。

---

### 3.2 【P1-重要】回合管理系统

#### 3.2.1 TurnManager

```csharp
/// <summary>
/// 管理单位轮流行动的回合生命周期。
/// 底层 Tick 持续运转，TurnManager 只控制"当前谁在行动"和"时间预算分配"。
/// </summary>
public class TurnManager
{
    public CombatUnitLogic CurrentActiveUnit { get; private set; }
    public int CurrentRoundNumber { get; private set; } = 0;

    private readonly List<CombatUnitLogic> _turnOrder = new();
    private int _currentTurnIndex = -1;
    private readonly LogicEventBus _eventBus;

    // 事件
    public event Action<CombatUnitLogic> TurnStarted;
    public event Action<CombatUnitLogic> TurnEnded;
    public event Action<int> RoundStarted;

    public TurnManager(LogicEventBus eventBus)
    {
        _eventBus = eventBus;
    }

    /// <summary>
    /// 根据速度属性排序，决定回合顺序
    /// </summary>
    public void BuildTurnOrder(IEnumerable<CombatUnitLogic> units)
    {
        _turnOrder.Clear();
        _turnOrder.AddRange(units.OrderByDescending(u => u.Stats.ActionSpeed));
        // 主角永远先手（设计决策）
        var playerUnit = _turnOrder.FirstOrDefault(u => u.IsPlayerControlled);
        if (playerUnit != null && _turnOrder.IndexOf(playerUnit) != 0)
        {
            _turnOrder.Remove(playerUnit);
            _turnOrder.Insert(0, playerUnit);
        }
    }

    /// <summary>
    /// 开始下一回合。返回是否成功开始（如果战斗已结束则返回 false）。
    /// </summary>
    public bool StartNextTurn(long currentTick)
    {
        _currentTurnIndex++;

        // 跳过已死亡单位
        while (_currentTurnIndex < _turnOrder.Count &&
               _turnOrder[_currentTurnIndex].Stats.CurrentHealth <= 0)
        {
            _currentTurnIndex++;
        }

        // 一轮结束，开始新一轮
        if (_currentTurnIndex >= _turnOrder.Count)
        {
            _currentTurnIndex = 0;
            CurrentRoundNumber++;
            RoundStarted?.Invoke(CurrentRoundNumber);

            // 再次跳过死亡单位
            while (_currentTurnIndex < _turnOrder.Count &&
                   _turnOrder[_currentTurnIndex].Stats.CurrentHealth <= 0)
            {
                _currentTurnIndex++;
            }

            if (_currentTurnIndex >= _turnOrder.Count)
            {
                // 所有单位都死了
                _eventBus.RaiseBattleEnded("ALL_DEAD");
                return false;
            }
        }

        CurrentActiveUnit = _turnOrder[_currentTurnIndex];

        // 重置该单位的上一次肾上腺素
        CurrentActiveUnit.Stats.CurrentAdrenaline = 0;

        // 分配时间预算
        CurrentActiveUnit.Stats.TimeBudget = CurrentActiveUnit.Stats.MaxTimeBudget;

        TurnStarted?.Invoke(CurrentActiveUnit);
        _eventBus.RaiseTurnStarted(CurrentActiveUnit);

        return true;
    }

    /// <summary>
    /// 结束当前回合（时间预算耗尽或手动结束）
    /// </summary>
    public void EndCurrentTurn(long currentTick)
    {
        if (CurrentActiveUnit == null) return;

        // 并发行动权随回合结束而自动结束
        _concurrentActionSystem.OnTurnEnded();

        // 肾上腺素在回合结束后归零
        CurrentActiveUnit.Stats.CurrentAdrenaline = 0;

        var endedUnit = CurrentActiveUnit;
        CurrentActiveUnit = null;

        TurnEnded?.Invoke(endedUnit);
        _eventBus.RaiseTurnEnded(endedUnit);

        StartNextTurn(currentTick);
    }

    /// <summary>
    /// 获得当前回合顺序的快照（用于 UI）
    /// </summary>
    public IReadOnlyList<CombatUnitLogic> GetTurnOrder() => _turnOrder.AsReadOnly();
}
```

#### 3.2.2 回合与时间线的关系

回合不改变 BattleTimeline 的运作方式。BattleTimeline 持续推进 Tick，TurnManager 只是在其上叠加了一个"当前活跃单位"的概念。

**关键洞察：** 即使不是某单位的回合，该单位在时间线上已有的 Intent 仍然会执行。这为主角并发行动能力提供了基础——主角在激活并发行动后，可以在敌方回合内排入 Intent，与敌方动作在时间线上重叠。

```
时间线视角：
  Tick:  0    100   200   300   400   500   600
         [====单位A的回合（预算 3.0s）====]
                [主角并发行动激活]
                ├── 闪避 0.3s ──┤
                ├── 反击 0.4s ──┤
                                                   [==单位B的回合==]
                        ↓
                主角与单位A的动作在同一时间线上重叠
                → CombatArbiter 判定交互
```

#### 3.2.3 并发行动系统

```csharp
/// <summary>
/// 主角的并发行动能力：使用局外资源激活后，主角获得在当前活跃单位回合内行动的权利。
/// 不是"抢回合"或"偷时间"，而是获得在当前回合内的并发行动权。
/// 激活状态下，主角可以排入攻击、移动、格挡、闪避，所有动作消耗当前回合的时间预算。
/// </summary>
public class ConcurrentActionSystem
{
    private readonly TurnManager _turnManager;
    private readonly BattleTimeline _timeline;
    private readonly LogicEventBus _eventBus;

    /// <summary>
    /// 并发行动是否已激活
    /// </summary>
    public bool IsActive { get; private set; }

    /// <summary>
    /// 格挡和闪避是否可用（仅在并发行动激活时）
    /// </summary>
    public bool CanUseDefensiveActions => IsActive;

    /// <summary>
    /// 激活并发行动。只能在非主角回合内激活。
    /// </summary>
    /// <param name="metaResourceCost">消耗的局外资源量</param>
    /// <returns>是否成功激活</returns>
    public bool Activate(int metaResourceCost)
    {
        if (IsActive) return false; // 已经激活

        // 1. 检查局外资源
        if (!HasEnoughMetaResource(metaResourceCost)) return false;

        // 2. 检查当前是否在非主角回合
        var activeUnit = _turnManager.CurrentActiveUnit;
        if (activeUnit == null || activeUnit.IsPlayerControlled) return false;

        // 3. 检查主角本身是否可以行动
        var player = _turnManager.GetPlayerUnit();
        if (player == null || !player.StateMachine.CanAct) return false;

        // 4. 激活
        IsActive = true;
        ConsumeMetaResource(metaResourceCost);

        _eventBus.RaiseConcurrentActionActivated(player, activeUnit);
        return true;
    }

    /// <summary>
    /// 在并发行动模式下排入一个动作。消耗当前回合的时间预算。
    /// </summary>
    public bool ScheduleAction(Action action, GridDirection direction)
    {
        if (!IsActive) return false;

        var player = _turnManager.GetPlayerUnit();
        var activeUnit = _turnManager.CurrentActiveUnit;
        if (player == null || activeUnit == null) return false;

        // 检查时间预算是否足够
        if (activeUnit.Stats.TimeBudget < action.BaseTime) return false;

        // 消耗当前回合的时间预算
        activeUnit.Stats.TimeBudget -= action.BaseTime;

        // 排入时间线（从当前时间开始）
        long groupId = _timeline.ReserveGroupId();
        ActionScheduler.ScheduleAttack(_timeline, player, action, 0f,
            targetDirection: direction, groupId: groupId);

        return true;
    }

    /// <summary>
    /// 在并发行动模式下排入防御动作（格挡/闪避）。
    /// </summary>
    public bool ScheduleDefensive(ActionType defensiveType, float duration)
    {
        if (!IsActive) return false;

        var player = _turnManager.GetPlayerUnit();
        var activeUnit = _turnManager.CurrentActiveUnit;
        if (player == null || activeUnit == null) return false;

        // 检查时间预算
        if (activeUnit.Stats.TimeBudget < duration) return false;

        activeUnit.Stats.TimeBudget -= duration;

        long groupId = _timeline.ReserveGroupId();

        if (defensiveType == ActionType.Block)
            ActionScheduler.ScheduleBlock(_timeline, player, 0f, duration, groupId: groupId);
        else if (defensiveType == ActionType.Dodge)
            ActionScheduler.ScheduleDodge(_timeline, player, 0f, duration, groupId: groupId);

        return true;
    }

    /// <summary>
    /// 回合结束时自动调用的清理。
    /// </summary>
    public void OnTurnEnded()
    {
        if (!IsActive) return;
        IsActive = false;
        _eventBus.RaiseConcurrentActionDeactivated(_turnManager.GetPlayerUnit());
    }

    // 局外资源接口（与 MetaProgressionSystem 集成）
    private bool HasEnoughMetaResource(int cost) { /* ... */ return true; }
    private void ConsumeMetaResource(int cost) { /* ... */ }
}
```

---

### 3.3 【P0-关键】逻辑层表现调用事件化

#### 3.3.1 建立 LogicEventBus（统一事件总线）

```csharp
// 纯 C# 类，不继承 MonoBehaviour
public class LogicEventBus
{
    // 战斗事件
    public event Action<CombatUnitLogic, float, bool, float> DamageDealt;
    // target, damage, isCritical, impactVelocity

    public event Action<InteractionType, CombatUnitLogic, CombatUnitLogic> InteractionResolved;
    // type, attacker, defender

    public event Action<CombatUnitLogic, UnitState, UnitState> UnitStateChanged;
    // unit, fromState, toState

    public event Action<CombatUnitLogic, GridPoint, GridPoint> UnitMoved;
    // unit, from, to

    public event Action<CombatUnitLogic> UnitDefeated;
    public event Action<CombatUnitLogic> UnitStaggered;
    public event Action<CombatUnitLogic> UnitKnockedDown;

    // 回合事件
    public event Action<CombatUnitLogic> TurnStarted;
    public event Action<CombatUnitLogic> TurnEnded;
    public event Action<int> RoundStarted;

    // 并发行动事件
    public event Action<CombatUnitLogic, CombatUnitLogic> ConcurrentActionActivated;
    // player, turnOwner
    public event Action<CombatUnitLogic> ConcurrentActionDeactivated;

    // 时间线/表现事件
    public event Action<float> SlowMotionTriggered;
    public event Action<float> HitStopTriggered;
    public event Action<float, float> ScreenShakeRequested;
    public event Action<string> BattleEnded;
    public event Action<PlayerCommand> CommandRejected;

    public void ClearAllSubscribers()
    {
        DamageDealt = null;
        InteractionResolved = null;
        UnitStateChanged = null;
        UnitMoved = null;
        UnitDefeated = null;
        UnitStaggered = null;
        UnitKnockedDown = null;
        TurnStarted = null;
        TurnEnded = null;
        RoundStarted = null;
        ConcurrentActionActivated = null;
        ConcurrentActionDeactivated = null;
        SlowMotionTriggered = null;
        HitStopTriggered = null;
        ScreenShakeRequested = null;
        BattleEnded = null;
        CommandRejected = null;
    }

    // 便捷发射方法
    public void RaiseDamageDealt(CombatUnitLogic target, float damage, bool isCritical, float impactVelocity)
        => DamageDealt?.Invoke(target, damage, isCritical, impactVelocity);

    public void RaiseInteractionResolved(InteractionType type, CombatUnitLogic attacker, CombatUnitLogic defender)
        => InteractionResolved?.Invoke(type, attacker, defender);

    public void RaiseUnitStateChanged(CombatUnitLogic unit, UnitState from, UnitState to)
        => UnitStateChanged?.Invoke(unit, from, to);

    public void RaiseUnitMoved(CombatUnitLogic unit, GridPoint from, GridPoint to)
        => UnitMoved?.Invoke(unit, from, to);

    public void RaiseTurnStarted(CombatUnitLogic unit)
        => TurnStarted?.Invoke(unit);

    public void RaiseTurnEnded(CombatUnitLogic unit)
        => TurnEnded?.Invoke(unit);

    public void RaiseConcurrentActionActivated(CombatUnitLogic player, CombatUnitLogic turnOwner)
        => ConcurrentActionActivated?.Invoke(player, turnOwner);
    public void RaiseConcurrentActionDeactivated(CombatUnitLogic player)
        => ConcurrentActionDeactivated?.Invoke(player);

    public void RaiseBattleEnded(string message)
        => BattleEnded?.Invoke(message);

    public void RaiseScreenShake(float intensity, float duration)
        => ScreenShakeRequested?.Invoke(intensity, duration);

    public void RaiseHitStop(float duration)
        => HitStopTriggered?.Invoke(duration);

    public void RaiseSlowMotion(float scale)
        => SlowMotionTriggered?.Invoke(scale);

    public void RaiseCommandRejected(PlayerCommand cmd, string reason)
        => CommandRejected?.Invoke(cmd);
}
```

#### 3.3.2 逻辑层所有表现调用改为事件发射

**改造前（PhysicsEngine）：**
```csharp
if (GameFeelManager.Instance != null)
{
    GameFeelManager.Instance.ShowDamageNumber(target.transform.position, damage, isBigHit);
    GameFeelManager.Instance.ScreenShake(intensity, 0.3f);
    GameFeelManager.Instance.HitStop(0.1f);
}
var targetBounce = target.GetComponent<UnitBounce>();
if (targetBounce != null) targetBounce.OnImpact(intensity);
```

**改造后（PhysicsEngine）：**
```csharp
// 逻辑层只发射事件
_eventBus.RaiseDamageDealt(target, damage, isBigHit, impactVelocity);
if (intensity > 0.2f)
{
    _eventBus.RaiseScreenShake(intensity * 0.5f, 0.1f + intensity * 0.3f);
    _eventBus.RaiseHitStop(0.05f + intensity * 0.1f);
}
// 表现层订阅这些事件，自行处理 UI、特效、震屏、顿帧等
```

**改造前（CombatArbiter）：**
```csharp
if (GameFeelManager.Instance)
{
    GameFeelManager.Instance.HitStop(0.1f);
    GameFeelManager.Instance.ShowStatusText(pDef.Owner.transform.position, "PARRY", Color.cyan);
}
```

**改造后（CombatArbiter）：**
```csharp
_eventBus.RaiseInteractionResolved(InteractionType.Parry, attacker, defender);
_eventBus.RaiseHitStop(0.1f);
// 附带关键数据：位置通过 Unit.GridPosition → LogicGrid.GridToWorld() 计算
```

#### 3.3.3 消除 AttackIntent 中的视觉位置依赖

**问题代码：**
```csharp
// AttackIntent.ExecuteSuccess() 中
var movement = target.GetComponent<UnitMovement>();
if (movement != null && movement.IsMoving)
{
    var visualGrid = GridManager.Instance.WorldToGrid(target.transform.position);  // ← 读 Transform
    var visualOcc = target.GetProjectedOccupancy(visualGrid, target.FacingDirection);
    // 用视觉位置做命中判定 ← 这是根本性错误
}
```

**修正方案：** 命中判定必须基于逻辑时间点的确定状态。

逻辑位置在 Tick 边界上切换。在同一 Tick 内：
- `MoveIntent.ExecuteSuccess()` 启动视觉移动，但不改逻辑位置
- `CommitMoveStepIntent.ExecuteSuccess()` 在延时至移动结束后才改逻辑位置
- **`AttackIntent` 判定时，目标的逻辑位置就是其当前 `GridPosition`**

```csharp
// 正确的判定方式：只用逻辑数据
public override void ExecuteSuccess()
{
    var area = ActionDefinition.Pattern.GetAffectedTriangles(Owner.GridPosition, Owner.FacingDirection);
    var targets = GridManager.Instance.GetUnitsInArea(area, Owner);
    foreach (var target in targets)
    {
        // 目标的逻辑 Occupancy 就是此刻的 GridPosition + Volume
        // 无需检查视觉位置
        PhysicsEngine.ResolveCollision(_timeline, Owner, target, ActionDefinition);
    }
}
```

如果一个单位正在移动中（`IsMoving == true`），它的逻辑 Occupancy 仍在旧位置，而 Reservation 在新路径上。Attack 命中旧位置是正确行为——这代表在单位完全离开前攻击到达。

---

### 3.4 【P0-关键】所有逻辑状态迁移到 Tick 系统

#### 3.4.0 `Update()` 的保留边界

`Update()` / `LateUpdate()` 仍可用于以下职责：

- 输入设备采样：读取鼠标、键盘、手柄，生成 `PlayerCommand`
- UI 刷新：显示时间线、按钮状态、暂停菜单
- 视觉插值：根据逻辑状态和 `VisualTime` 平滑移动模型
- 相机、特效、音效、伤害数字、震屏、顿帧等表现反馈

禁止在 `Update()` / `LateUpdate()` 中执行以下职责：

- 修改单位血量、资源、状态、位置、朝向
- 执行命中、格挡、闪避、碰撞、胜负判定
- 调度或取消会影响战斗结果的 Intent
- 让 AI 基于 `Time.time` / `Time.unscaledTime` 做决策

**判断标准：** 如果一段代码会改变回放结果，它必须进入 `ProcessTick()`；如果只改变玩家看到的表现，它可以留在 Unity 生命周期中。

#### 3.4.1 CombatUnit 的资源管理移出 Update()

**改造前：**
```csharp
private void Update()
{
    float dt = Time.deltaTime;
    // 肾上腺素衰减    ← 设计变更：不再衰减
    // 体力恢复         ← 设计变更：移除体力
    CurrentAdrenaline -= 5f * dt;
    CurrentStamina += regenRate * dt;
}
```

**改造后：**
```csharp
// CombatUnitLogic — 纯 C# 类，不再继承 MonoBehaviour
public class CombatUnitLogic
{
    public UnitStats Stats;
    public UnitStateMachine StateMachine;

    // 肾上腺素不再有每 Tick 衰减
    // 时间预算由 TurnManager 和 Intent 执行管理
    // 回合结束时的肾上腺素归零由 TurnManager.EndCurrentTurn() 处理

    public void OnTick(long tick)
    {
        // 目前无需每 Tick 更新（资源不再自动恢复）
        // 保留此方法作为扩展点：
        // - Buff/Debuff 的 Tick 效果（由 BuffSystem 处理）
        // - 状态机的持续时间检查
        StateMachine.CheckTimedTransition(tick);
    }
}
```

`CombatUnit` 作为过渡期 MonoBehaviour 可以暂时保留注册、绑定和表现桥接职责，但这些职责不得修改 `CombatUnitLogic` 的战斗状态。最终由 `CombatUnitView` 替代它对表现层的引用。

#### 3.4.2 战斗胜利条件检测移入 Tick

**改造前（BattleManager.Update）：**
```csharp
private void Update()
{
    CheckWinCondition(); // 每帧 FindObjectsByType → 极低效
}
```

**改造后：** 作为 `ProcessTick()` 的一个阶段

```csharp
// 在 ProcessTick 中按需调用
private void CheckWinCondition()
{
    bool playerAlive = false, enemyAlive = false;
    foreach (var unit in _activeUnits)
    {
        if (unit.Stats.CurrentHealth <= 0) continue;
        if (unit.IsPlayerControlled) playerAlive = true;
        else enemyAlive = true;
    }

    if (!playerAlive || !enemyAlive)
    {
        _battleEnded = true;
        _eventBus.RaiseBattleEnded(!playerAlive ? "DEFEAT" : "VICTORY");
    }
}
```

#### 3.4.3 改进后的 ProcessTick 完整流程

```csharp
private void ProcessTick(long tick)
{
    // 阶段 0: 输入命令处理
    _commandProcessor.ProcessCommands(tick);

    // 阶段 1: 单位 Tick（状态机、Buff）
    foreach (var unit in _activeUnits)
    {
        unit.OnTick(tick);
    }
    _buffSystem.TickAll(tick);

    // 阶段 2: 取出当前帧 Intent
    _frameIntents.Clear();
    while (_events.Count > 0 && _events[0].Tick <= tick)
    {
        _frameIntents.Add(_events[0].Intent);
        _events.RemoveAt(0);
    }

    // 阶段 3: 仲裁
    if (_frameIntents.Count > 0)
    {
        CombatArbiter.Resolve(_frameIntents, this, _eventBus);
    }

    // 阶段 4: 执行
    foreach (var intent in _frameIntents)
    {
        if (!intent.IsCancelled)
            intent.ExecuteSuccess();
    }

    // 阶段 5: AI 决策 (按固定 Tick 间隔)
    if (tick % AI_DECISION_INTERVAL == 0)
    {
        foreach (var ai in _aiControllers)
            ai.Think(tick);
    }

    // 阶段 6: 回合管理 — 检查当前活跃单位的时间预算
    _turnManager.CheckTimeBudget(tick);

    // 阶段 7: 胜利条件 (按固定 Tick 间隔)
    if (tick % WIN_CHECK_INTERVAL == 0 || _frameIntents.Count > 0)
    {
        CheckWinCondition();
    }

    // 阶段 8: 发射帧完成事件
    if (_frameIntents.Count > 0)
    {
        OnTickProcessed?.Invoke(tick, _frameIntents.ToArray());
    }

    // 阶段 9: 清理死亡单位
    ProcessDeaths(tick);
}
```

---

### 3.5 【P0-关键】引入单位状态机

状态机必须早于资源模型和回合系统全量迁移。当前 `IsActing`、`InWindup`、`InRecovery`、`IsMoving`、`IsStaggered`、`IsKnockedDown` 等 bool 标志会让 Tick 化和事件化产生大量临时兼容逻辑；先收敛为单一状态枚举，可以显著降低后续改造的返工。

#### 3.5.1 状态定义

```csharp
public enum UnitState
{
    Idle,           // 可接受指令
    Windup,         // 攻击前摇
    Recovery,       // 攻击后摇
    Moving,         // 移动中
    Blocking,       // 格挡中
    Dodging,        // 闪避中
    Staggered,      // 硬直
    KnockedDown,    // 击倒
    Recovering,     // 起身恢复中
    Dead            // 死亡
}

public class UnitStateMachine
{
    public UnitState CurrentState { get; private set; } = UnitState.Idle;
    public UnitState PreviousState { get; private set; } = UnitState.Idle;
    public long StateStartTick { get; private set; }
    public long StateEndTick { get; private set; }

    public bool CanAct => CurrentState == UnitState.Idle;

    /// <summary>
    /// 单位是否可以在并发行动中被针对（例如被格挡/闪避反制）
    /// </summary>
    public bool IsTargetableByConcurrentAction =>
        CurrentState is UnitState.Idle
                     or UnitState.Windup
                     or UnitState.Recovery
                     or UnitState.Moving;

    public bool TransitionTo(UnitState newState, long currentTick, long durationTicks = 0)
    {
        if (!IsValidTransition(CurrentState, newState)) return false;
        PreviousState = CurrentState;
        CurrentState = newState;
        StateStartTick = currentTick;
        StateEndTick = durationTicks > 0 ? currentTick + durationTicks : long.MaxValue;
        return true;
    }

    /// <summary>
    /// 检查定时状态是否到期（如硬直持续 N Ticks 后自动恢复）
    /// </summary>
    public void CheckTimedTransition(long currentTick)
    {
        if (StateEndTick != long.MaxValue && currentTick >= StateEndTick)
        {
            if (CurrentState == UnitState.Staggered || CurrentState == UnitState.KnockedDown)
            {
                // 硬直到期不会自动恢复——需要主动 Recover
                // 设计决策：被击倒后必须手动恢复
            }
            else
            {
                TransitionTo(UnitState.Idle, currentTick);
            }
        }
    }

    private static bool IsValidTransition(UnitState from, UnitState to)
    {
        return (from, to) switch
        {
            // Idle 可以去任何主动状态
            (UnitState.Idle, _) => true,
            // 任何状态可以去 Idle（恢复）
            (_, UnitState.Idle) => true,
            // 任何状态可以去 Dead
            (_, UnitState.Dead) => true,
            // Staggered/KnockedDown 只能去 Recovering
            (UnitState.Staggered, UnitState.Recovering) => true,
            (UnitState.KnockedDown, UnitState.Recovering) => true,
            (UnitState.Recovering, UnitState.Idle) => true,
            // 不允许的转换会返回 false
            _ => false
        };
    }

    public float GetProgress(long currentTick)
    {
        if (StateEndTick == long.MaxValue || StateStartTick == StateEndTick) return 0f;
        long total = StateEndTick - StateStartTick;
        long elapsed = currentTick - StateStartTick;
        return Math.Clamp((float)elapsed / total, 0f, 1f);
    }
}
```

#### 3.5.2 改造 StateChangeIntent

```csharp
// 不再直接设置 bool 标志
public override void ExecuteSuccess()
{
    var timeline = BattleTimeline.Instance;
    long durationTicks = Mathf.RoundToInt(DurationSeconds * BattleTimeline.TicksPerSecond);
    Owner.StateMachine.TransitionTo(_targetState, timeline.CurrentTick, durationTicks);
    _eventBus.RaiseUnitStateChanged(Owner, oldState, _targetState);
}
```

---

### 3.6 【P1-重要】输入系统改为命令队列模式

#### 3.6.1 命令定义

```csharp
public abstract class PlayerCommand
{
    public CombatUnitLogic Unit;
    public long IssuedAtTick;
}

public class SelectUnitCommand : PlayerCommand { }
public class DeselectCommand : PlayerCommand { }
public class MoveCommand : PlayerCommand
{
    public GridPoint Destination;
}
public class AttackCommand : PlayerCommand
{
    public Action Action;
    public GridDirection Facing;
}
public class BlockCommand : PlayerCommand
{
    public float Duration;
}
public class DodgeCommand : PlayerCommand
{
    public float Duration;
}
public class RecoverCommand : PlayerCommand { }
public class EndTurnCommand : PlayerCommand { }

/// <summary>
/// 激活并发行动 — 花费局外资源，获得在当前回合内行动的权利。
/// </summary>
public class ActivateConcurrentCommand : PlayerCommand
{
    public int MetaResourceCost;
}

/// <summary>
/// 在并发行动模式下排入攻击动作。
/// </summary>
public class ConcurrentAttackCommand : PlayerCommand
{
    public Action Action;
    public GridDirection Facing;
}

/// <summary>
/// 在并发行动模式下排入防御动作（仅在并发行动激活时可用）。
/// </summary>
public class ConcurrentDefendCommand : PlayerCommand
{
    public ActionType DefensiveType; // Block 或 Dodge
    public float Duration;
}
```

#### 3.6.2 命令处理

```csharp
public class PlayerCommandProcessor
{
    private readonly Queue<PlayerCommand> _pendingCommands = new();
    private readonly BattleTimeline _timeline;
    private readonly LogicEventBus _eventBus;
    private readonly TurnManager _turnManager;
    private readonly ConcurrentActionSystem _concurrentActionSystem;

    public void EnqueueCommand(PlayerCommand command)
    {
        command.IssuedAtTick = _timeline.CurrentTick;
        _pendingCommands.Enqueue(command);
    }

    // 在 ProcessTick 的阶段 0 调用
    public void ProcessCommands(long currentTick)
    {
        while (_pendingCommands.Count > 0)
        {
            var cmd = _pendingCommands.Dequeue();
            ExecuteCommand(cmd, currentTick);
        }
    }

    private void ExecuteCommand(PlayerCommand cmd, long tick)
    {
        switch (cmd)
        {
            case ActivateConcurrentCommand activate:
                // 激活并发行动 — 不检查"是否你的回合"
                if (_concurrentActionSystem.IsActive)
                {
                    _eventBus.RaiseCommandRejected(cmd, "Concurrent action already active");
                    return;
                }
                _concurrentActionSystem.Activate(activate.MetaResourceCost);
                break;

            case ConcurrentAttackCommand conAtk:
                // 并发攻击 — 必须在并发行动激活状态下
                if (!_concurrentActionSystem.IsActive)
                {
                    _eventBus.RaiseCommandRejected(cmd, "Concurrent action not active");
                    return;
                }
                _concurrentActionSystem.ScheduleAction(conAtk.Action, conAtk.Facing);
                break;

            case ConcurrentDefendCommand conDef:
                // 并发防御 — 格挡/闪避，仅在并发行动激活时可用
                if (!_concurrentActionSystem.IsActive)
                {
                    _eventBus.RaiseCommandRejected(cmd, "Concurrent action not active");
                    return;
                }
                _concurrentActionSystem.ScheduleDefensive(conDef.DefensiveType, conDef.Duration);
                break;

            default:
                // 常规命令 — 必须在单位自己的回合内
                if (!_turnManager.IsCurrentUnit(cmd.Unit))
                {
                    _eventBus.RaiseCommandRejected(cmd, "Not your turn");
                    return;
                }
                if (!cmd.Unit.StateMachine.CanAct)
                {
                    _eventBus.RaiseCommandRejected(cmd, "Unit cannot act");
                    return;
                }

                switch (cmd)
                {
                    case AttackCommand attack:
                        attack.Unit.Stats.TimeBudget -= attack.Action.BaseTime;
                        ActionScheduler.ScheduleAttack(_timeline, attack.Unit,
                            attack.Action, 0f, attack.Facing);
                        break;

                    case MoveCommand move:
                        var path = FindPath(move.Unit, move.Destination);
                        float moveDuration = ActionScheduler.EstimateMoveDuration(move.Unit, path);
                        move.Unit.Stats.TimeBudget -= moveDuration;
                        ActionScheduler.ScheduleMoveTo(_timeline, move.Unit,
                            move.Destination);
                        break;

                    case EndTurnCommand:
                        _turnManager.EndCurrentTurn(tick);
                        break;

                    // 注意：BlockCommand 和 DodgeCommand 不在常规回合内处理
                    // 它们只能通过 ConcurrentDefendCommand 在并发行动中执行
                }
                break;
        }
    }
}
```

#### 3.6.3 TacticsController 改造

```csharp
// 不再直接设置 IgnoreUnitClicks 布尔标志
// 而是通过输入模式状态机管理

public enum InputMode { Normal, TargetSelection, TimelinePlacement, DodgeCounter }

public class InputModeManager
{
    private InputMode _currentMode = InputMode.Normal;
    private readonly Stack<InputMode> _modeStack = new();

    public void PushMode(InputMode mode)
    {
        _modeStack.Push(_currentMode);
        _currentMode = mode;
    }

    public void PopMode()
    {
        _currentMode = _modeStack.Count > 0 ? _modeStack.Pop() : InputMode.Normal;
    }

    public InputMode CurrentMode => _currentMode;
}
```

---

### 3.7 【P1-重要】逻辑世界与视觉世界分离

#### 3.7.1 双层 CombatUnit

```csharp
// 逻辑层 — 纯 C# 数据类
public class CombatUnitLogic
{
    public string UnitId;
    public bool IsPlayerControlled;
    public GridPoint GridPosition;
    public GridDirection FacingDirection;
    public UnitStateMachine StateMachine;
    public UnitStats Stats;
    public UnitVolumeDefinition VolumeDef;
    public ActionLibrarySO ActionLibrary;
}

// 表现层 — MonoBehaviour 桥接
public class CombatUnitView : MonoBehaviour
{
    public CombatUnitLogic Logic { get; private set; }

    private UnitMovement _movement;
    private UnitAnimator _animator;

    public void Bind(CombatUnitLogic logic)
    {
        Logic = logic;
        SyncVisualPosition();
    }

    public void OnLogicEvent(LogicEvent evt)
    {
        switch (evt)
        {
            case UnitMovedEvent move:
                _movement.PlayMove(move.From, move.To, move.Duration);
                break;
            case DamageDealtEvent dmg:
                _animator.PlayHitReaction(dmg.Intensity);
                break;
            case StateChangedEvent state:
                _animator.TransitionTo(state.NewState);
                break;
        }
    }

    private void LateUpdate()
    {
        // 视觉插值跟随逻辑位置
        if (Logic.StateMachine.CurrentState == UnitState.Moving)
            _movement.UpdateVisualInterpolation(Logic);
        else
            SyncVisualPosition();
    }
}
```

#### 3.7.2 消除 FindObjectsByType 和 GetComponent 查询

所有逻辑层的 Unit 引用通过 `LogicWorld` 集中管理：

```csharp
public class LogicWorld
{
    private readonly Dictionary<string, CombatUnitLogic> _units = new();
    private readonly List<CombatUnitLogic> _playerUnits = new();
    private readonly List<CombatUnitLogic> _enemyUnits = new();

    public void RegisterUnit(CombatUnitLogic unit)
    {
        _units[unit.UnitId] = unit;
        (unit.IsPlayerControlled ? _playerUnits : _enemyUnits).Add(unit);
    }

    public void UnregisterUnit(string unitId)
    {
        if (_units.TryGetValue(unitId, out var unit))
        {
            _units.Remove(unitId);
            (unit.IsPlayerControlled ? _playerUnits : _enemyUnits).Remove(unit);
        }
    }

    public IReadOnlyList<CombatUnitLogic> PlayerUnits => _playerUnits;
    public IReadOnlyList<CombatUnitLogic> EnemyUnits => _enemyUnits;
    public CombatUnitLogic GetUnit(string id) => _units.GetValueOrDefault(id);

    public IEnumerable<CombatUnitLogic> AllAlive =>
        _units.Values.Where(u => u.Stats.CurrentHealth > 0);
}
```

---

### 3.8 【P1-重要】网格系统逻辑化

网格逻辑化需要提前到 P1。命中判定、占位、移动 Reservation、AI 寻路、回放快照都依赖同一份确定性网格数据；如果继续让核心逻辑依赖 `GridManager.Instance` 和 Unity 世界坐标，纯逻辑测试与回放会被卡住。

#### 3.8.1 GridManager 拆分

```csharp
// 逻辑层 — 纯数据网格
public class LogicGrid
{
    private Dictionary<TrianglePoint, string> _occupancyMap = new();     // point → unitId
    private Dictionary<TrianglePoint, string> _reservationMap = new();   // point → unitId

    public void RegisterOccupancy(string unitId, List<TrianglePoint> volume) { }
    public void UnregisterOccupancy(string unitId) { }
    public bool IsOccupied(TrianglePoint point, string ignoreUnitId = null) { }
    public HashSet<string> GetUnitsInArea(List<TrianglePoint> area, string ignoreUnitId = null) { }

    // 坐标转换（纯数学，不依赖 Unity）
    public static Vector3Pure GridToWorld(GridPoint gp, float hexSize) { }
    public static GridPoint WorldToGrid(Vector3Pure wp, float hexSize) { }
}

// 表现层 — MonoBehaviour 桥接
public class GridView : MonoBehaviour
{
    public LogicGrid LogicGrid { get; private set; }
    public float HexSize = 1.0f;

    public Vector3 GridToWorld(GridPoint gp) { /* 使用 Unity Vector3 */ }
    public GridPoint WorldToGrid(Vector3 wp) { /* ... */ }
    public TrianglePoint WorldToTriangle(Vector3 wp) { /* ... */ }

    private void OnDrawGizmos() { /* 可视化逻辑网格的 Occupancy */ }
}
```

---

### 3.9 【P1-重要】AI 系统 Tick 化

```csharp
public class AIControllerLogic  // ← 纯 C# 类
{
    public CombatUnitLogic ControlledUnit;
    public CombatUnitLogic TargetUnit;

    private long _nextThinkTick;
    private const long THINK_INTERVAL_TICKS = 6;  // 0.1s @ 60 Ticks/s

    public void Think(long currentTick)
    {
        if (currentTick < _nextThinkTick) return;
        _nextThinkTick = currentTick + THINK_INTERVAL_TICKS;

        // 只有在该 AI 单位的回合内才思考
        // （除非 AI 有被动反应能力——未来扩展）
        if (!_turnManager.IsCurrentUnit(ControlledUnit)) return;

        if (ControlledUnit.StateMachine.CurrentState == UnitState.Staggered
            || ControlledUnit.StateMachine.CurrentState == UnitState.KnockedDown)
        {
            // 硬直状态 → 尝试恢复
            if (ControlledUnit.Stats.TimeBudget > ActionScheduler.EstimateRecoverDuration())
            {
                ScheduleRecovery();
            }
            return;
        }

        if (!ControlledUnit.StateMachine.CanAct) return;
        if (ControlledUnit.Stats.TimeBudget <= 0) return;

        if (TryScheduleAttack()) return;
        if (TryScheduleMovement()) return;

        // 无事可做，结束回合
        _turnManager.EndCurrentTurn(currentTick);
    }
}
```

---

### 3.10 【P2-改善】Buff / 状态效果系统

```csharp
public abstract class StatusEffect
{
    public string Id;
    public long AppliedAtTick;
    public long DurationTicks;

    public bool IsExpired(long currentTick) => currentTick >= AppliedAtTick + DurationTicks;

    public abstract void OnApply(CombatUnitLogic unit);
    public abstract void OnTick(CombatUnitLogic unit, long currentTick);
    public abstract void OnRemove(CombatUnitLogic unit);
}

public class BuffSystem
{
    private readonly Dictionary<string, List<StatusEffect>> _unitBuffs = new();

    public void ApplyBuff(CombatUnitLogic unit, StatusEffect effect, long currentTick)
    {
        effect.AppliedAtTick = currentTick;
        effect.OnApply(unit);
        if (!_unitBuffs.ContainsKey(unit.UnitId))
            _unitBuffs[unit.UnitId] = new List<StatusEffect>();
        _unitBuffs[unit.UnitId].Add(effect);
    }

    public void TickAll(long tick)
    {
        foreach (var (unitId, buffs) in _unitBuffs)
        {
            for (int i = buffs.Count - 1; i >= 0; i--)
            {
                buffs[i].OnTick(_logicWorld.GetUnit(unitId), tick);
                if (buffs[i].IsExpired(tick))
                {
                    buffs[i].OnRemove(_logicWorld.GetUnit(unitId));
                    buffs.RemoveAt(i);
                }
            }
        }
    }
}
```

---

### 3.11 【P1/P2】回放记录与序列化支持

```csharp
public class BattleReplay
{
    // 记录每个 Tick 的输入命令
    public List<TickRecord> Records = new();

    public struct TickRecord
    {
        public long Tick;
        public List<PlayerCommand> Commands;
        public List<LogicEvent> Events;
    }

    public void Record(long tick, List<PlayerCommand> commands, List<LogicEvent> events)
    {
        Records.Add(new TickRecord { Tick = tick, Commands = commands, Events = events });
    }

    // 回放时：重新推进 Timeline，在相同 Tick 喂入相同命令
    // 因为逻辑是确定性的，结果必然一致
    // RNG 种子固定即可确保 AI 决策一致
}
```

#### 3.11.1 确定性验收标准

回放不是最后才补的功能，而是每阶段重构的验收工具。至少需要记录以下快照：

```csharp
public struct LogicSnapshot
{
    public long Tick;
    public List<UnitSnapshot> Units;      // UnitId, GridPosition, Facing, Health, Resources, State
    public List<string> ScheduledIntents; // Tick, OwnerId, Type, Priority, GroupId
    public List<string> Events;           // 本 Tick 发出的逻辑事件
    public uint RngState;
}
```

每次重构后运行同一条命令序列：

1. 固定初始场景、固定 RNG 种子
2. 输入相同 `PlayerCommand` 序列
3. 推进相同 Tick 数
4. 对比最终 `LogicSnapshot`
5. 对比关键 Tick 的事件日志

只要快照不一致，就说明改动影响了战斗结果，必须明确这是预期玩法变更还是重构回归。

---

## 四、分阶段实施路线

### 第一阶段：设计定稿 + 基线验收（本周）

| 任务 | 产出 | 方式 |
|------|------|------|
| 资源模型定稿（时间 + 肾上腺素） | 设计文档 | 只定规则，不立刻删除旧字段 |
| 回合结构定稿（规划阶段 vs 执行阶段） | 设计文档 | 只定接口与状态流 |
| 交互判定矩阵定稿 | 设计文档 | 明确同 Tick 多方冲突排序 |
| 并发行动能力接口定义 | 接口文档 | 与 meta-progression 系统对齐 |
| 当前战斗基线记录 | 快照/日志 | 记录 1-2 个典型战斗片段 |
| **纯 C# 最小原型验证** | 可运行原型 | 验证 Timeline + Arbiter + TurnBudget 核心循环 |

### 第二阶段：P0 地基与兼容壳（1 周）

| 任务 | 产出 | 影响范围 |
|------|------|----------|
| 创建 `LogicEventBus` | 事件总线类 | 新增文件 |
| 创建 `UnitRegistry` / `LogicWorld` 最小壳 | 缓存单位列表 | 替换高频 `FindObjectsByType` |
| 创建 `UnitStateMachine` | 状态机类 | 先桥接旧 bool，不改玩法 |
| 扩展 `BattleTimeline.ProcessTick()` 阶段 | Tick 骨架 | 命令、单位 Tick、Intent、胜负、清理 |
| 创建 `VisualEventSubscriber` | 表现订阅器 | 新增文件 |
| 创建 `LogicSnapshot` 测试夹具 | 快照对比工具 | 新增测试/调试代码 |

**此阶段以新增和桥接为主，不删除 Stamina / Focus，不重写回合系统。**

### 第三阶段：逻辑层去表现化 + Tick 边界收敛（1-2 周）

| 任务 | 说明 |
|------|------|
| 重构 `PhysicsEngine.ResolveCollision()` | 移除所有 `GameFeelManager` 调用，改为发射事件 |
| 重构 `CombatArbiter.ApplyInteraction()` | Parry/Dodge/Clash 改为发射事件 |
| 重构 `AttackIntent.ExecuteSuccess()` | 移除 `GetComponent<UnitMovement>` 和 `transform.position` 依赖 |
| 重构 `CombatUnit.OnImpact()` | 只修改逻辑状态并发射事件，不直接处理表现 |
| `BattleManager.Update()` → Tick 阶段 | 胜负判定不再每帧扫描 |
| `EnemyAIController.Update()` → Tick 间隔决策 | AI 不再依赖 `Time.unscaledTime` |
| 明确保留的 `Update()` | 输入、UI、视觉插值、特效 |

### 第四阶段：状态机、命令队列、逻辑网格（1-2 周）

| 任务 | 说明 |
|------|------|
| `CombatUnit` 状态标志位 → `UnitStateMachine` | 8 个 bool → 1 个 enum |
| `StateChangeIntent` 改用状态机 | 集中状态校验 |
| `TacticsController` 改用命令队列 | 移除 `IgnoreUnitClicks` 标志 |
| 创建 `PlayerCommand` 体系 + `CommandProcessor` | 输入在 Tick 阶段消费 |
| 在 `GridManager` 中分离 `LogicGrid` | 占位、Reservation、命中区域改用纯逻辑数据 |
| 添加快照回归用例 | 验证状态机/命令/网格迁移不改变既有结果 |

### 第五阶段：资源模型与回合系统迁移（1-2 周）

| 任务 | 说明 |
|------|------|
| 移除 `CombatUnit` 中的体力/专注玩法依赖 | 字段可短期保留，逻辑不再读取 |
| 引入 `TimeBudget` | 动作消耗时间预算，而非体力 |
| 引入新 `Adrenaline` 生命周期 | 不随时间衰减，按回合结束规则归零 |
| 创建 `TurnManager` | 当前活跃单位、回合预算、回合切换 |
| 创建 `ConcurrentActionSystem` | 主角并发行动权、共享时间预算 |
| AI 接入回合/预算规则 | AI 不再自行用冷却时间模拟回合 |

### 第六阶段：表现层重构、回放与性能基准（1-2 周）

| 任务 | 说明 |
|------|------|
| 创建 `CombatUnitView` | 表现层桥接 |
| 重构 `UnitMovement` | 改为纯视觉插值 |
| 重构 `GameFeelManager` | 改为事件驱动 |
| 添加 `UnitAnimator` | 状态机驱动的动画控制器 |
| 添加 `BuffSystem` | Tick 驱动的 Buff/Debuff |
| 完整回放/序列化 | 命令日志 + 事件日志 + 快照校验 |
| 性能基准对比 | Tick 耗时、GC、单位数量扩展测试 |

### 每阶段验收标准

| 验收项 | 标准 |
|------|------|
| 可运行性 | 主战斗场景可进入、可行动、可结束 |
| 快照一致性 | 非玩法变更阶段的固定命令序列输出一致 |
| 表现兼容 | 伤害数字、震屏、移动插值、UI 不丢失 |
| 性能 | 不新增高频 `FindObjectsByType` / `GetComponent` 热路径 |
| 依赖方向 | Core 逻辑不新增视觉层引用 |

---

## 五、架构收益总结

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| **战斗资源** | 体力 + 专注 + 肾上腺素 + 时间（混乱） | 时间 + 肾上腺素（清晰） |
| **回合模型** | 伪实时（Update 驱动 AI） | 回合制表面 + 实时底层 |
| **主角并发行动** | 不支持 | 原生支持（ConcurrentActionSystem） |
| **逻辑可测试性** | 必须运行 Unity 场景 | 纯 C# 单元测试 |
| **回放确定性** | 受帧率、Time.timeScale 影响 | 完全确定性 |
| **逻辑帧一致性** | 部分 Update 部分 Tick | 战斗结果全部 Tick 驱动 |
| **表现层独立性** | 逻辑调用表现 API | 事件驱动，单向依赖 |
| **输入健壮性** | 布尔标志，易遗漏 | 状态机 + 命令队列 |
| **调试体验** | 状态散落在 8 个 bool 中 | 单一状态枚举 + 转换日志 |
| **网络化准备** | 不可行 | 命令同步即可实现联机 |
| **性能** | 每帧 FindObjectsByType | 缓存列表 + 按需检测 |

---

## 六、风险与注意事项

1. **渐进式重构**：不推倒重来。每个阶段产出独立可运行的中间版本。
2. **表现层兼容**：在 `CombatUnitView.Bind()` 阶段，可以同时保留旧的 `CombatUnit` 字段作为桥接，逐步迁移。
3. **`Update()` 边界**：允许输入、UI、视觉插值留在 Unity 生命周期中，但不得修改战斗结果。
4. **确定性基线**：每阶段都要跑固定命令序列和快照对比，避免“看起来没坏”的隐性回归。
5. **性能基准**：每次重构后进行性能对比，确保 Tick 驱动不引入额外开销。
6. **同 Tick 多方仲裁**：多人同帧交互必须有稳定排序规则，避免 pairwise 顺序依赖。
7. **团队共识**：建议团队评审此方案后，选定各阶段的优先级和排期。
8. **局外资源接口**：主角并发行动能力消耗的"局外资源"需要在 meta-progression 系统中明确定义接口，避免耦合。
9. **RNG 确定性**：AI 决策和随机元素需要使用固定种子的 RNG，确保回放一致性。

---

*方案编写日期：2026-07-04*
*更新时间：2026-07-07（收紧 P0 范围、明确 Update 边界、前置状态机/逻辑网格、补充确定性验收）*
