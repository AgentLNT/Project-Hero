# 01 - 基线、清单与不变量测试 —— 规范基线记录（合并版）

> 状态：**完成**（静态基线 + 编译验证全部交付；运行时片段为静态推导，已按总则诚实标注待运行时验证，未伪造通过）
> 执行时间：2026-09-22。执行方式：两名专家并行——「Unity 架构师」负责运行时/代码链静态分析（全程只读，未启动 Editor）；「Unity 编辑器工具开发者」负责资产配置/程序集盘点与 batchmode 编译验证（唯一启动 Editor 者）。
> 本文档是任务 01「必须产出」第 1 项要求的规范记录。详细证据位于两份分工记录：
> - `优化任务/执行记录/01-基线记录-运行时与代码链.md`（716 行，代码语义/数值链/不变量用例/终态地图/敌我/寻路/击退/性能框架）
> - `优化任务/执行记录/01-基线记录-资产配置与程序集.md`（394 行，资产 GUID/字段/引用/同名 ID/asmdef/Authoring 闭包/伤害盘点/编译验证）
> - `优化任务/执行记录/01-诊断输出.txt`（1206 行原始诊断 dump，10 小节 0 错误）

## 1. 环境事实（已复核）

| 项 | 值 |
|---|---|
| Unity | 6000.6.2f1（revision 770e33f6875c），URP 17.6.0，test-framework 1.8.0 |
| 项目路径 | `C:\Users\1\repos\Project Hero\Project-Hero`（git 仓库，master，任务开始/结束均无既有文件改动） |
| 程序集 | 0 个自定义 asmdef；89 个 .cs 全在 Assembly-CSharp（Editor 目录 1 个脚本在 Assembly-CSharp-Editor） |
| 测试 | `Assets/Tests` 不存在；0 个 EditMode/PlayMode 测试 |
| 场景 | 唯一项目场景 `Assets/Scenes/CombatSampleScene.unity`（15 GO / 28 MonoBehaviour / 0 缺失脚本）；Player/Enemy 为场景内嵌，无 Prefab |
| 配置资产 | ActionLibrarySO ×3、AttackPattern ×10（**非任务描述的 12 个，无 R3 版本**）、UnitVolume ×3、Prefab ×3 |
| 编译 | **通过，0 编译错误**（batchmode + `-executeMethod`，exit 0；两次失败尝试的原文与修复已记录） |

## 2. 关键结论摘要

1. **唯一顶层时钟**：`CombatDemo.Update → BattleTimeline.AdvanceTime(Time.deltaTime)`（CombatDemo.cs:89）；11 个 Legacy 从属写入回调（含运行时创建的 `TimelineEditorUI.Update`）、12 个只读回调、0 处 FixedUpdate/Animator。任务 03B 建议把唯一 Bootstrap 挂在 `CombatSampleScene/CombatDemo` GO 位。
2. **数值兼容锚点（任务 08 依据）**：夹具 F1 HeavySmash 68.8 伤/击倒/1 格、F2 QuickSlash 23.64 无击退、F3 Clash 残余 100.8 伤/1 格——与主方案 0.4.2.1 新公式逐位一致（MomentumUnits=动量×100、/5000、/1000、ControlResistanceUnits=90000）。
3. **最危险残留（任务 05 必须替换）**：①体力不足中断后 AttackIntent 仍照常命中；②Intercept 不清理剩余移动事件且 `ExecuteInterruption` 为全项目零调用的死代码；③`BattleManager.EndBattle` 零清理且 timeScale=0.2 慢速续跑。
4. **45 个命名不变量验收用例**（任务包工作步骤 4 全量）已拟好前置/操作/预期/启用任务号；当前无测试基础设施，全部待任务 02/03/03B 启用——未用跳过测试冒充覆盖。
5. **02B 两个硬阻塞点**：`Pathfinder.GridPoint` 嵌套类型与 `ImpactType` 定义在 `PhysicsEngine.cs` 内，Authoring 闭包搬迁前必须拆出；asmdef 不能引用 Assembly-CSharp，闭包必须物理搬迁且 .cs 连 .meta 一起移（MonoScript GUID 表已列）。
6. **敌我迁移阻塞项**：`IsPlayerControlled` 单布尔承担阵营/胜负/AI/命令/反应五重职责；5 个 Attack 建议 `AllowedTargetRelations=Hostile` 但**无友军/中立证据，须设计确认**（Whirlwind 环形 Pattern 风险最高），未知处标为阻塞配置迁移而非默认补 Hostile。
7. **同名局部 ID**：QuickSlash 等 5 个 ID 跨库标量字段一致、Pattern 指向不同半径变体（R1/R2 形状不同）——**不得自动合并**，02B 须显式映射。
8. **当前战斗最小迁移面 = 14 资产**（ForRadius1/2 + 10×Pattern + Radius_1/2）；ForRadius3（空库）与 Radius_3 未被场景引用，02B 需决策去留。
9. **旧寻路 1/2 权重已成立**：偶数方向 w1/奇数方向 w2，Swiftness 只在选路后影响逐边时长（与新规则语义一致）；旧保护值 1000 次迭代/±100 窗口 vs 新冻结上限 4096/256/192 已分栏，默认战场测量余量巨大。
10. **性能基线框架已冻结（调度者采纳口径）**：60 Ticks/s；典型 2 单位/≤12 计划/≤6 Reservation，最大 8/64/32；30 分钟目标时长（1/10/30 分钟窗口）；Step+快照 P50≤0.5ms/P95≤1.0ms/P99≤2.0ms；每 Tick 平均分配 ≤8KB；GC 每 60s ≤1 次且单次 ≤5ms；托管内存 ≤256MB；历史归档增长 ≤10MB/30min。旧实现零埋点→旧值记为"无测量"，不伪造。

## 3. 验收标准逐项核对

| 验收标准 | 结论 | 证据位置 |
|---|---|---|
| 每个结论定位到文件/类型/运行结果/复现步骤 | ✅ | 两份记录全部 文件:行 或 GUID/复现命令 |
| ≥2 个典型片段有确定输入和可比较输出 | ✅（静态推导） | 运行时记录 §6 三片段（攻击命中/移动/死亡结束），含逐中间值公式；运行时确认待 03B |
| 五条核心不变量均有测试名/前置/操作/预期 | ✅ 45 个用例 | 运行时记录 §7 |
| 旧二体标量仲裁与新多体有向目标分栏 | ✅ | 运行时记录 §8 vs 主方案 0.4.2 |
| 动量/普通伤害/Clash 剩余/控制阈值可复算 | ✅ F1/F2/F3 | 运行时记录 §9、§10 |
| 攻击时序夹具（旧实现+新规则预期） | ✅ | 运行时记录 §10（ResolvedWindup=60/Impact=160/Recovery=30/End=190/Budget=90） |
| Block/Dodge/Move 基线分栏 | ✅ | 运行时记录 §12 |
| 寻路基线含真实场景测量与上限余量 | ✅（ExpandedNodes 实测待 06） | 运行时记录 §16 |
| 旧击退基线定位全部写入/障碍/顺序/死亡 | ✅ | 运行时记录 §17 |
| 敌我盘点覆盖命令/AOE/反应/AI/占位/胜负，逐 Attack 迁移表 | ✅（Hostile 为推断，阻塞项已标） | 运行时记录 §15 |
| 伤害盘点给显式候选映射+歧义清单 | ✅ | 资产记录 §6（5 项歧义待 02B/08 决策） |
| 终态写入地图逐项列残留 | ✅ 12 条入口+3 处危险残留 | 运行时记录 §11 |
| 时间线编辑基线逐项证明且确认删除不左吸 | ✅ | 运行时记录 §14 |
| Unity 回调清单三类区分+场景路径 | ✅ | 运行时记录 §1 |
| 战斗配置清单可追溯+同名 ID/缺失 ID/悬空引用 | ✅（无悬空引用，0 MISSING SCRIPT） | 资产记录 §1-3 |
| Authoring 闭包+asmdef 不可行性说明 | ✅ | 资产记录 §5 |
| 无法自动化原因明确，不用跳过冒充 | ✅ | 两份记录均已声明（0 测试基础设施+AI 随机性） |
| Git 差异只含基线与最小测试接缝 | ✅ | 见 §5 足迹 |

## 4. 与任务描述的差异与处理

1. **动作资产数量**：任务描述 12 个，实际 10 个（无 R3）——以资产事实为准，已记录。
2. **"仅战斗结束清完整"的表述修正**：旧系统现实是死亡/击退路径清理完整，**战斗结束恰恰零清理**（最危险残留），已如实记录。
3. **`Assets/Editor/Diagnostics` 空目录**：诊断工作期间产生的空占位目录（两位专家均未认领），调度者已删除该空目录及 Unity 生成的 2 个 .meta，工作区足迹已收敛。
4. **batchmode 沙箱限制**：桌面环境用任务给定 pwsh 命令即可；本沙箱因 `%LOCALAPPDATA%\Unity` 写入受限，需经 harness `unity run` 变体（等价命令见资产记录 §8.3）。

## 5. 任务 01 全部产物与 git 足迹

| 产物 | 归属 |
|---|---|
| `优化任务/执行记录/01-基线记录.md`（本文档，规范合并版） | 调度者 |
| `优化任务/执行记录/01-基线记录-运行时与代码链.md` | 架构师 |
| `优化任务/执行记录/01-基线记录-资产配置与程序集.md` | 编辑器工具开发者 |
| `优化任务/执行记录/01-诊断输出.txt` | 编辑器工具开发者（诊断工具输出） |
| `Assets/Scripts/Editor/BaselineDiagnostics/BaselineDiagnosticRunner.cs` + .meta | 编辑器工具开发者（只读诊断工具，菜单+`-executeMethod` 双入口，可随时重跑回归） |

git 复核：仅新增上述未跟踪文件；**零既有文件修改**（无 M/D）；Logs 在 gitignore 内。无玩法改动、无旧字段删除、无全仓格式化。

## 6. 交接模板

```text
任务：01 - 基线、清单与不变量测试
结果：完成（静态基线与编译验证全部交付；运行时片段与 45 个用例的自动化按总则诚实标注待验证）

已实现：
- 战斗入口/回调三类分类地图 + 场景挂载反查表 + 03B 所有权建议
- UnityEngine 依赖/标识来源/责任归属盘点
- 45 个命名不变量验收用例（前置/操作/预期/启用任务号）
- CombatArbiter 能力边界、PhysicsEngine 数值链夹具 F1/F2/F3（与新公式逐位一致）、ActionScheduler 时序夹具
- 12 条计划终态入口清理地图（3 处危险残留）
- Block/Dodge/Move/敌我/伤害/时间线编辑/寻路/击退基线（全部 characterization 分栏）
- 资产 GUID/fileID/字段/引用/同名 ID 清单 + Authoring 依赖闭包（2 个硬阻塞点）
- 编译验证：0 错误、exit 0（复现命令已记录）；长战斗性能阈值框架冻结

关键契约：
- 数值锚点：F1(68.8/击倒/1格)、F2(23.64/无击退)、F3(Clash 100.8/1格)
- 03B 唯一 Bootstrap 位：CombatDemo GO；Legacy 从属写入者登记表见运行时记录 §1.1
- 02B 前置拆分：Pathfinder.GridPoint 独立化、ImpactType 出 PhysicsEngine；QuickSlash 类同名 ID 不得自动合并
- 性能冻结口径见 §2.10

验证：
- 编译：batchmode exit 0，0 编译错误（原文与复现步骤见资产记录 §8）
- git：工作树仅新增文件，零既有修改
- 45 用例与 3 片段运行时确认待任务 02/03/03B 基础设施

改动范围：
- 见 §5 产物表（4 份记录文件 + 1 个只读诊断脚本及其 .meta）

未完成或未验证：
- 3 个典型片段的运行时确认（AI 随机性→建议 03B 隐藏场景固定输入脚本）
- 旧 ExpandedNodes 实测值（任务 06 只读夹具）
- 逐 Attack AllowedTargetRelations 需设计确认（02B 阻塞项）；伤害迁移 5 项歧义待 02B/08 决策

对后续任务的约束：
- 任务 02：asmdef 不得引用 Assembly-CSharp；先拆 GridPoint 与 ImpactType
- 任务 05：终态协调器必须替换 3 处危险残留；禁止沿用 CancelGroup+Schedule 作为权威排程编辑
- 任务 08：数值锚点用 F1/F2/F3；不得继承 pairwise 顺序、逐单位击退提交、Reservation 先到先得
- 任务 03B：覆盖运行时创建的 TimelineEditorUI 与 NextActionPreview（DontDestroyOnLoad）；New 模式前须证明 Time.timeScale 4 个写入点不再影响逻辑 Tick 率
- 任务 11：性能阈值按 §2.10 冻结口径执行，旧值不伪造
```

## 7. 调度者备注

- 任务 01 达到验收标准，可以作为任务 02（程序集边界与纯数据）的前置输入。按 00 号总则默认顺序，下一任务是 **02**（前置 01，已满足）。
- 两个待设计决策（AllowedTargetRelations 人工确认、伤害迁移 5 项歧义）需要在 02B/08 动工前由用户拍板；已作为阻塞项写入记录，不阻塞 02。
- 诊断工具 `BaselineDiagnosticRunner` 可随时重跑（只读），建议任务 02/03 完成后重跑一次作为回归对比。
